{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.jingshanghui.example/t2/sprint3/sync-event-envelope.v1.schema.json",
  "title": "T2-SYN-001 immutable POS event envelope",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "eventId", "stream", "eventType", "eventVersion", "aggregateId", "aggregateVersion",
    "deviceId", "storeId", "terminalId", "sequenceNo", "occurredAt",
    "idempotencyKey", "correlationId", "payloadHash", "payload"
  ],
  "properties": {
    "eventId": {"type": "string", "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"},
    "stream": {"type": "string", "pattern": "^[a-z][a-z0-9.-]{0,63}$"},
    "eventType": {"type": "string", "pattern": "^[a-z][a-z0-9.-]{0,95}\\.v[1-9][0-9]*$"},
    "eventVersion": {"type": "integer", "minimum": 1},
    "aggregateId": {"type": "string", "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"},
    "aggregateVersion": {"type": "integer", "minimum": 1},
    "deviceId": {"type": "string", "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"},
    "storeId": {"type": "string", "pattern": "^[1-9][0-9]{0,18}$"},
    "terminalId": {"type": "string", "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"},
    "sequenceNo": {"type": "integer", "minimum": 1},
    "occurredAt": {"type": "string", "format": "date-time"},
    "idempotencyKey": {"type": "string", "minLength": 16, "maxLength": 128},
    "correlationId": {"type": "string", "minLength": 16, "maxLength": 64},
    "payloadHash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "payload": {"type": "object"}
  }
}
