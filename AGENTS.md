# 鲸熵汇收银系统研发强制规则

本仓库用于可商业落地的多租户收银系统。任何人工或 AI 变更都必须遵守本文件；目录内若有更具体的 `AGENTS.md`，只允许收紧规则，不允许降低本文件的资金、库存、租户、安全和验收底线。

## 1. 需求权威与冲突处理

- 范围权威：`docs/连接器型商业收银经营平台_商业可落地优化方案_V3.0.md` 的商业 V1 边界。
- 业务行为权威：`docs/详细设计/31` 至 `39`。
- 技术实现权威：`docs/连接器型商业收银经营平台_技术架构与开发规范_V1.0.md`。
- 商业验收权威：`docs/详细设计/40_商业V1验收测试计划_V1.0.md`。
- `完整功能规划_V2.0` 是全景 Backlog，不代表全部进入商业 V1。
- 已批准变更必须记录在 `docs/governance/change-log.md`，架构决策必须记录为 ADR。
- 发现冲突、缺失或存在多种会改变资金/库存/租户事实的解释时，停止该部分实现并提出决策，不得自行猜测。

## 2. 开发准入

每个功能进入编码前必须具备：

1. 唯一 `requirement_id`，并登记在 `docs/governance/rtm.csv`；
2. 来源文档与章节、范围、非目标和可执行验收标准；
3. 涉及核心业务时，明确状态机、不变量、权限、审计、幂等和失败补偿；
4. 涉及跨模块时，先批准 API/事件/数据契约和数据主权；
5. 涉及数据库时，先定义 Flyway 迁移、兼容、容量和回滚/前向修复；
6. 涉及设备时，明确认证机型、SDK、实机和模拟器用例。

## 3. 不可突破的领域底线

- `tenant_id` 必须由可信上下文注入；客户端提交的租户标识不得直接成为授权依据。
- RuoYi 平台主键沿用 `BIGINT`；离线可创建的业务实体使用 ULID；业务 `tenant_id` 为 `VARCHAR(20)`。
- 结算金额使用最小货币单位整数并保存币种；单价、成本、税率和中间值使用 `BigDecimal/DECIMAL`，禁止 `float/double`。
- 订单、支付、退款、库存、积分和储值事实不得物理删除或用覆盖更新抹去历史，只能以状态迁移、冲正或新账本记录修正。
- 支付 `UNKNOWN` 必须查询或对账确认，禁止自动再次扣款。
- 库存只能通过不可变账本变化；可用量、在手量和预占量必须可重算、可对账。
- 优惠分摊必须金额守恒；退款沿用成交快照，不得按当前促销重算。
- POS 本地交易、支付意图和 Outbox 必须在同一个 SQLite 事务中提交；同步必须幂等并允许重复和乱序。
- Redis、搜索、报表和消息系统不得成为订单、支付或库存唯一事实来源。
- 核心系统不得针对鲸熵汇、美团、抖音、京东或其他渠道写特殊分支；渠道差异只能进入连接器与映射层。

## 4. T0 边界

T0 只允许：治理、ADR、RTM、工程骨架、契约骨架、开发环境、CI、健康检查、模拟器和构建验证。

T0 禁止：正式交易逻辑、真实支付、库存扣减、促销计算、生产密钥、生产数据和未经批准的 V1 外功能。

## 4.1 当前 T1-Prep 阶段边界

- 当前唯一技术基线为 annotated tag `t0-baseline-2026-08-16`；T1-Prep 变更必须从该 tag 建立独立评审分支。
- T1-Prep 只允许项目章程、范围与非目标、RTM、ADR、PoC 方案、选型输入、测试与故障注入计划、CI/证据规范、风险和启动评审材料。
- 未经项目发起人明确 Go 确认，不得进入 T1 PoC 编码；不得开发订单、支付、退款、库存、促销等正式商业业务。
- 真实设备型号/SDK、至少一个真实支付沙箱及设计伙伴未落实的相关需求必须标记 `BLOCKED`；允许使用 Fake 验证契约，但 Fake 证据不得替代实机、支付沙箱或商业验收。
- 鲸熵汇连接器资料按项目发起人指令延后收集，在收银系统主体完成前保持 `DEFERRED`；不得猜测其接口、鉴权、限流或回调行为。
- 禁止真实支付、生产密钥、生产终端证书和未脱敏商户数据进入 T1 环境、仓库、日志、制品或测试证据。
- Aviator、simple-http、MySQL Connector/J 的法务或替换事项不阻断 T1-Prep 和风险 PoC，但仍是商业发布前强制阻断项，不得静默关闭。
- GitHub 不升级套餐；继续使用仓库自有依赖差异、SBOM、漏洞、许可证、密钥和 IaC 补偿门禁。

## 4.2 当前 T1 Week 1 条件准入

- 项目发起人已于 2026-08-16 确认 `CONDITIONAL GO`；只允许 RTM 中已经通过准入并转为 `IN_PROGRESS` 的 `STATIC/FAKE` 风险 PoC。
- Week 1 仅允许稳定契约、合成数据、Fake Provider/Device、可复现故障夹具、隔离 PoC 探针、T1 CI 与证据；所有探针必须位于可整体删除的 `poc/`、测试或专用契约目录。
- `T1-HWD-002`、`T1-PRN-001`、`T1-SCN-001`、`T1-SCL-001`、`T1-IO-001`、`T1-PAY-002`、`T1-PAR-001` 继续 `BLOCKED`，不得以模拟器/Fake 关闭。
- 支付候选池为十家，T1 只从中评选五家运行 Fake 契约并最多准入一家真实沙箱；当前 Week 1 禁止沙箱调用和真实支付。
- Week 1 完成后必须提交周门禁报告并再次等待项目发起人确认；不得自动进入 Week 2 或 T2。

## 4.3 当前 T1 Week 2 条件准入

- 项目发起人已于 2026-08-16 确认《T1 Week 1 周门禁报告》并授权 Week 2；Week 1 证据继续只代表 `STATIC/FAKE`，不得升级为外部或商业结论。
- Week 2 只允许 `T1-OFF-001`、`T1-SYN-001`、`T1-TEN-001`、`T1-DPK-001`、`T1-UPG-001`、`T1-PAY-001`、`T1-SEC-001`、`T1-CI-001` 的隔离内部探针；`T1-HWD-001` 仅保持 Week 1 结果，不在本周扩张。
- 允许 SQLite 合成事务/Inbox、虚构租户攻击、临时生成的 10k/100k 合成数据包、虚构 App/Schema 升级和五家支付 Fake 回归；禁止创建正式业务表、API、页面、支付 SDK 或真实网络调用。
- SQLite 的 `syn_fact`、`syn_intent`、`syn_outbox` 等仅为可整体删除的风险模型，不能被复用或宣称为正式订单、支付、退款、库存或促销实现。
- 10k/100k 数据包的 CI 结果只记录 `FAKE` 趋势；在主认证 Android 设备缺失时，不得形成设备性能结论。
- `T1-HWD-002`、`T1-PRN-001`、`T1-SCN-001`、`T1-SCL-001`、`T1-IO-001`、`T1-PAY-002`、`T1-PAR-001` 继续 `BLOCKED`；不得创建绿色占位或用本周结果关闭。
- 支付探针不得导入网络客户端，不得读取沙箱/生产凭据；五家 Fake 回归不等于机构接入、签约或可商用。
- Week 2 完成后必须提交周门禁报告并再次等待项目发起人确认；不得自动进入 Week 3 或 T2。

## 4.4 当前 T1 Week 3 条件准入

- 项目发起人已于 2026-08-16 确认《T1 Week 2 周门禁报告》并授权 Week 3；既有 Week 1/2 结果继续只代表 `STATIC/FAKE`。
- Week 3 只允许 `T1-OFF-001`、`T1-SYN-001`、`T1-TEN-001`、`T1-DPK-001`、`T1-UPG-001`、`T1-PAY-001`、`T1-SEC-001`、`T1-CI-001` 的隔离交叉故障、最终收敛、安全和证据探针；`T1-HWD-001` 只做既有回归，不扩张设备范围。
- 允许 Outbox 出队/ACK 丢失/积压恢复、Inbox 重复乱序与重启叠加、支付 `UNKNOWN`/回调/退款/合成对账、数据包下载中断/切换 kill/重放、待同步升级/兼容窗口/前向修复以及固定失败 seed 机制。
- 所有新增数据库对象必须使用 `syn_` 前缀并位于临时目录；所有包、App、Schema、租户、支付和对账数据必须为虚构合成数据；不得接入正式模块或创建生产迁移。
- 支付探针继续禁止网络客户端、凭据和沙箱调用；`UNKNOWN` 不得触发自动二次扣款，退款 `UNKNOWN` 必须通过查询或回调收敛。
- 失败 seed 必须写入机器可校验清单并纳入后续回归；没有观察到失败时必须明确记录空集合，不得伪造失败或隐去失败。
- `T1-HWD-002`、`T1-PRN-001`、`T1-SCN-001`、`T1-SCL-001`、`T1-IO-001`、`T1-PAY-002`、`T1-PAR-001` 继续 `BLOCKED`，不得创建绿色占位。
- Week 3 完成后必须提交周门禁报告并再次等待项目发起人确认；不得自动进入 Week 4、T2 或正式商业开发。

## 4.5 当前 T1 Week 4 条件准入

- 项目发起人已于 2026-08-16 确认《T1 Week 3 周门禁报告》并授权 Week 4；Week 4 只允许重复验证、清理、证据汇总和 T1 退出评审准备。
- 必须在 GitHub Ubuntu 与 Windows 两类干净执行器上复跑 Week 1—3；跨平台比较使用排除时间戳和性能趋势字段后的归一化语义证据，同时原始证据必须独立保留，不得篡改为字节一致。
- 允许新增机器可读的复现摘要、T1 PoC SBOM/许可证清单、安全审计、证据总清单、失败 seed 总账、运行手册、风险/成本/架构建议和退出评审报告；禁止新增风险模型以外的业务行为。
- 任何观察到的失败 seed、P0/P1 缺陷、摘要不一致或证据分级违规必须阻断退出建议；不得通过删除用例、跳过平台、忽略错误或放宽阈值收口。
- `T1-HWD-002`、`T1-PRN-001`、`T1-SCN-001`、`T1-SCL-001`、`T1-IO-001`、`T1-PAY-002`、`T1-PAR-001` 继续 `BLOCKED`，不得改为 `ACCEPTED` 或创建绿色占位。
- Week 4 只能形成 T1 退出和 T2 的 `GO`、`CONDITIONAL GO` 或 `NO-GO` 建议；未经项目发起人确认不得结束 T1、创建 T1 完成 tag、进入 T2-Prep 或开发正式业务。

## 4.6 当前 T2-Prep 条件准入

- 项目发起人已于 2026-08-16 接受 T1 内部 `STATIC/FAKE` 风险基线 `CONDITIONAL PASS`，只授权从 Week 4 最终封存提交 `962c4ed5e631bccd5c6fff737ed8e97fb665fd03` 建立 T2-Prep 候选分支。
- T2-Prep 只允许项目章程、范围/非目标、商业 V1 冻结清单、唯一 RTM、模块准入依赖、详细设计 31—40 复核、迭代/RACI、环境/迁移/测试/CI、灰度/回退、外部解阻和启动评审材料。
- 未经项目发起人确认《T2 正式开发启动评审报告》，不得开发正式租户、商品、价格、POS、订单、支付、退款、库存、促销、会员、采购或报表业务能力。
- 正式业务需求在 T2-Prep 中只能保持 `DRAFT`、`BLOCKED` 或 `DEFERRED`；不得以文档、T1 Fake 或静态校验把业务需求标记为 `IMPLEMENTED`、`VERIFIED` 或 `ACCEPTED`。
- `T1-HWD-002`、四类外设、`T1-PAY-002`、`T1-PAR-001` 及其 T2 映射项继续 `BLOCKED`；鲸熵汇和商业许可证事项继续 `DEFERRED`。低等级证据不得解除高等级阻断。
- 商业 V1 以便利店为主样板；零食折扣店和社区超市只通过版本化行业模板表达差异，不得复制核心状态机或在核心领域中按业态写条件分支。
- 模块准入顺序为租户/组织/门店与安全基座、商品/条码/价格、POS/班次/订单/现金与离线、支付/退款、库存/采购/成本、基础促销/会员/报表；跨模块契约和数据主权先于实现。
- T2-Prep 候选 annotated tag 只能在全部本地与 GitHub 门禁通过、目标提交明确并由项目发起人再次确认后创建；当前阶段只准备 tag 方案，不得执行创建或推送。
- T2-Prep 完成后必须提交启动评审报告并等待确认；不得自动进入 T2 编码，不得宣称系统已具备商用能力。

## 4.7 T2-Prep 已封存、Gate 0/1 待准入

- 项目发起人已于 2026-08-16 接受 T2-Prep `CONDITIONAL PASS`；annotated tag `t2-prep-baseline-2026-08-16` 已按授权指向 `557ba270479935d6b44968cf70b47033f7d3d656` 并推送核验。
- T2-Prep 治理需求可记录为 `ACCEPTED`，但所有正式 `T2-*` 业务需求继续保持 `DRAFT/BLOCKED/DEFERRED`，不得因 tag 封存自动转为 `READY`。
- 在项目发起人另行确认 Gate 0/1 首波启动指令前，仍禁止正式租户、组织、门店、商品、价格、订单、支付、库存、促销等业务编码。
- ADR-019 保持 `Proposed`；只有正式首波范围、Requirement ID 和准入门禁获批后才可转 `Accepted`。
- Tag 封存报告、RTM 接受状态、CI 和证据索引属于基线后的治理记录，不得移动、覆盖或重建已发布 tag。

## 4.8 当前 T2 Gate 0 / Sprint S0 条件准入

- 项目发起人已于 2026-08-16 确认以 annotated tag `t2-prep-baseline-2026-08-16` 为唯一不可变技术基线，并授权从治理提交 `7fe4391069d8ee6c641d1b3e509d9f90050be5ef` 建立 `t2/gate0-sprint0-20260816`。
- Gate 0 正式实现只允许 `T2-IAM-001`、`T2-ORG-001`、`T2-RBAC-001`、`T2-CFG-001`、`T2-AUD-001`、`T2-SEC-001`、`T2-OBS-001`、`T2-MIG-001`；每项必须先通过准入清单才可从 `DRAFT` 转为 `READY/IN_PROGRESS`。
- Gate 1 的 `T2-PRD-001..004`、`T2-PRC-001..002`、`T2-DPK-001` 只允许领域模型、OpenAPI、事件/Schema、迁移设计、合成向量和验收用例准备，状态必须保持 `DRAFT`，不得生成正式业务实现。
- Gate 0 采用 `jshpos-foundation` 模块承载领域与应用逻辑；RuoYi 的认证会话、租户拦截、角色/菜单权限、部门数据范围和操作日志可以作为平台适配器复用，但不得把鲸熵汇领域规则写入 `ruoyi-system`、Controller 或通用工具类。
- 所有 `jsh_*` 租户表访问必须在可信租户上下文缺失时失败关闭；客户端参数、Header、导出路径、缓存键、任务参数或对象存储键中的租户值不得直接成为授权依据。
- 测试数据只允许两个以上虚构租户和多组织/多门店合成数据；真实支付、生产密钥、未脱敏数据、SANDBOX、REAL_DEVICE 和 PILOT 证据继续禁止或保持原阻断状态。
- 订单、支付、退款、库存、采购、成本、促销及 Gate 1 商品/价格正式实现继续禁止。Gate 0 周门禁报告须经项目发起人确认后才可启动 Gate 1 正式编码。

## 4.9 当前 T2 Gate 1 / Sprint S1 条件准入

- 项目发起人已于 2026-08-16 确认《T2 Gate 0 / Sprint S0 周门禁报告》，接受 Gate 0 `CONDITIONAL PASS`，并授权从 Gate 0 封板提交 `cf2ef29bd74c5d0f8fa5845a689305ebb56c7ef2` 建立 `t2/gate1-sprint1-20260816`。
- Gate 0 的 `T2-IAM-001`、`T2-ORG-001`、`T2-RBAC-001`、`T2-CFG-001`、`T2-AUD-001`、`T2-SEC-001`、`T2-OBS-001`、`T2-MIG-001` 可按本次确认更新为 `ACCEPTED`。
- Gate 1 正式实现只允许 `T2-PRD-001`、`T2-PRD-002`、`T2-PRD-003`、`T2-PRD-004`、`T2-PRC-001`、`T2-PRC-002`、`T2-DPK-001`；每项必须先完成数据主权、状态、不变量、权限、审计、API/事件、Flyway、容量、回退和测试准入。
- Gate 1 使用独立 `jshpos-catalog` 模块承载商品、价格、导入和数据包服务；可依赖 `jshpos-foundation` 的可信上下文、审计和可观测性，但不得反向写入平台表或修改 RuoYi 系统模块。
- 商品采用 SPU+SKU；租户内标准条码全局唯一并按字符串保存；数量和单位换算使用 `BigDecimal/DECIMAL` 或受约束整数分子分母；销售金额使用 CNY 最小货币单位整数。
- 商品导入必须先完整预检到 staging，再通过版本指针发布；任何错误不得形成部分可见商品。回退使用状态和补偿，不物理删除已经发布或被后续修改的数据。
- 价格采用版本化价格簿；门店价优先于租户基础价，同范围同优先级的有效期不得重叠；发布后内容不可变，失败发布不得改变上一有效版本。
- 数据包必须绑定可信 tenant/store、Schema、版本、摘要和签名；签名私钥不得进入仓库、配置、日志或测试制品。Gate 1 只实现服务端包构建/校验与标准契约，不以此替代 Android 端原子安装或实机性能证据。
- 仅允许两个以上虚构租户、多组织、多门店和三业态合成数据；10k/100k 测试只形成服务器端合成容量证据，不形成生产或实机性能承诺。
- 订单、支付、退款、库存、采购、成本、促销及 T2 后续 Gate 继续禁止；真实支付、生产密钥、未脱敏数据、SANDBOX、REAL_DEVICE 和 PILOT 状态不得因 Gate 1 内部证据改变。
- Gate 1 完成后必须提交周门禁报告并等待项目发起人确认；不得自动启动 Gate 2，不得宣称系统完成 Alpha、可试点或可商用。

## 4.10 当前 T2 Gate 2 / Sprint S2 条件准入

- 项目发起人已于 2026-08-16 确认《T2 Gate 1 / Sprint S1 周门禁报告》，接受 Gate 1 `CONDITIONAL PASS`，并授权从 Gate 1 封板提交 `6a94bc6af2938fba6b9a1af123eb94b6312af9b2` 建立 `t2/gate2-sprint2-20260816`。
- Gate 1 的 `T2-PRD-001..004`、`T2-PRC-001..002`、`T2-DPK-001` 可按本次确认更新为 `ACCEPTED`。
- Gate 2 正式实现只允许 `T2-POS-001..005`、`T2-ORD-001..002`、`T2-OFF-001`；每项必须先完成数据主权、状态与冻结点、金额不变量、权限审计、API/事件、SQLite/Flyway、容量、回退和测试准入，才可从 `DRAFT` 更新为 `READY/IN_PROGRESS`。
- `T2-SYN-001` 本 Sprint 仅允许 Inbox/Outbox 同步契约、冲突矩阵、测试向量和故障恢复方案，状态保持 `DRAFT`；不得实现网络 Worker、Push/Pull/Ack、服务端 Sync Inbox 或正式远程同步运行时。
- Gate 2 使用独立 `jshpos-order` 服务端模块与 Flutter `features/checkout`/`features/shift`/`infrastructure/local_database` 边界；可依赖 Gate 0/1 的可信上下文、组织门店与商品价格契约，不得把交易规则写入 Controller、通用工具类、RuoYi 系统模块或设备适配插件。
- POS 正式 SQLite 模型必须重新设计并使用 `local_*` 正式表，严禁复制、迁移或引用 T1 `syn_*` 探针表；本地现金成交的订单、订单行、成交快照、现金事实、班次现金流水、打印任务、Outbox 与幂等结果必须在一个 SQLite 事务中提交。
- 金额使用最小货币单位整数，数量和单位换算使用精确十进制或整数分数；订单快照一经提交不可覆盖，现金收款必须绑定可信租户、门店、终端、员工、班次和业务日快照。
- 本 Sprint 只准入现金，不准入第三方/聚合支付、退款、库存、采购、成本、促销或后续 Gate；实机/外设、支付沙箱和设计伙伴继续 `BLOCKED`，鲸熵汇与商业许可证继续 `DEFERRED`。
- Gate 2 完成后必须提交周门禁报告并等待项目发起人确认；不得自动启动 S3、正式同步或 Gate 3，不得宣称系统完成 Alpha、可试点或可商用。

## 4.11 当前 T2 Sprint S3：正式同步收口与 Gate 3-Prep 条件准入

- 项目发起人已于 2026-08-16 确认《T2 Gate 2 / Sprint S2 周门禁报告》，接受 Gate 2 `CONDITIONAL PASS`，并授权从 Gate 2 最终提交 `968ae7be34ab144c970e5c92fb7ffbddf60bf5e1` 建立 `t2/gate23-sprint3-20260816`。
- Gate 2 的 `T2-POS-001..005`、`T2-ORD-001..002`、`T2-OFF-001` 可按本次确认更新为 `ACCEPTED`。
- S3 唯一允许正式实现的需求是 `T2-SYN-001`；必须先冻结数据主权、事件状态、ACK、单调游标、冲突、权限、审计、API/Schema、迁移、容量、故障恢复和回退，才可从 `DRAFT` 进入 `READY/IN_PROGRESS`。
- POS Outbox、云端 Sync Inbox、领域业务事实和 ACK 各自必须有明确事务边界；网络为至少一次传输，业务效果通过稳定 event_id、payload hash、Inbox/Outbox 和领域幂等实现一次。
- ACK 丢失、请求超时或进程重启必须复用原 event_id、payload 和 hash；禁止重新生成业务命令处理未知结果，禁止静默删除未 ACK 事件。
- tenant_id 只能来自可信设备/服务端认证上下文；请求体、Header、任务、缓存、导出、对象存储键或客户端游标不得直接成为租户授权依据。设备 ID 和门店 ID 必须经服务端注册表与可信租户交叉校验。
- `T2-PAY-001`、`T2-PAY-003`、`T2-REF-001`、`T2-REC-001` 本 Sprint 只允许领域状态机、端口、OpenAPI/事件 Schema、合成向量和验收用例准备，继续保持 `DRAFT`；不得创建正式支付、退款或对账运行时。
- `T2-PAY-002` 继续 `BLOCKED`；支付沙箱资料齐备后只能提交独立解阻评审，未经项目发起人确认不得网络调用。真实资金、生产密钥和未脱敏数据继续禁止。
- 库存、采购、成本、促销及后续 Gate 正式实现继续禁止；硬件、支付沙箱和设计伙伴保持 `BLOCKED`，鲸熵汇与商业许可证保持 `DEFERRED`。
- S3 完成后必须提交周门禁报告并等待项目发起人确认；不得自动进入 Gate 3 正式支付编码，不得宣称系统完成 Alpha、可试点或可商用。

## 4.12 当前 T2 Gate 3A：Provider 无关支付核心条件准入

- 项目发起人已于 2026-08-16 确认《T2 Sprint S3 同步收口与 Gate 3-Prep 周门禁报告》，接受 `T2-SYN-001 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；Gate 3A 从已通过全量门禁的治理提交 `60828acbbe18c30914a076709905eeb525ffeb5f` 建立分支 `t2/gate3a-sprint4-20260816`。
- Gate 3A 正式实现只允许 `T2-PAY-001`、`T2-PAY-003`、`T2-REF-001`、`T2-REC-001`；每项必须先冻结数据主权、状态机、不变量、权限、审计、API/事件、Flyway、容量、回退、Fake 固定向量和 CI 准入。
- `jshpos-payment` 是支付、支付尝试、Provider 观察值、退款、对账差异和支付审计的唯一写入 Owner；订单模块只通过受控只读端口提供原单成交快照，禁止支付 Mapper 直接修改订单表。
- 支付 `UNKNOWN` 只允许用原 attempt/request 标识经可信查询观察、验签回调观察或账单观察收敛；禁止自动创建第二次扣款。到达顺序不决定资金状态，成功资金事实不可回退，冲突必须隔离和审计。
- 退款只允许关联原成功电子支付与不可变原单快照；处于 `PROCESSING/UNKNOWN/SUCCEEDED` 等占额状态的累计退款金额和数量不得超过原可退上限，超额和跨租户请求必须失败关闭。
- Provider 观察合并必须通过内部应用端口；Gate 3A 禁止创建 Provider HTTP/SDK 适配器、外部回调 Controller、账单下载器、凭据配置或任何支付网络调用。Fake 只允许位于测试范围并标记 `FAKE`。
- `T2-PAY-002` 继续 `BLOCKED`。未经独立支付沙箱解阻评审和项目发起人确认，不得访问沙箱/生产端点，不得使用生产密钥、真实资金或未脱敏支付数据。
- 库存、采购、成本、促销及后续 Gate 继续禁止；`SANDBOX`、`REAL_DEVICE`、`PILOT` 证据不得由 Fake 替代。Gate 3A 完成后必须提交周门禁报告并等待确认，不得宣称 Alpha、可试点或可商用。

## 4.13 当前 T2 Gate 3B-Prep / Gate 4A Sprint S5 条件准入

- 项目发起人已于 2026-08-16 确认《T2 Gate 3A 周门禁报告》，接受 `T2-PAY-001`、`T2-PAY-003`、`T2-REF-001`、`T2-REC-001` 更新为 `ACCEPTED`，并授权从 Gate 3A 封存提交 `451a48f982d3a88c68ff20ca283a190e7bf53ccf` 建立 `t2/gate4a-sprint5-20260816`。
- Gate 3B-Prep 只允许首接 Provider、授权沙箱、测试终端、正式接口、签名验签、回调、退款、账单、限流、网络和技术联系人的资料核验及安全方案；`T2-PAY-002` 继续 `BLOCKED`，未经独立解阻评审确认不得创建网络适配器、配置凭据或发起 Provider 网络调用。
- Gate 4A 正式实现只允许 `T2-INV-001`、`T2-INV-002`、`T2-INV-004`；必须先冻结数据主权、状态、不变量、权限、审计、API/事件、Flyway、容量、回退、合成向量和 CI 准入。
- `jshpos-inventory` 是库存命令、不可变数量流水、可重建余额、负库存策略版本、库存异常、审计和库存 Outbox 的唯一写入 Owner；订单和支付退款只通过受控只读端口提供权威来源快照，客户端不得自报租户、SKU、出入库数量或资金结果。
- 便利店正式默认策略为 `DENY`；受控 `ALLOW_AND_ALERT` 必须绑定不可变策略版本并形成负库存异常和审计。普通 POS 即时销售本阶段不建立预占，外部订单预占保持后续设计项。
- 销售完成和成功原单退货分别生成 `SALE_OUT`、`SALE_RETURN_IN`，来源事件重放只能返回原结果；流水、余额、命令结果、审计和 Outbox 必须同事务提交。数量使用 `DECIMAL(19,6)/BigDecimal`，禁止浮点数。
- `T2-INV-003`、`T2-PUR-001`、`T2-CST-001`、`T2-TRF-001` 只允许设计、契约和测试准备，状态保持 `DRAFT`；禁止盘点、采购、成本、调拨和促销正式运行时。
- 仅允许虚构租户、组织、门店、仓库、商品、订单和退款合成数据；`SANDBOX`、`REAL_DEVICE`、`PILOT` 继续不能由 Fake 替代。完成后必须分别提交 Gate 3B 准备报告和 Gate 4A 周门禁报告等待确认。

## 4.14 当前 T2 Gate 4B / Sprint S6 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 4A / Sprint S5 周门禁报告》，接受 `T2-INV-001`、`T2-INV-002`、`T2-INV-004` 更新为 `ACCEPTED`，并授权从 Gate 4A 最终治理提交 `9557112a4eb573ec4a54ef30be477f1ab8f09d31` 建立 `t2/gate4b-sprint6-20260817`。
- Gate 4B 正式实现只允许 `T2-INV-003` 与 `T2-PUR-001`；必须先冻结数据主权、状态机、不变量、权限、审计、API/事件、Flyway、容量、回退、固定合成向量和 CI 准入。
- 动态盘点必须保存账面快照、每次计数事实、截止序号、校准账面量、差异、复核和审批责任链；入账只能调用库存 Owner 追加 `STOCKTAKE_GAIN/LOSS`，禁止把余额覆盖为实盘数。
- `jshpos-procurement` 独占供应商、采购订单、收货和采购退货单据；采购订单不改变库存，只有已确认收货和获批原收货退货可通过内部受控端口调用 `jshpos-inventory`，并与库存命令、流水、余额、审计和 Outbox 同事务提交。
- 采购数量使用 `DECIMAL(19,6)/BigDecimal` 和冻结单位换算；采购价格只作为采购商业快照保存，`T2-CST-001` 继续 `DRAFT`，本 Sprint 禁止创建成本余额、成本流水、移动平均成本或财务入账运行时。
- `T2-TRF-001` 继续 `DRAFT`，只允许调拨状态、不变量、契约和测试向量准备；禁止调拨发出、在途或收货运行时。支付 `T2-PAY-002` 继续 `BLOCKED`，Provider 网络调用必须为 0。
- 所有盘点和采购数据使用可信租户与门店范围，覆盖 Mapper、XML、任务、缓存、导出和对象存储攻击；仅允许虚构合成数据，`SANDBOX`、`REAL_DEVICE`、`PILOT` 继续不能由 Fake 替代。
- 完成后必须提交《T2 Gate 4B / Sprint S6 周门禁报告》等待确认；不得自行进入成本、调拨、促销或后续 Gate，不得宣称 Alpha、可试点或可商用。

## 4.15 当前 T2 Gate 4C / Sprint S7 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 4B / Sprint S6 周门禁报告》，接受 `T2-INV-003`、`T2-PUR-001` 更新为 `ACCEPTED`，并授权从 Gate 4B 最终报告提交 `02e1854a9b2a63a46323f27457bd61a708f740a9` 建立 `t2/gate4c-sprint7-20260817`。
- Gate 4C 正式实现只允许 `T2-CST-001`；必须先冻结成本数据主权、输入事实、状态与顺序、不变量、权限、审计、API/事件、Flyway、容量、回退、固定向量和 CI 准入。
- `jshpos-costing` 是库存领域内成本策略、不可变成本流水、可重建成本余额、出库成本快照、成本审计和成本 Outbox 的唯一写入 Owner；不得直接修改 `inv_stock_balance`、`inv_stock_ledger`、`pur_*` 或历史成本流水。
- 成本效果只能由库存 Owner 在同一数据库事务内通过进程内受控端口提交已落库的库存流水事实；采购收货和采购退货成本还必须由采购 Owner 只读端口提供已确认原单价格、冻结换算和原收货关系。外部 Controller 不得直接构造成本事实或提交 tenant_id、单位成本和资金结果。
- 商业 V1 仅准入 `WAREHOUSE` 成本范围和 `CNY`；数量使用 `DECIMAL(19,6)/BigDecimal`，单位成本和成本金额使用以最小货币单位计量的 `DECIMAL(25,6)/BigDecimal`，舍入固定为 `HALF_EVEN`，禁止浮点数。
- 成本流水只追加，余额是可丢弃投影。库存流水序号必须按成本维度单调处理；重复输入返回原结果，同库存流水异摘要、顺序缺口和不可解释晚到必须失败关闭并进入重建/人工修复，不得按到达顺序静默改写历史。
- 销售和盘亏按事务时移动平均成本出库；销售退货优先使用原销售成本；采购收货使用冻结采购价换算；采购退货使用关联原收货成本；负库存只允许使用已有最近有效成本并标记估计，后续入库差异通过新差异事实表达，不回写历史出库。
- `T2-TRF-001` 继续 `DRAFT`，只允许补充调拨领域模型、状态机、不变量、API/事件、Flyway 设计、固定向量和验收用例；禁止创建调拨运行时、表、Controller、任务或库存移动。
- `T2-PAY-002` 继续 `BLOCKED`；未经独立支付沙箱解阻评审不得进行 Provider 网络调用。调拨、促销、应付、发票、总账、批次成本及后续 Gate 正式实现继续禁止。
- 仅允许两个虚构租户、多门店、多仓库和三业态合成数据；`SANDBOX`、`REAL_DEVICE`、`PILOT` 继续不能由 Fake 替代。完成后必须提交《T2 Gate 4C / Sprint S7 周门禁报告》等待确认，不得宣称 Alpha、可试点或可商用。

## 4.16 当前 T2 Gate 4D / Sprint S8 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 4C / Sprint S7 周门禁报告》，接受 `T2-CST-001` 更新为 `ACCEPTED`，并授权从 Gate 4C 封板提交 `b84555ee7af02c650a47146426f6c9e4029827b5` 建立 `t2/gate4d-sprint8-20260817`。
- Gate 4D 正式实现只允许 `T2-TRF-001`；必须先冻结调拨数据主权、申请/审批/发出/在途/收货/差异/取消状态、不变量、权限、审计、API/事件、Flyway、容量、回退、固定合成向量和 CI 准入。
- `jshpos-transfer` 独占调拨单、调拨行、发出/收货事实、在途流水、差异处置、调拨审计和调拨 Outbox；不得直接写 `inv_stock_*`、`inv_cost_*`，不得修改历史库存、成本或调拨在途流水。
- 调拨发出和收货是两个独立幂等事实：发出通过库存 Owner 追加来源仓 `TRANSFER_OUT`，收货通过库存 Owner 追加目的仓 `TRANSFER_IN`。禁止使用跨仓单事务覆盖两个余额来伪造原子调拨；重复同摘要返回原结果，同键异摘要失败关闭。
- 在途数量由调拨只追加流水重建。多收必须拒绝；少收、拒收、破损和在途损失必须进入显式差异状态并审批处置。发出前可取消且无库存效果；发出后禁止原地取消，必须走显式反向调拨或差异处置。
- 来源仓 `TRANSFER_OUT` 由成本 Owner 冻结发出单位成本；目的仓 `TRANSFER_IN` 必须通过调拨 Owner 只读端口引用原发出成本，并参与目的仓移动加权。禁止由客户端提交成本，禁止按目的仓当前平均成本伪造调拨入库成本。
- 数量使用 `DECIMAL(19,6)/BigDecimal`；成本使用以最小货币单位计量的 `DECIMAL(25,6)/BigDecimal`，舍入固定 `HALF_EVEN`，禁止浮点数。商业 V1 只准入同租户、仓级、CNY 基础调拨，不含多货主、批次、库位、越库和复杂配送 WMS。
- `T2-PRM-001..003` 只允许领域模型、状态、不变量、OpenAPI/Schema、固定向量和验收用例准备，必须保持 `DRAFT`，不得创建促销表、服务、Controller、计算引擎或 POS 运行时。会员、报表及后续 Gate 继续 `DRAFT`。
- `T2-PAY-002` 继续 `BLOCKED`；未经独立支付沙箱解阻评审不得进行 Provider 网络调用。仅允许两个虚构租户、多门店、多仓库和三业态合成数据；`SANDBOX`、`REAL_DEVICE`、`PILOT` 不得由 Fake 替代。
- 完成后必须提交《T2 Gate 4D / Sprint S8 周门禁报告》等待确认；不得自动启动促销、会员、报表或后续 Gate，不得宣称 Alpha、可试点或可商用。

## 4.17 当前 T2 Gate 5A / Sprint S9 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 4D / Sprint S8 周门禁报告》，接受 `T2-TRF-001` 更新为 `ACCEPTED`，并授权从 Gate 4D 最终治理提交 `63537b5cb7ceeb1fe6b04107b53e2e68941b25ad` 建立 `t2/gate5a-sprint9-20260817`。
- Gate 5A 只允许按 `T2-PRM-001 → T2-PRM-002 → T2-PRM-003` 顺序准入；后一项只有在前一项完成正式实现与内部验证后才能从 `DRAFT` 进入 `READY/IN_PROGRESS`，不得一次铺开后补设计。
- `jshpos-promotion` 是规则、版本、试算、解释、人工优惠授权、成交优惠快照、分摊、退款恢复投影、促销审计和促销 Outbox 的唯一写入 Owner；不得回写价格簿、订单历史、支付事实、库存或成本流水。
- 首版正式规则只允许单品固定价/直减/折扣、第二件或第 N 件、固定组合价、满额/满件减，以及门店、渠道、SKU、分类、品牌、业务日和有效时段作用域。优惠券、积分、储值、会员等级、拼团、秒杀、预算抢占、营销触达、广告归因和动态定价继续禁止运行时。
- 固定计算顺序为基础/门店价、单品促销、组合/门槛、受权手工改价、整单折扣、抹零。候选按 `priority desc, rule_version_id asc` 稳定排序；互斥和叠加必须显式，Java 与 Dart 对同一黄金向量逐字段一致。
- 金额使用最小货币单位整数，数量与比例使用 `BigDecimal/DECIMAL` 或 Dart 精确整数/十进制算法，禁止浮点数参与最终金额。分摊采用最大余数法并按 `order_line_no, sku_id, line_id` 稳定决胜；退款只读取原成交快照和累计已退上限。
- 服务端新表必须按 ADR-027 登记 `MP_ENTITY/XML_ONLY/READ_PROJECTION`，所有新表、字段、持久化模型、Record 和核心代码具备有效中文注释；已发布迁移不得修改，失败只能前向修复。
- POS 离线规则包必须绑定可信租户/门店、Schema、引擎兼容版本、有效期、内容摘要和签名；双槽校验后原子切换，损坏、过期或能力不兼容时失败关闭，不得静默使用旧规则或猜测优惠。
- `T2-PAY-002` 继续 `BLOCKED`，Provider 网络调用为 0；会员、报表及后续 Gate 继续 `DRAFT`，仅允许两个虚构租户、多门店、多终端和三业态合成数据，`SANDBOX/REAL_DEVICE/PILOT` 不得由 Fake 替代。
- 完成后必须提交《T2 Gate 5A / Sprint S9 周门禁报告》等待确认；不得自动进入会员、报表或后续 Gate，不得宣称 Alpha、可试点或可商用。

## 4.18 当前 T2 Gate 5B / Sprint S10 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 5A / Sprint S9 周门禁报告》，接受 `T2-PRM-001..003` 更新为 `ACCEPTED`，并授权在分支 `t2/gate5b-sprint10-20260817` 启动 Gate 5B。
- 只允许按 `T2-POS-006 → T2-ORD-003 → T2-REF-002` 顺序准入和实现；前项未形成独立 `VERIFIED` 证据时，后项必须保持 `DRAFT`，不得通过预建绿色实现或占位测试绕过顺序。
- POS 促销结算必须在同一 SQLite 事务冻结购物篮输入、规则包版本、报价 fingerprint、人工优惠审计引用、成交促销快照、订单/订单行、现金收款、班次现金效果和 Outbox。任一写入失败整体回滚；恢复只可复用原幂等键与原请求摘要，禁止按新规则重算。
- Order Owner 只验证并绑定 Promotion Owner 的不可变快照，不得复制促销算法或写促销表。金额统一满足 `gross - discount + surcharge = receivable`，订单头与逐行合计必须一致。
- 退货退款按原订单与原成交快照累计控制数量和金额，最后一次合法退款吸收余数。Promotion、Order/Refund、Payment、Inventory 和 Audit 各写自己的事实，只能通过受控端口、版本化事件与 Inbox/Outbox 协作；禁止跨模块 Mapper 更新其他 Owner 表。
- 本 Sprint 只允许现金和既有 Provider 无关支付/退款核心的内部合成闭环。`T2-PAY-002` 继续 `BLOCKED`；Provider SDK/HTTP、真实回调端点、账单下载、终端、生产密钥与真实资金调用数必须为 0。
- `T2-MEM-001/002`、`T2-RPT-001/002` 只允许领域模型、隐私/权限边界、API/事件、查询投影、合成向量和验收用例准备，状态必须保持 `DRAFT`，不得出现运行时 Controller、Job、表或写服务。
- 仅允许两个虚构租户、多门店、多终端和三业态合成数据；`SANDBOX/REAL_DEVICE/PILOT` 不得由 Fake 替代。完成后提交《T2 Gate 5B / Sprint S10 周门禁报告》等待确认，不得自行进入后续 Gate 或宣称 Alpha、可试点或可商用。

## 4.19 当前 T2 Gate 5C / Sprint S11 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 5B / Sprint S10 周门禁报告》，接受 Gate 5B `CONDITIONAL PASS`，并授权将 `T2-POS-006`、`T2-ORD-003`、`T2-REF-002` 更新为 `ACCEPTED`。Gate 5C 从 Gate 5B 封板提交 `2b55ad6154b75bce1ff19c68a50e025afe7f1e93` 建立 `t2/gate5c-sprint11-20260817`。
- Gate 5C 只允许按 `T2-MEM-001 → T2-MEM-002` 顺序准入；前项未形成独立 `VERIFIED` 证据时，后项必须保持 `DRAFT`，不得预建账本运行时或绿色占位。
- `jshpos-member` 是会员主体、身份映射、同意记录、隐私请求、等级事实、积分流水和可重建投影的唯一写入 Owner。Order、Promotion、Return 和 Reporting 只能通过受控端口或版本化事件读取必要快照，禁止跨模块 Mapper 写会员表。
- 会员身份必须以 ULID 为永久主键，手机号等凭证不得作主键。精确查找只保存标准化后 HMAC 索引，可逆值使用版本化 AEAD 密文或令牌化引用；密钥不进代码、仓库、日志或制品。
- 身份绑定、解绑、合并/拆分、同意与撤回、隐私请求必须带稳定幂等键、请求摘要、权限、范围和不可篡改审计。删除请求遇法定保留时必须限制用途并记录依据，不得直接删除交易或权益事实。
- POS 本地只能保存有有效期的会员令牌、脱敏展示、等级/权益快照版本和摘要，禁止手机号、证件号、地址或其他不必要明文 PII。撤回、过期、换机和终端解绑必须可失效并清理。
- 积分和等级效果只能追加事实，余额和等级投影必须可从流水重建。成交获取、消费、冻结/解冻、到期、退货冲正和人工调整必须关联原事实、规则版本、原因、操作/审批人和业务日；禁止直接改余额或回写历史。
- 商业 V1 积分抵扣继续不支持 POS 离线消费。成交/退货通过同步后的服务端事件异步入账；重复、乱序、晚到、同键异内容和重放均必须失败关闭或幂等收敛。
- `T2-RPT-001/002` 本 Sprint 只允许契约、数据主权到可丢弃查询投影、重建、脱敏导出和合成验收准备，状态保持 `DRAFT`，禁止表、Controller、Job 或正式运行时。
- `T2-PAY-002` 继续 `BLOCKED`，Provider 网络调用必须为 0。优惠券、储值、充值、预算抢占、营销自动化、外部会员平台、真实短信、报表运行时及后续 Gate 继续禁止。
- 只允许两个虚构租户、多组织、多门店、多终端和三业态合成数据；真实 PII、`SANDBOX/REAL_DEVICE/PILOT` 证据保持为 0。完成后必须提交周门禁报告等待确认，不得宣称 Alpha、可试点或可商用。

## 4.20 当前 T2 Gate 5D / Sprint S12 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 5C / Sprint S11 周门禁报告》，接受 Gate 5C `CONDITIONAL PASS`，并授权将 `T2-MEM-001/002` 更新为 `ACCEPTED`。Gate 5D 从 Gate 5C 封板提交 `12c916c7a4b956a0bdca09ebc3ee6b4e19f9cf63` 建立 `t2/gate5d-sprint12-20260817`。
- Gate 5D 只允许按 `T2-RPT-001 → T2-RPT-002` 顺序准入；前项未形成独立 `VERIFIED` 证据时，后项必须保持 `DRAFT`，不得预建支付报表运行时或绿色占位。
- `jshpos-reporting` 是可丢弃查询投影、消费检查点、差异、导出任务、导出制品元数据和报表审计的唯一写入 Owner。Reporting 只能消费其他 Owner 的版本化事件或明确只读端口，禁止跨模块 Mapper 访问或修改私有表。
- 每条输入必须冻结 source event ID、内容摘要、Schema 版本、投影版本、可信租户、组织/门店、业务日和检查点。重复同摘要幂等，同键异摘要拒绝；乱序、晚到和缺口必须显式记录并可重放补算，投影可整体删除后从权威事实重建。
- 销售、收银、库存与成本口径只能累加 Owner 已冻结的精确增量，不得在报表层重新计算订单金额、促销分摊、库存可用量或移动加权成本。金额使用最小货币单位整数，数量和单位成本使用精确 `DECIMAL/BigDecimal`，禁止浮点数。
- 报表查询必须使用可信 `tenant_id` 和组织/门店数据范围；缓存、任务、临时文件、对象键和下载令牌必须带租户命名空间。导出必须有独立权限、审批阈值、范围/行数上限、字段白名单、脱敏、水印、CSV 公式注入防护、短期单次下载和完整审计。
- `T2-RPT-002` 只允许 Provider 无关支付/退款事实、内部合成账单与固定差异向量。真实 Provider 账单、下载、SDK/HTTP、回调、终端、密钥和资金继续被 `T2-PAY-002` 阻断，内部证据不得解除。
- 只允许两个虚构租户、多组织、多门店、多终端和三业态合成数据；真实 PII、`SANDBOX/REAL_DEVICE/PILOT` 证据继续为 0。BI 自助分析、任意 SQL、跨租户大盘、AI 建议、税务、总账、发票、应付和数据仓库不在本 Sprint。
- 完成后必须提交《T2 Gate 5D / Sprint S12 周门禁报告》等待确认；不得自动进入后续 Gate，不得宣称 Alpha、可试点或可商用。

## 4.21 当前 T2 Gate 6A / Sprint S13 条件准入

- 项目发起人已于 2026-08-17 确认《T2 Gate 5D / Sprint S12 周门禁报告》，接受 `T2-RPT-001/002` 更新为 `ACCEPTED`，并授权从 Gate 5D 封板提交 `1bd27f70d39dd2056ffecf3b25f07aa9c7953606` 建立 `t2/gate6a-sprint13-20260817`。
- Gate 6A 只允许按 `T2-TRM-001 → T2-BAK-001` 顺序准入；终端需求未形成独立 `VERIFIED` 证据前，备份需求必须保持 `DRAFT`，不得预建绿色运行时或倒补准入材料。
- `jshpos-sync` 内的 Terminal Registry 是终端登记、激活、凭据摘要、状态、版本、能力快照和终端审计的唯一写入 Owner。保留 `pos_sync_device` 作为既有正式物理表并通过前向迁移扩展，不得创建第二套可竞争终端事实源。
- 激活授权绑定可信租户、组织/门店、终端档案和有效期；激活码与设备凭据只返回一次且服务端只保存 HMAC/SHA-256 摘要。客户端不得自报 tenant_id、门店、能力或认证状态；吊销、克隆、版本/能力欺骗和时钟偏移失败关闭并审计。
- `jshpos-resilience` 在 BAK-001 获准后才可创建，独占备份清单、恢复演练、校验、RPO/RTO 证据和恢复审计；不得成为订单、支付、库存或报表事实源，不得用备份值回写或覆盖权威业务事实。
- 备份密钥与数据分离，仓库只允许虚构临时密钥；恢复必须从空隔离环境开始并核验清单、摘要、加密、版本、Flyway、租户守恒、投影重建、游标和审计。Alpha 候选内部目标为 RPO≤15分钟、RTO≤60分钟，不等于商业 SLA。
- `T2-UPG-001` 只允许版本契约、签名/摘要、兼容窗口、灰度状态机、营业时段保护、健康检查、回退/前向修复设计和合成故障向量，必须保持 `DRAFT`；禁止正式升级运行时和真实终端命令。
- `T2-UAT-001`、`T2-REL-001` 只允许差距、证据目录、签署和 Go/No-Go 模板，状态保持 `DRAFT`。`T2-PAY-002`、`T2-HWD-001`、`T2-PAR-001` 保持 `BLOCKED`，Fake 不得替代 SANDBOX、REAL_DEVICE 或 PILOT。
- 只允许两个虚构租户、多组织、多门店、多终端和三业态合成数据；Provider 网络、生产密钥、真实资金、真实 PII、未经授权云资源写入、真实终端命令和后续 Gate 正式编码继续禁止。
- 完成后必须分别提交 Gate 6A 周门禁报告与 UPG-001 设计准备/实机阻断报告并等待确认；不得自行启动完整 Alpha、发布或商业验收。

## 4.22 当前 T2 Gate 6B / Sprint S14 条件准入

- 项目发起人已于 2026-08-20 确认 Gate 6A `CONDITIONAL PASS`，接受 `T2-TRM-001/BAK-001` 更新为 `ACCEPTED`；TRM 不等于 `REAL_DEVICE`，BAK 仅为 `SYNTHETIC_RESTORE` 内部目标且不构成生产灾备或商业 SLA。
- Gate 6B 唯一允许正式实现的需求是 `T2-UPG-001`；必须先冻结数据主权、状态机、不变量、权限、审计、API/事件、Flyway、容量、兼容、回退、运行手册、故障注入和测试准入。
- `jshpos-release` 独占发布物、门店范围、灰度批次、终端软件任务、命令结果、发布事件和审计。它只能通过只读端口消费 Terminal Registry、Sync、Shift、Payment、Refund、对象存储和公钥注册表，不得跨模块 Mapper 修改其他 Owner 私表。
- 发布物必须绑定 release ID、类型、版本、通道、可信租户/门店范围、SHA-256、签名/key version、build commit、SBOM 及 app/protocol/Schema/system/capability 兼容窗口；签名通过后身份字段冻结。
- 发布、灰度、终端任务必须使用具名状态机、稳定幂等键、乐观锁和只追加事件/审计。摘要/签名错误、旧包重放、跨租户、下载中断、迁移/健康失败、pending Outbox、UNKNOWN 支付/退款、营业时段、版本能力不兼容和终端吊销必须失败关闭或显式暂停。
- MySQL/SQLite Schema 只前进；Schema 失败使用安全前向修复，禁止修改已发布迁移。应用/Web/APK 只有在兼容边界满足时才能通过新事实表达回退，不得重写原任务历史。
- 本 Sprint 只允许合成签名包、虚构终端和软件执行结果。`T2-HWD-001` 解阻前禁止真实 APK 安装、厂商静默安装 SDK、固件、真实重启或远程命令；合成结果不得升级为 `REAL_DEVICE`。
- `T2-PAY-002`、`T2-HWD-001`、`T2-PAR-001` 保持 `BLOCKED`，`T2-UAT-001/REL-001` 保持 `DRAFT`。三项外部 P0 任一未解除时不得启动完整 Alpha UAT；Provider 网络、生产密钥、真实资金/PII、未经授权云写入继续禁止。
- 完成后必须提交 Gate 6B 周门禁报告、HWD 实机升级解阻准备报告和外部 P0 差距清单等待确认；不得自行更新 UPG 为 ACCEPTED，不得宣称 Alpha、可试点或可商用。

## 4.23 当前 T2 Gate 6C-Prep：外部 P0 解阻准备条件准入

- 项目发起人已于 2026-08-20 确认 Gate 6B `CONDITIONAL PASS`，授权 `T2-UPG-001` 由 `VERIFIED` 更新为 `ACCEPTED`；接受范围仅为 Provider 无关发布治理、合成签名包、虚构终端和软件执行结果，不等于 `REAL_DEVICE`、生产发布链路或商业 SLA。
- Gate 6C-Prep 只允许支付沙箱、真实硬件和设计伙伴三条外部 P0 的资料收集、离线验真方案、证据清单、RACI、截止点、Alpha 差距、Go/No-Go 和回退模板；不得新增或修改正式业务运行时、数据库迁移、Provider/设备适配器或真实 UAT 脚本。
- `T2-PAY-002`、`T2-HWD-001`、`T2-PAR-001` 必须保持 `BLOCKED`，直到各自真实证据齐备、独立解阻评审通过并由项目发起人确认。公开资料、模板、Fake、模拟器和自填声明均不得提升为 `SANDBOX/REAL_DEVICE/PILOT`。
- 支付轨可维护候选池和收件清单，但首接 Provider、授权沙箱、测试终端、官方版本化接口、签名/回调/退款/账单/限流/网络及技术联系人任一必需项缺失时，网络调用必须为 0；禁止创建 SDK/HTTP 客户端、回调入口、账单下载器或凭据配置。
- 硬件轨可维护主机、两种打印、扫码、电子秤、钱箱、客显、SDK/固件/样机和认证矩阵，但实机解阻前禁止真实 APK 安装、静默安装 SDK、固件、重启、钱箱脉冲或远程命令；软件生成记录不得宣称硬件通过。
- 伙伴轨必须取得 5 家目标和至少 3 家可验证书面试点意愿，样本须有授权、目的限制、最小化、脱敏、保留期限和删除证明；不得把内部人员、虚构商户或合成数据计为伙伴证据。
- `T2-UAT-001`、`T2-REL-001` 继续 `DRAFT`。三条外部 P0 任一未解除时，完整 Alpha UAT 和正式 Go/No-Go 必须为 `NO-GO`；只允许准备证据目录、角色签署、环境/RACI 和决策模板。
- 仓库、日志、CI 和制品继续禁止沙箱/生产 Secret、真实资金、真实 PII、未脱敏商户数据和未经授权云资源写入。外部材料只登记脱敏元数据、摘要、来源、版本、保管人和受控位置引用。
- 完成后必须提交三条独立外部解阻报告与《T2 Gate 6C-Prep 启动评审报告》等待确认；不得自行解除阻断、发起外部网络/设备命令、启动完整 Alpha/UAT 或宣称可试点、可商用。

## 4.24 当前 T2 Gate 6C：外部证据首批收件与离线验真条件准入

- 项目发起人已于 2026-08-20 接受 Gate 6C-Prep `CONDITIONAL PASS`，仅授权从封存提交 `74f5100b89a7cf98acd28622e5ccdc4beb7626b7` 进入外部证据收件与离线验真；Provider 网络、真实终端命令、现场试点和完整 Alpha UAT 均未授权。
- 支付轨只允许按拉卡拉、汇付天下、易宝支付、银联商务、通联支付的优先顺序维护统一 RFI、公开官方资料摘要和受控材料缺口。公开网页中的沙箱/生产地址只能作为文档事实，禁止探测或调用；Secret、商户号、终端号和证书值不得进入 Git/CI。
- 硬件轨只允许登记商米 T3 PRO MAX 与 iMin D4 Pro 等候选及打印、扫码、秤、钱箱、客显的公开规格、RFI 和样机缺口；在精确 SKU、固件、SDK 授权、样机及联系人形成受控证据并获批前，APK 安装、固件、重启、打印、钱箱脉冲、秤和远程命令执行数必须为 0。
- 伙伴轨不得虚构目标。真实主体未由用户/商务在受控系统提供前，目标和书面意愿计数必须保持 `0/5` 与 `0/3`；内部人员、匿名线索、公开门店名、合成数据和口头意向均不得计数或触发联系/试点。
- 公开官方资料使用 `PUBLIC_OFFICIAL_DOCUMENT` 子标签，只能证明候选公开信息及待询范围，不能把需求整体提升为 `VERIFIED_DOCUMENT`。只有授权、主体、版本、适用范围、摘要、保管人、到期和删除/吊销均闭环的完整受控包，才可提交独立执行准入评审。
- `T2-PAY-002/HWD-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`。三条报告可以独立给出 `NO-GO` 或补件建议，但未经项目发起人逐轨确认不得解除状态或产生外部执行证据。
- 仓库只保存公开 URL、必要事实摘要、响应字节数/SHA-256 和受控材料不透明引用；公开网页原始响应、受控原文、Secret、真实 PII、样机证书和未脱敏商户数据不得提交。
- 本阶段结束必须提交支付、硬件、伙伴三条独立执行准入报告和更新后的 Alpha P0 差距。缺少完整受控材料时必须如实 `NO-GO/BLOCKED`，不得创建绿色占位、启动 Gate 6C 执行、完整 Alpha/UAT 或宣称可试点、可商用。

## 4.25 当前 T2 Gate 6D / Sprint S15：内部产品化第一波条件准入

- 项目发起人已于 2026-08-20 明确外部支付沙箱、真实硬件和设计伙伴资料暂不阻断内部业务产品化开发，并授权从 Gate 6C 封存提交 `aece518b9ef1057462e835ad7f98ce1aa2bffbf3` 建立 `t2/gate6d-sprint15-internal-productization`。
- Gate 6D 只允许按 `T2-POS-007 → T2-POS-008 → T2-ADM-001 → T2-E2E-001` 顺序准入和实现；前项未形成独立 `VERIFIED` 证据时，后项必须保持 `DRAFT`，不得预建运行时、绿色占位或倒补设计。
- Flutter POS 页面只能调用正式应用服务、Repository 和 `PosDeviceGateway`；禁止 UI 直接访问 SQLite、`MethodChannel`、拼装订单/支付/库存/促销事实或自行计算最终金额。可信终端、门店、员工、权限、班次和业务日上下文必须由应用层校验并失败关闭。
- POS 必须覆盖大触控区、键盘/扫码输入、防重复点击、断网与同步积压、权限拒绝、错误恢复、业务日切换和安全退出。打印仅允许形成打印任务、预览及 Fake Device Adapter 调用；不得将软件预览提升为 `REAL_DEVICE`。
- Vue 管理后台只能通过已批准正式 API 使用 Foundation、Catalog、RBAC 和 Audit Owner 能力；路由权限仅控制展示，服务端权限仍为最终授权。前端禁止跨模块直查、复制状态机或重新计算金额、库存、成本和促销。
- `T2-E2E-001` 只允许两个虚构租户、多门店、多终端、三业态和现金支付的内部合成闭环；E2E 必须复用既有 Owner 端口与正式 SQLite/MySQL Schema，不新增库存、成本、促销、会员或报表算法。
- `T2-PAY-002/HWD-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`。Provider 网络、真实终端命令、现场试点和完整 Alpha UAT 执行数必须为 0。
- 本 Sprint 只允许现金和既有 Provider 无关 Fake；禁止 Provider SDK/HTTP、真实回调/账单、真实资金、生产密钥、真实 PII、真实外设与 APK 安装。内部合成证据不得宣称 Alpha、可试点或可商用。
- 完成后必须提交《T2 Gate 6D / Sprint S15 周门禁报告》等待项目发起人确认；不得自动把四项需求更新为 `ACCEPTED`，不得自行进入 Gate 6E。

## 4.26 当前 T2 Gate 6E / Sprint S16：后台运营、退货退款与内部 Alpha 候选条件准入

- 项目发起人已于 2026-08-20 接受 Gate 6D `CONDITIONAL PASS`，授权 `T2-POS-007/008`、`T2-ADM-001`、`T2-E2E-001` 更新为 `ACCEPTED`，并从封板提交 `281e98a6b286f1343a012ed289cecb195858dcc7` 建立 `t2/gate6e-sprint16-internal-alpha-candidate`。
- Gate 6E 只允许按 `T2-ADM-002 → T2-POS-009 → T2-E2E-002` 顺序准入；前项未形成独立 `VERIFIED` 证据时后项必须保持 `DRAFT`，不得预建运行时、绿色占位或倒补设计。
- ADM-002 只允许通过各 Owner 正式 API 产品化库存/盘点、采购、调拨、成本、促销、会员、报表、终端与发布后台；禁止跨模块 Mapper、任意 SQL、前端状态机复制和权威金额/数量/成本/促销重算。
- 后台写操作必须显示服务端状态/版本、二次确认、稳定幂等键、权限/数据范围和审计；库存与成本页面不得覆盖余额或历史流水，会员页面不得使用真实 PII。
- POS-009 只有在 ADM-002 独立 VERIFIED 后才可准入；必须通过 Return/Refund、Promotion、Payment、Inventory 与 Audit 正式编排端口读取原订单/促销快照和累计已退上限，禁止 UI 直连 SQLite、MethodChannel、Mapper 或拼装领域事实。
- E2E-002 只有在 POS-009 独立 VERIFIED 后才可实现，最高证据为 `INTERNAL_ALPHA_CANDIDATE`；不得更新或替代 `T2-UAT-001`，P0/P1 未关闭时保持 `IN_PROGRESS/NO-GO`。
- `T2-PAY-002/HWD-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`。Provider 网络、真实设备命令、现场试点和完整 Alpha UAT 必须为 0。
- 本 Sprint 禁止新增既有 Owner 领域算法、真实支付/回调/账单、真实外设/APK 安装、生产密钥、真实资金/PII、现场试点、生产部署和商业可用声明。
- 完成后必须提交 Gate 6E 周门禁暨内部 Alpha 候选收口报告等待确认；不得自行启动外部执行、完整 Alpha UAT 或发布准备。

## 4.27 当前 T2 Gate 6F：外部 P0 恢复对接与完整 Alpha UAT-Prep 条件准入

- 项目发起人已于 2026-08-21 接受 Gate 6E `CONDITIONAL PASS`，授权 `T2-ADM-002/POS-009/E2E-002` 更新为 `ACCEPTED`，并从 Gate 6E 最终封板提交 `0dd20e90c32914da48b3154f0bf1781ab8f2ba71` 建立 `t2/gate6f-external-p0-uat-prep-20260821`。`T2-E2E-002` 仍只代表 `INTERNAL_ALPHA_CANDIDATE`。
- 本阶段只允许支付、硬件和伙伴三轨的候选冻结、统一 RFI、受控材料元数据收件、离线验真、独立执行准入报告，以及完整 Alpha UAT/发布准备。不得新增或修改正式业务运行时、迁移、Provider/设备适配器或外部调用代码。
- 支付首接候选可以在文档层选定，但 `T2-PAY-002` 必须保持 `BLOCKED`；完整授权沙箱、测试商户/终端、适用产品/API 版本、签名/回调/退款/账单/限流/白名单和具名技术联系人达到 `VERIFIED_DOCUMENT` 后，仍须项目发起人独立确认才可网络执行。
- 主认证与兼容机型可以在候选层冻结，但 `T2-HWD-001` 必须保持 `BLOCKED`；精确 SKU/固件、SDK 授权、样机、外设 BOM、静默安装/升级权限和联系人达到 `VERIFIED_DOCUMENT` 后，仍须独立确认才可安装、重启或下发命令。
- 伙伴轨不得虚构、抓取或擅自联系门店。`T2-PAR-001` 保持 `BLOCKED`；真实目标必须为 5 家，至少 3 家书面意愿及样本授权、保留/删除、旧系统对账和现场责任边界完成离线验真后，仍须独立确认才可开展现场执行。
- 仓库、日志、CI 和制品只保存非敏感元数据及受控材料不透明引用。Secret、真实 PII、生产密钥、未脱敏商户数据、设备证书、真实商户号/终端号和个人联系方式不得进入普通仓库或制品。
- `T2-UAT-001/REL-001` 继续 `DRAFT`。三条外部 P0 未全部独立解阻前，完整 Alpha UAT、现场试点、生产发布和商业可用声明固定为 `NO-GO`；本阶段只能冻结矩阵、RACI、缺陷等级、证据等级、签署、数据清除、灰度/回退与 Go/No-Go 模板。
- `T2-LIC-001` 继续 `DEFERRED`，但必须形成 Aviator、simple-http、MySQL Connector/J 的替换或法务关闭计划，商业发布前未关闭即 `NO-GO`。`T2-JSH-001` 继续 `DEFERRED`，不得猜测或提前接入。
- 完成后必须提交支付、硬件、伙伴三份独立执行准入报告、《T2完整Alpha UAT启动评审报告》和《T2发布准备差距报告》，等待项目发起人逐项确认；不得自动解除任何阻断。

## 4.28 当前 T2-PAY-002：拉卡拉受控材料离线验真条件准入

- 项目发起人已于 2026-08-21 接受 Gate 6F-Prep `CONDITIONAL PASS`，仅授权从 Gate 6F 封板提交 `7ccc50112d954c1d4c9bdd3464da4b3a2eb9ec3e` 对拉卡拉首接文档候选开展受控材料收件与离线验真。
- 用户指令中的受控材料 ID 仍是未填写占位符，不得把方括号说明、Gate 6C 公开网页、内部 Fake、环境变量名或自动生成 ID 当成真实受控材料。实际材料 ID 未提供时，所有必需项必须保持 `MISSING`。
- 本阶段只允许登记不透明材料 ID 和非敏感元数据，核验来源、授权签署、适用产品、接口版本、范围、SHA-256、保管人、到期、轮换、删除和吊销规则；受控原文不得复制到 Git、日志、CI 或普通 Artifact。
- `VERIFIED_DOCUMENT` 是整包结论：授权沙箱、测试商户/终端、正式接口、签名验签、回调、退款、账单、限流/白名单、Secret 保管和具名联系人必须全部通过，任一缺失、冲突、过期或无法独立复核都必须 `NO-GO`。
- `T2-PAY-002` 继续 `BLOCKED`。即使达到 `VERIFIED_DOCUMENT`，也只能提交更新后的独立执行准入报告；未经项目发起人再次确认，不得创建 Provider SDK/HTTP 适配器、探测域名、调用沙箱、接收真实回调或下载账单。
- Provider 网络、真实资金、生产密钥、真实商户/终端标识和未脱敏支付数据必须为 0；发现疑似生产凭据时停止验真、隔离并要求吊销，不得验证可用性。
- `T2-HWD-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-LIC-001/JSH-001` 继续 `DEFERRED`；不得启动完整 Alpha、真实设备、伙伴联系、现场试点或生产发布。
- 实际材料 ID 未齐时必须提交缺件报告和补件指令，不得伪造更新后的绿色《T2-PAY-002 支付沙箱执行准入评审报告》。

## 4.29 当前 T2 Gate 6G / Sprint S17：商业 V1 内部核心代码收口条件准入

- 项目发起人已于 2026-08-21 授权从支付缺件封板提交 `a6c91a1d66857583ce7e498541b63bcf0b81dc52` 建立 `t2/gate6g-sprint17-core-productization`，外部资料暂不阻断内部核心代码与业务代码收口。
- Gate 6G 只允许按 `T2-CORE-001 → T2-API-001 → T2-DAT-001 → T2-INT-001 → T2-E2E-003` 串行准入；前项未形成独立 `VERIFIED` 证据时，后项必须保持 `DRAFT`，不得预建绿色实现或倒补设计。
- `T2-CORE-001` 先建立 Requirement→页面→API→Application→Domain→Repository/Mapper→Schema→测试覆盖矩阵，审计并修复生产路径中的 Fake/Mock/Stub/InMemory/Locked/TODO/空实现/硬编码合成数据；外部边界只允许显式失败关闭，不得伪造成功。
- `T2-API-001` 只收口已接受能力的正式 REST/OpenAPI/事件/错误码/幂等/关联/分页/排序/版本兼容；不得新增 Provider SDK/HTTP、真实回调或跨 Owner Mapper，不得由客户端 tenant_id 决定授权。
- `T2-DAT-001` 只允许前向迁移、初始化、合成种子、投影重建、安全清理与环境一致性；已发布 MySQL/SQLite 迁移不得修改，所有数据继续使用两个虚构租户、三业态、多门店、多终端。
- `T2-INT-001` 必须通过正式应用端口、版本化事件、Inbox/Outbox 或明确只读端口组合已接受 Owner；每个 Owner 只写自己的事实，UNKNOWN 只能查询/观察收敛，金额、数量、分摊、库存和成本逐字段守恒。
- `T2-E2E-003` 必须同时运行正式服务端、Web、Flutter、MySQL 与 SQLite 路径，不得使用数据库后门或测试专用绿色占位；证据上限为 `INTERNAL_V1_CORE_CANDIDATE`，不更新或替代完整 Alpha UAT。
- `T2-PAY-002/HWD-001/PAR-001/PRN-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 与全部 `V1-*` 汇总验收项继续 `DRAFT`，`T2-LIC-001/JSH-001` 继续 `DEFERRED`。
- Provider 网络、真实资金、真实设备命令、伙伴联系、现场试点、完整 Alpha 与生产部署必须为 0；不得用 Fake、内部 E2E 或软件执行解除外部阻断。
- 完成后必须提交核心完整性审计、API/装配差距、Gate 6G 周门禁、RTM/缺陷/证据和 Gate 6H 指令，等待项目发起人确认；不得自动启动 Gate 6H、完整 Alpha 或生产发布。

## 4.30 当前 T2 Gate 6H / Sprint S18：体验、性能、运维与内部发布候选条件准入

- 项目发起人已于 2026-08-21 接受 Gate 6G `CONDITIONAL PASS`，授权 `T2-CORE-001/API-001/DAT-001/INT-001/E2E-003` 更新为 `ACCEPTED`，并从 Gate 6G 治理封板提交 `c2db49fb47db5fe30fe01515b60ee6f054b214e3` 建立独立 Gate 6H 分支。
- Gate 6H 只允许按 `T2-UX-001 → T2-PERF-001 → T2-OPS-001 → T2-RC-001` 顺序准入；前项未形成独立 `VERIFIED` 证据时，后项必须保持 `DRAFT`，不得预建绿色发布候选或倒补设计。
- `T2-UX-001` 只收口便利店主路径以及零食折扣店、社区超市的版本化展示/导航/录入体验差异；不得按业态复制订单、价格、促销、库存、成本或退款算法。Flutter UI 继续只调用应用服务，Vue 继续只调用正式 Owner API。
- `T2-PERF-001` 只建立内部合成容量、并发、冷启动、扫码录入、结算、同步积压和报表性能基线。阈值必须区分正确性硬门禁与趋势告警；GitHub 托管执行器结果不得宣称真实 Android、生产容量或商业 SLA。
- `T2-OPS-001` 只收口内部部署配置、日志/指标/关联标识、诊断包、备份恢复、升级回退和运维手册；诊断与制品必须脱敏并按租户命名空间，禁止生产密钥、真实 PII、未经授权云写入和真实终端命令。
- `T2-RC-001` 只构建不包含外部 P0 的内部发布候选，绑定 commit、版本、SHA-256、合成签名、SBOM、许可证、迁移清单、变更说明和 Go/No-Go。证据上限为 `INTERNAL_RELEASE_CANDIDATE`，不得替代 `T2-UAT-001/REL-001`。
- `T2-PAY-002/HWD-001/PAR-001/PRN-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 与全部 `V1-*` 汇总验收项继续 `DRAFT`，`T2-LIC-001/JSH-001` 继续 `DEFERRED`。
- Provider 网络、真实资金、真实设备命令、伙伴联系、现场试点、完整 Alpha 与生产部署必须为 0；不得以体验测试、内部压测、合成运维演练或内部 RC 解除外部阻断。
- 完成后必须提交 Gate 6H 周门禁暨内部发布候选报告、RTM/缺陷/性能/证据索引和下一步操作指令，等待项目发起人确认；不得自动启动完整 Alpha、现场试点或生产发布。

## 4.31 当前 T2 Gate 6I-Prep：外部 P0 执行准入与完整 Alpha UAT 冻结

- 项目发起人已于 2026-08-21 接受 Gate 6H `CONDITIONAL PASS`，授权 `T2-UX-001/PERF-001/OPS-001/RC-001` 更新为 `ACCEPTED`；`T2-RC-001` 仍只代表 `INTERNAL_RELEASE_CANDIDATE`，不等于 `SANDBOX/REAL_DEVICE/PILOT/FULL_ALPHA/PRODUCTION` 或商业 SLA。
- Gate 6I-Prep 仅允许从 Gate 6H 封板提交 `e2bd699ad565845c363ffe478320eb2b3f9a0015` 建立独立准备分支，完成支付、硬件打印、设计伙伴、商业许可证四条独立执行准入材料和完整 Alpha UAT 冻结；不得修改正式业务运行时、数据库迁移、Provider/设备适配器或生产部署配置。
- 支付轨 `T2-PAY-002` 继续 `BLOCKED`。拉卡拉受控材料实际 ID 仍为 `0/11` 时，整包必须保持 `NOT_ACHIEVED/NO-GO`；公开资料、占位符、Fake 或环境变量名不得替代授权沙箱、测试商户/终端、版本化接口、签名回调、退款账单、限流白名单、Secret 系统引用和具名联系人。
- 硬件打印轨 `T2-HWD-001/PRN-001` 继续 `BLOCKED`。主认证与兼容候选、两种打印、扫码、电子秤、钱箱、客显、样机、SDK/固件和安装升级授权必须形成完整受控包；在独立执行准入获批前，APK 安装、打印、固件、重启、钱箱脉冲、秤、客显和远程命令执行数必须为 0。
- 伙伴轨 `T2-PAR-001` 继续 `BLOCKED`。真实目标不足 `5/5`、可验证书面意愿不足 `3/3`，或脱敏样本授权、保留删除、旧系统对账和现场责任边界任一未闭环时，必须 `NO-GO`；不得抓取、虚构或擅自联系门店。
- 许可证轨 `T2-LIC-001` 继续 `DEFERRED` 且是商业发布强制阻断项。Aviator、simple-http、MySQL Connector/J 必须分别形成替换并回归通过，或取得适用分发模式的法务书面批准；Owner、截止条件和证据缺失时不得标记关闭。
- `T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001` 继续 `DEFERRED`。支付、硬件打印和伙伴轨未经独立执行准入确认前，完整 Alpha UAT 必须 `NO-GO`；许可证未关闭、完整 Alpha 未 `ACCEPTED` 前，生产发布和商业声明必须 `NO-GO`。
- 仓库、日志、CI 和普通制品只允许不透明材料引用、SHA-256 与非敏感状态；Secret、真实 PII、生产密钥、真实商户/终端标识、设备证书、伙伴身份联系方式和受控原文不得进入仓库。误收时立即隔离、通知、轮换或删除，不得测试其有效性。
- 本阶段 Provider 网络、真实资金、真实设备命令、伙伴联系、现场试点、完整 Alpha、生产部署和商业声明必须全部为 0。完成后提交四条独立执行准入报告与《T2完整Alpha UAT启动评审报告》等待项目发起人逐项确认，不得自动进入外部执行或完整 Alpha。

## 4.32 当前 T2 Gate 7A / Sprint S19：商业 V1 内部业务差距审计条件准入

- 项目发起人已于 2026-08-21 授权从 Gate 6I-Prep 封存提交 `d66c252587561428def95058d67bc830e391a9ab` 建立 `t2/gate7a-sprint19-v1-business-gap`；本阶段只允许需求审计、原子 RTM、契约、验收与串行开发计划，不得新增正式业务运行时、迁移或外部适配器。
- 必须以 Requirement→旅程/页面→API→Application→Domain→Repository/Mapper→MySQL/SQLite→事件/Outbox→权限/审计→测试→CI 的链路复核全部既有 `ACCEPTED` 能力。证据完整的能力只能归入 V1 汇总或在原 Owner 内受控扩展，不得重复创建模块、状态机、表或 Requirement。
- 确认缺失的候选按 Gate 7B POS 交易运营、Gate 7C 商品门店供应与条件社区超市能力、Gate 7D 日结异常开通与经营管理、Gate 7E 内部汇总验收拆分；所有新增 Requirement 在本阶段必须保持 `DRAFT`。
- Gate 7B 第一批只建议 `T2-POS-010 → T2-POS-011 → T2-ORD-004`；项目发起人未确认 Gate 7A 报告前不得进入 READY/IN_PROGRESS。`T2-EXG-001/MEM-003/SAA-001/SUB-001/SVC-001` 还必须取得独立 CR 才可 READY。
- 储值预付、复杂优惠券/预算、发票应付总账税务、批次成本、复杂 WMS、加盟结算、供应链金融、连接器、AI、任意 SQL 与跨租户经营大盘不得准入。条件批次效期不含批次成本、库位、波次或复杂 WMS。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`。允许外部端口失败关闭，但不得模拟成功或用内部证据解除 `SANDBOX/REAL_DEVICE/PILOT` 阻断。
- Provider 网络、真实资金、真实设备/外设命令、伙伴联系、现场试点、完整 Alpha、生产部署和商业声明必须全部为 0。完成 Gate 7A 报告与 CI 后必须等待项目发起人确认，不得自动启动 Gate 7B。

## 4.33 T2 Gate 7B / Sprint S20：POS 交易运营第一批已封存

- 项目发起人已于 2026-08-21 接受 Gate 7A，并授权从封板提交 `fd255f45115015fa0bab91f1fd8b1c14c2acc51e` 建立 `t2/gate7b-sprint20-pos-operations`，严格按 `T2-POS-010 → T2-POS-011 → T2-ORD-004` 串行开发。
- `T2-POS-010` 已由候选 Run `32487652575` 独立验证并由 Run `32488566595` 闭环；`T2-POS-011` 已由候选 Run `32494347095` 独立验证并由封存提交 `94a315ee2aa0304e7a02dfb20182eef0e0281e7a` 的 Run `32495308502` 闭环；`T2-ORD-004` 已由 Run `32499856217` 独立验证。项目发起人已于 2026-08-22 将三项全部更新为 `ACCEPTED`，证据上限仍为内部软件执行。
- ORD-004 仅允许把 `DRAFT/PENDING_PAYMENT` 且无成功或未知资金、无库存出库的交易迁移为 `CANCELLED`；`COMPLETED/CONFIRMED` 及已有资金、库存、成本、促销和审计事实不得回退、删除或覆盖，只能追加 `RETURN_REFUND_REQUIRED/PAYMENT_REVERSAL_OBSERVATION_REQUIRED/EXPLICIT_COMPENSATION_REQUIRED` 处置路由。
- 购物篮取消、挂单取消、未完成本地单作废、服务端取消墓碑和成交后处置路由必须使用稳定幂等键、内容摘要、可信租户/门店/终端/员工/班次/业务日、只追加处置事实、审计和 Outbox；同键异内容、跨租户、业务日漂移、支付 `PROCESSING/UNKNOWN/SUCCEEDED`、库存已出或状态竞态必须失败关闭。
- POS-010 复用 Checkout/Order Owner，在独立只追加表记录非销售现金移动和非销售钱箱请求；现金移动、班次理论现金/版本、审计、幂等结果和 Outbox 必须同事务，钱箱请求金额恒为零且不得伪造设备成功。
- 本地权限来自已认证员工会话，服务端仍执行权限和门店范围校验；`tenant_id`、门店、终端、员工、班次和业务日只来自可信上下文。现金移动输入必须为正数最小货币单位整数，Owner 冻结其正负方向。
- SQLite/Flyway 只允许独立前向迁移，已发布迁移不得修改；现金和审计事实不得更新或删除。真实钱箱未解阻时设备执行状态固定为 `BLOCKED_EXTERNAL`，真实外设命令必须为 0。
- `T2-EXG-001/PAY-004` 与 Gate 7C—7E 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`。
- Provider 网络、真实资金、真实设备/外设命令、伙伴联系、现场试点、完整 Alpha、生产部署和商业声明继续为 0。本节记录第一批历史边界；当前准入以 4.34 为准。

## 4.34 当前 T2 Gate 7B / Sprint S20-B：第二批准入准备

- 项目发起人已于 2026-08-22 接受第一批 `CONDITIONAL PASS`，授权 `T2-POS-010/POS-011/ORD-004` 更新为 `ACCEPTED`，并从封存提交 `a9e368a2f09dcfb4565f4bc392e4f77d5c664805` 建立 `t2/gate7b-sprint20b-pos-operations`。
- 本阶段只允许 `T2-EXG-001` 独立 CR、仓库事实与范围影响分析，以及 `T2-EXG-001/PAY-004` 的 Owner、状态机、不变量、API/事件、迁移、页面、故障向量和验收设计；禁止新增或修改任何正式运行时、Mapper、数据库迁移、Flutter/Vue 页面、Provider 或设备适配器。
- `T2-EXG-001` 必须保持“原单退货退款 + 新销售 + 只追加关联”的编排语义；Return、Order、Payment、Inventory、Promotion 各写自己的事实，禁止建立独立换货资金/库存状态机或跨 Owner Mapper。
- `T2-PAY-004` 必须使用 Provider 无关 tender plan 和精确份额守恒；订单有任何支付 `PROCESSING/UNKNOWN` 时禁止重建或替换业务命令。电子份额继续受 `T2-PAY-002` 阻断，生产路径不得模拟成功。
- 两项必须保持 `DRAFT`，ADR-044 保持 `Proposed`。完成准备报告后等待项目发起人确认；未经确认不得转为 READY/IN_PROGRESS，不得编码，不得启动 Gate 7C。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；Provider 网络、真实资金、真实设备/外设命令、伙伴联系、现场试点、完整 Alpha、生产部署和商业声明全部为 0。

## 4.35 当前 T2 Gate 7B / Sprint S20-B：T2-EXG-001 独立正式开发

> 本节保留 T2-EXG-001 实施时的历史边界；当前执行状态以随后 4.36 节为准。

- 项目发起人已于 2026-08-22 接受 CR-T2G7B-009 和第二批准备阶段 `CONDITIONAL PASS`，授权从封存提交 `4d184a1cf5998c91d9cc9db359a4d5ed0d16e0c3` 建立 `t2/gate7b-sprint20b-exg-runtime`，仅准入 `T2-EXG-001`。
- ADR-044 已为 `Accepted`。`T2-EXG-001` 已完成独立实现与候选全量 CI，当前为 `VERIFIED` 并等待项目发起人确认；`T2-PAY-004` 必须继续 `DRAFT`，未经项目发起人再次确认不得编码。
- 换货只能表达为“原单退货退款 + 新销售 + ReturnOrchestration Owner 只追加关联”。原退货退款复用既有 Return/Refund/Promotion/Payment/Inventory Owner，新销售复用既有 Checkout/Order Owner；换货不得复制或覆盖任一 Owner 的状态机、金额、促销、库存或成本事实。
- 换货 Saga 只持有两条腿的稳定命令 ID、Owner 聚合引用、摘要、检查点和审计。跨 Owner 使用正式应用端口与观察，不使用分布式事务或跨 Owner Mapper；成功资金、库存或订单事实不得补偿删除。
- `RETURN_UNKNOWN/SALE_UNKNOWN` 只能沿原 returnId/orderId/commandId 查询或观察；禁止生成替代退款、扣款或销售命令。部分成功且无法自动收敛必须进入 `MANUAL_RECOVERY_REQUIRED` 并保留完整审计。
- MySQL 与 SQLite 只允许独立前向迁移；换货事件与腿关联只追加，Saga 头只允许具名条件状态迁移。Flutter 页面只能调用 Exchange、Return 和 Sale 正式应用端口，禁止直接访问 SQLite、Mapper 或拼装 Owner 事实。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；Provider 网络、真实资金、真实设备/外设命令、伙伴联系、现场试点、完整 Alpha、生产部署和商业声明全部为 0。
- `T2-EXG-001` 独立周门禁报告和证据索引必须随封存提交复跑完整 CI 并等待确认；不得自动准入 `T2-PAY-004` 或 Gate 7C。

## 4.36 当前 T2 Gate 7B / Sprint S20-B：T2-PAY-004 独立正式开发

- 项目发起人已于 2026-08-22 接受 `T2-EXG-001 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；仅授权从封板提交 `2cc9f88b8ad2d0530a77a9341d396dd1a2fc1e5f` 建立 `t2/gate7b-sprint20b-pay004-runtime`，独立实现 `T2-PAY-004`。
- `T2-PAY-004` 采用 Payment Owner 独占的版本化 TenderPlan 与 2—8 个精确份额；份额合计必须逐分等于订单应收，同币种 CNY，现金最多一份且必须位于最后，电子份额严格串行。
- `SUCCEEDED` 不可回退；`PROCESSING/UNKNOWN` 继续占额并阻断后续份额、取消和替代命令。超时、ACK 丢失、重启或结果未知只能复用原 plan/allocation/command 身份查询或观察，禁止生成替代扣款。
- 现金份额只能调用 Order/Shift Owner 受控端口追加部分现金事实、班次流水、审计与 Outbox；Payment Mapper 禁止写 `ord_*` 或 `shf_*`。Order Owner 只消费冻结计划和全部份额权威成功结果，不复制支付状态机。
- `T2-PAY-002` 继续 `BLOCKED`。正式装配不提供电子成功适配器；电子 collect 必须失败关闭为 `PAYMENT_EXTERNAL_BLOCKED`，不得出现 Provider SDK/HTTP、回调、账单、凭据或模拟成功。合成成功观察只允许位于测试范围。
- 原单退款必须按原成功份额、原介质和累计成功/占额上限恢复；现金退现金，电子份额未解阻时继续受控阻断，禁止转成现金或其他介质规避。
- MySQL 与 SQLite 只允许独立前向迁移；TenderPlan/Allocation 使用具名条件状态迁移，历史、观察、现金份额、审计与事件只追加。Flutter 页面只能调用 Tender 应用端口，不得直接访问 SQLite、MethodChannel 或拼装资金事实。
- `T2-HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 与 Gate 7C—7E 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；Provider 网络、真实资金、设备/外设命令、伙伴执行、完整 Alpha、生产部署和商业声明全部为 0。
- 候选提交 `dce3acb3f15d53120c1211c5cf663ce957a4edd3` 的 GitHub Actions Run `32544144916` 已完成九类门禁全绿；`T2-PAY-004` 当前为 `VERIFIED` 并等待项目发起人确认，确认前不得更新为 `ACCEPTED`，不得自动进入 Gate 7C。

## 4.37 当前 T2 Gate 7C / Sprint S21-A：T2-PRD-005 独立正式开发

- 项目发起人已于 2026-08-22 接受 `T2-PAY-004 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；仅授权从封板提交 `b3caf80072996a06f669ecff8213ecaefb27e3b6` 建立 `t2/gate7c-sprint21a-prd005-weighted-barcode`，独立实现 `T2-PRD-005`。
- Catalog Owner 独占版本化 EAN-13 秤码/金额码模板、发布状态、摘要、事件和审计；POS 通过签名 Catalog 数据包解析，Order Owner 只冻结解析快照，禁止复制模板发布或价格算法。
- 原始码按二进制字符串保存并保留前导零；字段位置使用 1 基下标。重量、数量、单位换算和单价使用 `BigDecimal/DECIMAL`，金额使用最小货币单位整数，舍入固定为 `HALF_EVEN`，禁止浮点数。
- 重量码冻结模板版本、SKU、主单位、精确数量、成交单价和金额；金额码以编码金额为权威并按单位精度反算数量，同时冻结舍入与人工复核标志。校验失败、模板歧义、过期、越界、溢出、摘要不一致和旧包不兼容必须失败关闭。
- 门店模板优先于租户模板；同一作用域、前缀、长度和有效窗口的歧义在发布前拒绝。已发布模板内容不可变，停用和纠正只能使用状态迁移或新版本，MySQL/SQLite 已发布迁移不得修改。
- `T2-HWD-001` 继续 `BLOCKED`；仅允许键盘楔入或合成扫码输入，不得访问电子秤/扫码厂商 SDK、串口、蓝牙或真实设备，不得用软件证据替代 `REAL_DEVICE`。
- `T2-LBL-001/RPL-001/DMT-001/ONB-001/LOT-001` 及 Gate 7D—7E 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`。
- 完成后必须提交 `T2-PRD-005` 独立周门禁报告并等待项目发起人确认；不得自动准入 `T2-LBL-001`、`T2-RPL-001` 或 Gate 7D。
- `T2-PRD-005` 候选提交 `131a76184d9be0f6a4785f8fbb055bcff77ee5e7` 已在 GitHub Actions Run `32551158599` 完成十类制品与完整门禁，现仅为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`；`T2-LBL-001`、`T2-RPL-001` 和后续需求继续 `DRAFT`，真实秤、厂商扫码 SDK 与实机性能仍为 `BLOCKED`。

## 4.38 当前 T2 Gate 7C / Sprint S21-B：T2-LBL-001 独立正式开发

- 项目发起人已于 2026-08-22 接受 `T2-PRD-005 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；该接受只覆盖 EAN-13 软件解析、精确计量快照、键盘楔入和合成扫码，真实电子秤、厂商扫码 SDK 与 `REAL_DEVICE` 仍为 `BLOCKED`。
- 仅授权从封存提交 `a290ca7833abccf4d04872fdb93378cf8cd27646` 建立 `t2/gate7c-sprint21b-lbl001-shelf-label`，独立实现 `T2-LBL-001`；`T2-RPL-001` 与 Gate 7C 后续需求继续 `DRAFT`。
- ShelfLabel Owner 只消费 Pricing/Catalog Owner 的已发布价格、商品和单位只读快照，独占价签模板、换签任务、任务项、异常、只追加事件和审计；不得修改价格、商品或历史发布版本，不得在 Vue 重新计算价格。
- 已发布价签模板不可覆盖修改，渲染只允许批准字段和纯文本输出；脚本、路径、公式、未知占位符、控制字符和超长内容必须失败关闭。预览必须展示商品、条码、单位、原价/现价、门店、价格版本、生效时间、任务状态和异常原因。
- 首次发布、未来生效、门店覆盖、批量调价和价格停用/回退必须形成稳定来源键、内容摘要和可重放任务；重复同摘要返回原结果，同键异摘要拒绝，迟到/乱序按生效时间、门店覆盖优先级和价格版本确定性收敛。
- 本 Sprint 允许软件预览、受权人工换签确认、异常处理和失败关闭打印端口；内部预览、Fake 或不可用端口不得标记为打印成功。`T2-PRN-001` 继续 `BLOCKED`，禁止打印机 SDK、USB、串口、蓝牙、真实打印或其他外设命令。
- 新增表必须先登记访问策略：模板为 `CRUD_ENTITY + HYBRID`，任务为 `CONTROLLED_WRITE + HYBRID`，任务项/异常/事件为 `APPEND_ONLY` 或具名状态迁移，复杂列表与预览 SQL 进入 XML；所有 SQL 显式携带可信 `tenant_id` 和门店范围。
- 仅允许两个虚构租户、多门店、三业态和合成价格/任务数据；Provider 网络、真实资金、真实设备/外设、完整 Alpha、现场试点和生产执行保持 0。
- 完成后必须提交 `T2-LBL-001` 独立周门禁报告并等待项目发起人确认；不得自动准入 `T2-RPL-001` 或 Gate 7D，不得宣称真实打印、Alpha、生产或商业验收通过。
- `T2-LBL-001` 候选提交 `69bd772556925f0cf09dc198e3eec969b6247977` 已在 GitHub Actions Run `32571686495` 完成九类制品与完整门禁，现仅为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`；`T2-RPL-001` 及后续需求继续 `DRAFT`，`T2-PRN-001` 和真实打印继续 `BLOCKED`。

## 4.39 当前 T2 Gate 7C / Sprint S21-C：T2-RPL-001 独立正式开发

- 项目发起人已于 2026-08-22 接受 `T2-LBL-001 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；该接受只覆盖内部价签软件流程，真实打印、厂商 SDK 与 `REAL_DEVICE` 继续 `BLOCKED`。
- 仅授权从封存提交 `f0d2f7b989d7dbaf535569e37c4a3c84d6fd61b9` 建立 `t2/gate7c-sprint21c-rpl001-replenishment`，独立实现 `T2-RPL-001`；`T2-DMT-001/ONB-001/LOT-001` 与 Gate 7D 后续继续 `DRAFT`。
- Replenishment Owner 独占 `rpl_*` 规则版本、生成运行、建议、状态事件、审计和 Outbox；Inventory Owner 只通过可信进程内端口提供可重建余额与最后流水序号，Procurement Owner 只通过正式端口创建采购 `DRAFT`，禁止跨 Owner Mapper 或直接更新 `inv_*`/`pur_*`。
- 商业 V1 只准入最低/最高库存、最小订货量、订货倍数、冻结单位换算和可选已批准在途量的确定性计算；禁止预测式 AI、黑盒评分、自动下单、自动审批或自动形成库存/成本效果。
- 建议必须冻结规则身份、库存流水序号、余额版本、各数量输入、在途量、单位换算、计算明细和内容摘要；重复同摘要返回原结果，同键异摘要拒绝，较新库存事实必须使旧开放建议显式 `STALE`。
- 转采购草稿只允许通过 Procurement Owner 端口，绑定原建议和稳定幂等键；供应商失效、单位换算变化、库存事实后移、跨租户/仓库或同键异内容必须失败关闭，审批前不得产生采购承诺、收货、库存或成本效果。
- 数量统一 `DECIMAL(19,6)/BigDecimal` 并使用 `CEILING` 向上满足采购倍数；禁止 `float/double`。所有 `tenant_id` 由可信上下文注入，覆盖 Mapper/XML、任务、缓存、导出和对象存储攻击。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，外部执行、完整 Alpha、现场试点和生产部署保持 0。完成后必须提交独立周门禁报告等待确认，不得自动启动后续需求。
- `T2-RPL-001` 候选提交 `8e5f05d211490166162373119964b6254a771460` 已在 GitHub Actions Run `32575585359` 完成九个 Job 与完整门禁，现仅为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`；百万级结果仅为合成趋势，`T2-DMT-001/ONB-001/LOT-001` 继续 `DRAFT`，所有外部证据状态与零执行边界不变。

## 4.40 当前 T2 Gate 7C / Sprint S21-D：T2-DMT-001 独立正式开发

- 项目发起人已于 2026-08-22 接受 `T2-RPL-001 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；该接受仅覆盖确定性补货内部软件执行，百万级结果仍只代表合成趋势。
- 仅授权从 RPL 封存提交 `fdd2667e48ce4197907416b823ff6cb917a66d6b` 建立 `t2/gate7c-sprint21d-dmt001-business-migration`，独立实现 `T2-DMT-001`；`T2-ONB-001/LOT-001` 与 Gate 7D 后续需求继续 `DRAFT`。
- Migration Owner 独占 `mig_*` 文件元数据、隔离 staging、映射、预检、批次、检查点、对账、审批、审计和清理；不得直接读写 `cat_*/sup_*/pur_*/inv_*/mem_*` 私有表。各 Owner 只通过正式应用端口写自己的事实并返回稳定摘要。
- 原文件只允许在请求内完成大小、类型、摘要、压缩与公式攻击检查，禁止进入 Git、日志、CI、普通制品或长期 staging。暂存业务行必须加密，密钥由仓库外 Secret 系统提供；缺少密钥时正式装配必须失败关闭。
- tenant_id 只能来自可信服务端上下文，文件内容、文件名、Header、查询参数和批次载荷中的租户值不得成为授权依据。文件必须绑定资料类型、Schema/映射版本、SHA-256、字符集、行数、来源系统、受控保管引用与上传人。
- 预检出现任一阻断错误不得调用 Owner 或形成部分可见事实。跨 Owner 导入使用稳定行身份、内容摘要、逐 Owner 检查点和可恢复 Saga，不承诺跨模块数据库大事务；同键异内容拒绝，部分成功必须进入 `COMPENSATION_REQUIRED` 或受控恢复，禁止删除已被引用事实。
- 商品/条码/单位/分类品牌经 Catalog 正式端口进入未激活版本；供应商经 Procurement 正式端口；期初库存经 Inventory 追加 `OPENING_IN`，激活后修正只能追加新库存事实；会员经 Member 正式端口并遵守身份密文、令牌摘要、最小展示与删除规则。
- 双人审批、Owner 对账、摘要与审计完整是激活前置条件。任一 P0/P1 差异必须 `NO-GO`；激活不得自动开店、发布价格、创建采购承诺或产生资金效果。临时加密 staging 可按保留策略物理清理，但批次、摘要、检查点、对账、审批和审计必须保留。
- 仅允许两个虚构租户、多门店、多仓库、三业态合成数据和经明确授权的脱敏样本；100k/1m 仅形成合成趋势。`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`，外部执行、完整 Alpha、现场试点和生产部署保持 0。
- `T2-DMT-001` 修复候选 `276d3489d77317badfa4a99e797a17f3fb87ad64` 已在 GitHub Actions Run `32585460410` 完成九个 Job 与完整门禁，现仅为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`；真实商户迁移和生产容量证据仍为 0，`T2-ONB-001/LOT-001` 继续 `DRAFT`。
- 完成后必须提交 `T2-DMT-001` 独立周门禁报告等待确认；不得自动准入 `T2-ONB-001/LOT-001` 或 Gate 7D，不得宣称生产容量、真实数据迁移、Alpha、生产或商业验收通过。

## 4.41 当前 T2 Gate 7C / Sprint S21-E：T2-ONB-001 独立正式开发

- 项目发起人已于 2026-08-23 接受 `T2-DMT-001 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；接受范围仅为内部软件执行、虚构数据或明确授权脱敏样本，100k 是内部上限且 1m 只代表合成趋势。
- 仅授权从 DMT 封存提交 `8b7d830a7b9b786978c4707e7890fcecec06db8f` 建立 `t2/gate7c-sprint21e-onb001-store-onboarding`，独立实现 `T2-ONB-001`；`T2-LOT-001` 与 Gate 7D 后续需求继续 `DRAFT`。
- 新增独立 `jshpos-onboarding` 作为 Onboarding Owner，独占开店计划、冻结配置快照、审批、步骤检查点、检查结果、里程碑、审计和 Outbox；不得拥有或复制其他 Owner 的业务事实。
- 可复制范围只允许营业时间/业务日、UI 布局选择、设备能力期望、行业模板引用、商品/价格范围引用、权限/小票/审批模板引用；禁止复制 Secret、终端凭据、会员身份、交易、库存余额、成本、积分、审计、Inbox/Outbox 或历史流水。
- 三业态差异只能通过 `CONVENIENCE/SNACK_DISCOUNT/COMMUNITY_SUPERMARKET` 的已发布版本化模板表达；核心状态机、金额、库存、成本、促销、支付和退款算法不得按业态分叉。
- 开店状态必须覆盖 `DRAFT/PREFLIGHTING/PREFLIGHT_FAILED/READY/APPROVED/APPLYING/APPLIED/CHECKING/CHECK_FAILED/READY_TO_OPEN/OPENED/FAILED/COMPENSATION_REQUIRED/CANCELLED`；同键异内容失败关闭，部分 Owner 成功使用原计划/原命令恢复或受审计补偿，禁止删除已产生事实。
- 支付、硬件和打印未解阻时检查结果只能为 `BLOCKED/UNAVAILABLE`；内部检查通过可以形成 `READY_TO_OPEN` 软件里程碑，但不得把外部阻断写为 `PASS`，完整 `OPENED` 的商业证据仍须真实外部解阻与后续 UAT。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；Provider 网络、真实资金、真实设备/外设、未经授权真实 PII、伙伴现场、完整 Alpha、生产和商业声明保持 0。
- 完成后必须提交 `T2-ONB-001` 独立周门禁报告等待确认；不得自动准入 `T2-LOT-001` 或 Gate 7D。

## 4.42 当前 T2 Gate 7C / Sprint S21-F：T2-LOT-001 独立正式开发（VERIFIED 待确认）

- 项目发起人已于 2026-08-23 接受 `T2-ONB-001 CONDITIONAL PASS` 并授权其更新为 `ACCEPTED`；接受范围仍仅为内部软件执行，不代表生产开店、支付沙箱、真实设备/打印、完整 Alpha、现场试点、生产或商业验收。
- 仅授权从 ONB 封存提交 `39f553500bcb67f685c211b178ce36797f1e2aa3` 建立 `t2/gate7c-sprint21f-lot001-lot-expiry`，独立实现 `T2-LOT-001`；Gate 7D 后续需求继续 `DRAFT`。
- LOT 能力只允许由 `COMMUNITY_SUPERMARKET` 已发布行业模板显式启用；`CONVENIENCE` 与 `SNACK_DISCOUNT` 必须默认关闭并在服务端、数据包和 POS 三层失败关闭，核心状态机不得按业态复制。
- Catalog Owner 独占商品批次/效期策略与版本；Inventory Owner 独占批次身份、只追加批次数量流水、可重建批次余额、FEFO 分配、临期投影、审计和 Outbox。采购、订单、退货、盘点和调拨只能通过具名正式端口提交来源事实，禁止跨 Owner Mapper。
- 商业 V1 只允许采购收货建批、销售 FEFO、原单退货优先归原批、采购退货、盘点/调拨保留批次来源、过期禁售和版本化临期预警；禁止批次成本、库位、波次、越库、加工、序列号、冷链、质检、召回、自动报损和复杂 WMS。
- 数量使用 `DECIMAL(19,6)/BigDecimal`，日期按可信门店业务时区与冻结策略计算；客户端不得自行声明 tenant_id、业态、过期状态、可用数量或成本。批次成本不得引入，Costing 继续使用已接受的仓级移动加权成本。
- POS 批次包必须绑定可信租户、门店、版本、摘要和适用模板；SQLite 只前进迁移必须把成交批次快照、订单事实和 Outbox 放在同一事务，未知/过期/余额不足/旧包/损坏包/跨租户包必须失败关闭。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；Provider 网络、真实资金、真实设备/外设、未经授权真实 PII、伙伴现场、完整 Alpha、生产和商业声明保持 0。
- 实现候选提交 `48b7bbd804d867e366eeef625bcc924d0e64f5a5` 的 GitHub Actions Run `32608223073` 已通过治理双平台、Server、Web、Flutter 双平台、MySQL、SQLite、Android/Kotlin、安全、SBOM、许可证和证据索引门禁；`T2-LOT-001` 仅更新为 `VERIFIED`，项目发起人确认前不得更新为 `ACCEPTED`。
- 必须提交 `T2-LOT-001` 独立周门禁报告等待确认；`T2-CLS-001/T2-EXC-001` 继续 `DRAFT`，不得自动进入 Gate 7D。

## 4.43 当前 T2 Gate 7D / Sprint S22-A：T2-CLS-001 门店业务日日结（VERIFIED 待确认）

- 项目发起人已于 2026-08-23 接受 `T2-LOT-001 CONDITIONAL PASS`，只允许把它更新为 `ACCEPTED`；该结论仅覆盖社区超市条件模板下的内部软件执行，不等于真实设备、完整 Alpha、生产或商业验收。
- 仅授权从 LOT 最终封存提交 `75377eb74d0339bfc513f72d0547b8649f8e5f37` 建立 `t2/gate7d-sprint22a-cls001-daily-close`，独立实现 `T2-CLS-001`；`T2-EXC-001` 与 Gate 7D 后续需求继续 `DRAFT`。
- `jshpos-operations` 是日结头、来源检查点、冻结快照、预检差异、审批/签署、状态历史、审计和 Outbox 的唯一写入 Owner；只能通过 Owner 显式只读端口或版本化事件读取权威事实，禁止跨 Owner Mapper、任意 SQL 或以报表覆盖业务事实。
- 日结身份必须绑定可信租户、门店、业务日、IANA 时区、业务日起点、关闭版本、来源检查点和 SHA-256；客户端提交的 tenant、金额、差异或状态不得成为权威事实。
- 状态必须覆盖 `DRAFT/PREFLIGHTING/PREFLIGHT_FAILED/READY/APPROVED/CLOSING/CLOSED/FAILED/CORRECTION_REQUIRED/COMPENSATION_REQUIRED`；创建人与审批/签署人分离，关闭事实与签名只追加，晚到事实只能形成显式差异和新更正版本。
- 预检必须失败关闭未关班、未确认现金差异、`UNKNOWN` 支付/退款、不可解释同步缺口/死信、投影缺口、摘要漂移、权限或审计缺失；外部 Provider 未解阻只能显示 `BLOCKED/UNAVAILABLE`，不得伪造渠道对账通过。
- `T2-PAY-002`、`T2-HWD-001`、`T2-PRN-001`、`T2-PAR-001` 继续 `BLOCKED`；`T2-JSH-001`、`T2-LIC-001` 继续 `DEFERRED`。Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产部署和商业声明继续为 0。
- 实现候选提交 `1d6598bcfe0d1d9c929237e70c9caf5644395ae2` 的 GitHub Actions Run `32611593930` 已通过治理双平台、Server、Web、MySQL 8.4、Flutter 双平台、SQLite、Android/Kotlin、安全、SBOM、许可证和证据索引门禁；封存提交继续执行百万级合成事实的实际 MySQL 精确聚合测试，结果只代表内部合成趋势。
- `T2-CLS-001` 仅更新为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`；项目发起人确认前不得更新为 `ACCEPTED`，`T2-EXC-001` 继续 `DRAFT`。
- 完成后必须提交《T2-CLS-001 独立周门禁报告》等待确认；项目发起人确认前只能更新为 `VERIFIED`，不得自动准入 `T2-EXC-001` 或 Gate 7E。

## 4.44 当前 T2 Gate 7D / Sprint S22-A 第二批：T2-EXC-001 统一异常中心

- 项目发起人已于 2026-08-23 接受 `T2-CLS-001 CONDITIONAL PASS`，只允许把它更新为 `ACCEPTED`；证据仍限虚构租户、虚构门店与合成权威事实，不等于外部或商业验收。
- 仅授权从 CLS 最终封存提交 `b240e0b697e40a16d4f85cb2447195284f15e21e` 建立 `t2/gate7d-sprint22a-exc001-exception-center`，独立实现 `T2-EXC-001`；`T2-MEM-003` 与 Gate 7D 后续需求继续 `DRAFT`。
- `jshpos-operations` 独占异常案件、来源观察、认领租约、转派、处置计划、修复命令引用、复核、状态历史、审计和 Outbox；来源 Owner 继续独占其事实，异常中心不得跨 Owner Mapper、直接改写余额/流水/资金/投影/日结或复制领域状态机。
- 来源只能经版本化事件或显式 Owner 端口形成；必须绑定可信租户与门店范围、稳定来源身份、序号、内容摘要、去重键和关联标识。同来源同摘要幂等，同来源异摘要追加观察并显式冲突，禁止覆盖历史。
- 状态至少覆盖 `OPEN/CLAIMED/IN_PROGRESS/WAITING_OWNER/RESOLVED/CLOSED/REOPENED/FAILED`；认领使用有期限租约和乐观锁，关闭必须具备 Owner 可验证结果和独立复核，重新出现只能重开或关联新案件。
- 修复只能调用对应 Owner 的具名端口并复用稳定命令；支付/退款 `UNKNOWN` 只允许观察查询、回调或对账收敛，禁止生成新扣款、退款或其他业务命令。未配置的 Owner 修复能力必须 `UNAVAILABLE` 失败关闭。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha、生产和商业声明保持 0。
- 完成后只允许把 `T2-EXC-001` 更新为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE` 并提交独立周门禁报告；不得自动准入 `T2-MEM-003`、其他 Gate 7D 需求或 Gate 7E。
- 实现候选提交 `1166ac98cb4041a5a02e2e35fd6f63dd46dc40f1` 的 GitHub Actions Run `32615492242` 已通过九个 Job 并生成九个带 GitHub SHA-256 的证据制品；`T2-EXC-001` 只更新为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`，项目发起人确认前不得更新为 `ACCEPTED`。
- 首次候选 Run `32615384874` 因 Linux 执行器不接受递归 glob 表达式而失败；修复提交 `1166ac98cb4041a5a02e2e35fd6f63dd46dc40f1` 改用跨平台 `rglob`，不得删除失败 Run、降低门禁或用自动重跑掩盖。
- 项目发起人已于 2026-08-23 接受 `T2-EXC-001 CONDITIONAL PASS` 并授权更新为 `ACCEPTED`；接受范围仍只有虚构租户、虚构门店与合成 Owner 事实的内部软件执行，不代表支付沙箱、真实设备/外设、完整 Alpha、生产、商业验收或 SLA。

## 4.45 当前 T2 Gate 7D / Sprint S22-A 第三批准备：T2-MEM-003 独立 CR

- 仅授权从 EXC 最终封存提交 `39a72c65a08899f305ee0c04a5e337e1ee9ffbc9` 建立 `t2/gate7d-sprint22a-mem003-prep`；本阶段只允许 CR、影响分析、领域设计、DRAFT 契约、固定合成向量、验收与 CI 准入准备。
- `T2-MEM-003` 必须保持 `DRAFT / CR_REQUIRED`。未经项目发起人再次确认，不得创建或修改会员权益/会员价运行时代码、数据库表或迁移、Controller、Flutter/Vue 业务页面、后台任务，也不得更新为 `READY/IN_PROGRESS`。
- CR 建议将会员等级权益和会员价定义为三业态均可配置、默认关闭的版本化模板能力；不得在核心状态机中按业态分叉。Member 独占权益定义与最小权益快照，Pricing/Promotion 只经正式端口计算，Order 冻结成交引用，Refund 只读原成交快照。
- 默认计算采用确定性 `BEST_PRICE`：普通基础/门店价与促销路径、会员价与明确可叠加权益路径分别形成可解释报价，再选更低应收；任意叠加必须由会员权益版本和促销版本双方显式允许。人工优惠与抹零继续沿用已接受的后置顺序，金额守恒和 Java/Dart 固定向量不得漂移。
- POS 离线只允许最小化、签名、限定租户/门店/有效期的权益包。过期、撤回、旧包、损坏包、跨租户包或摘要漂移必须让会员权益失败关闭；经明确确认可按非会员重新报价，但不得静默降级或复用旧报价。已冻结交易和退款继续读取原快照。
- 商业 V1 仅支持同租户且显式门店范围；权益成本默认归销售门店经营口径，不准入加盟结算、权益分摊清算、储值/预付、复杂券、预算抢占、真实 PII、短信触达或新积分算法。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-JSH-001/LIC-001` 继续 `DEFERRED`；`T2-SAA-001/SUB-001/SVC-001` 与 Gate 7E 继续 `DRAFT`。Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产和商业声明保持 0。
- 完成后必须提交《T2-MEM-003 独立 CR 与正式开发启动评审报告》等待确认；准备证据不得作为运行时 VERIFIED、外部证据或商业验收。
- 准备候选提交 `0ce1dae88bbab57ff75e320b33bc290a62d1b23f` 的 GitHub Actions Run `32617725005` 已通过 Ubuntu/Windows 治理、范围零越界和证据索引四个 Job，生成四个 SHA-256 制品；该结果只证明准备材料可复现，`T2-MEM-003` 仍为 `DRAFT / CR_REQUIRED`。

## 4.46 当前 T2 Gate 7D / Sprint S22-B：T2-MEM-003 独立正式开发

- 项目发起人已于 2026-08-23 接受 CR-T2G7D-005，授权从准备封存提交 `fd474767c182cbdb5c3df0a9e2e4688371f0587f` 建立 `t2/gate7d-sprint22b-mem003-runtime`；ADR-053 更新为 `Accepted`。
- 只允许实现 `T2-MEM-003`，并严格按 Member 权益 → Pricing 会员价 → Promotion BEST_PRICE → Order/Refund 原快照 → DataPackage/SQLite/Flutter/Vue → 跨端 E2E 顺序推进；前项未独立验证，后项不得铺开。
- 三业态必须复用同一能力和算法且默认关闭；启用必须绑定可信租户、显式门店范围、版本和审批。Member 独占权益版本与最小快照，Pricing 独占会员价候选，Promotion 独占权益调整与解释，Order/Refund 只冻结和读取原快照。
- 默认 `BEST_PRICE`，平价稳定选择普通价格/促销路径；会员权益和普通促销必须双方版本显式允许才可叠加。金额使用最小货币单位整数，数量/换算使用精确十进制，禁止浮点数和前端重算。
- 离线包必须签名并绑定可信租户、门店、版本、摘要、有效期与撤回纪元；过期、撤回、旧包、坏包、跨租户/门店或摘要漂移必须失败关闭。显式非会员重报必须产生新 fingerprint，禁止静默降级。
- 退款必须读取原订单、原权益、原会员价、原促销、原人工优惠与抹零快照；当前等级、价格或规则不得参与历史退款计算。真实 PII 不得进入 Pricing/Promotion/Order/包/日志/CI/普通制品。
- MySQL/SQLite 只允许 `V74` 之后的新前向迁移，已发布迁移不得修改；核心类、实体、状态机、应用服务和复杂 SQL 必须保留有效中文注释。
- `T2-SAA-001/SUB-001/SVC-001` 与 Gate 7E 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-JSH-001/LIC-001` 继续 `DEFERRED`。外部网络、真实资金、设备、伙伴、完整 Alpha、生产和商业声明保持 0。
- 六段串行实现均已完成独立 `VERIFIED_LOCAL`；候选提交 `e95def3cec932fcf307f38398b2f94e1246a899a` 的 GitHub Actions Run `32624915228` 九个 Job 全绿，生成九个 GitHub SHA-256 制品。干净 MySQL 8.4 已从 V1 前进至 V80；既有 Gate 4C/Gate 7C 菜单 ID 冲突只通过新增组合根幂等回调前向修复，未修改已发布迁移。
- `T2-MEM-003` 当前仅可更新为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE`；项目发起人确认前不得写为 `ACCEPTED`，不得自动进入 Gate 7E 或后续需求。失败 Run `32623623290/32624233813/32624504379` 必须原样保留。

## 4.47 当前 T2 Gate 7E / Sprint S23-A：商业 V1 内部汇总验收

- 项目发起人已于 2026-08-23 接受 `T2-MEM-003 CONDITIONAL PASS` 并授权更新为 `ACCEPTED`；证据仍只覆盖三业态默认关闭的会员权益、会员价和原快照退款等内部合成软件执行。
- 仅授权从 MEM003 封存提交 `feeecec5e1b438ba46f4225954e950d4e45ceb0c` 建立 `t2/gate7e-sprint23a-internal-v1-business-complete`，只准入 `T2-E2E-004`。
- T2-E2E-004 只拥有验收清单、固定向量、失败 seed、缺陷账和证据引用，不拥有或写入任何 Owner 事实；禁止新增业务表、迁移、Controller、领域算法、跨 Owner Mapper、直接数据库写入或测试后门。
- 同一 CI Run 必须实际执行 Server、Web、Flutter Linux/Windows、Android/Kotlin、MySQL、SQLite、Java/Dart 向量、迁移恢复、租户权限、安全、SBOM、许可证和内部汇总证据；P0/P1 必须为 0，缺失或跳过任一必需证据即失败关闭。
- 两个虚构租户、多组织、六门店/终端覆盖便利店、零食折扣店和社区超市；非社区超市批次能力必须关闭。金额、份额、优惠、库存、成本、会员权益和日结必须逐字段守恒。
- 证据上限固定为 `INTERNAL_V1_BUSINESS_COMPLETE_CANDIDATE`；`T2-UAT-001/T2-REL-001` 继续 `DRAFT`，不得更新或替代。
- `T2-SAA-001/SUB-001/SVC-001` 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-JSH-001/LIC-001` 继续 `DEFERRED`。Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产和商业声明保持 0。
- 完成后只能把 `T2-E2E-004` 更新为 `VERIFIED / AWAITING SPONSOR ACCEPTANCE` 并提交 Gate 7E 周门禁报告；不得自动启动外部 P0、完整 Alpha、试点或生产发布。

## 4.48 当前 T2 Gate 7F-Prep：外部 P0、许可证与完整 Alpha 执行准入准备

- 项目发起人已于 2026-08-23 接受 Gate 7E `CONDITIONAL PASS`，授权从封存提交 `3aaa92e9c90d1db540cfb6b70cdf65058c6a118f` 建立 `t2/gate7f-prep-external-p0-alpha-admission`。
- `T2-E2E-004` 可更新为 `ACCEPTED`，但证据上限仍为 `INTERNAL_V1_BUSINESS_COMPLETE_CANDIDATE`，不得更新或替代 `T2-UAT-001`。
- Gate 7F-Prep 只允许支付、真实 Android 主机、打印与外设、设计伙伴四条独立收件清单、受控材料离线元数据验真、商业许可证关闭计划和完整 Alpha UAT 启动冻结；禁止修改正式运行时、数据库迁移和依赖。
- 受控资料必须核验来源、授权签署、适用产品/型号与版本、范围、SHA-256、保管人、到期、轮换、删除和吊销；原文、Secret、商户号、终端号、证书、真实 PII 与未脱敏商户数据不得进入 Git、普通日志、CI 或普通制品。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 必须保持 `BLOCKED`。单轨完整材料达到 `VERIFIED_DOCUMENT` 后也只能提交独立执行准入评审，未经项目发起人逐轨确认不得产生 Provider 网络、真实资金、设备/外设命令或伙伴联系。
- `T2-LIC-001` 保持 `DEFERRED` 且是商业发布强制阻断项；Aviator、simple-http、MySQL Connector/J 必须逐项以替换回归或适用分发模式书面法务批准关闭。
- `T2-UAT-001/REL-001` 保持 `DRAFT`；四条外部 P0 未逐轨确认前，完整 Alpha、现场试点和生产发布必须 `NO-GO`。`T2-SAA-001/SUB-001/SVC-001` 继续 `DRAFT`，`T2-JSH-001` 继续 `DEFERRED`。
- 本阶段所有外部执行计数必须为 0。完成后只提交四条独立执行准入报告、许可证关闭计划、完整 Alpha UAT 启动评审和 Gate 7F-Prep 评审报告，等待项目发起人逐项确认。

## 4.49 当前 T2 Gate 8A-Prep：商业 SaaS 运营能力独立 CR 与正式开发准备

- 项目发起人已于 2026-08-23 授权从 Gate 7F-Prep 最终封存提交 `b47533eba707d486abe44dbf70ec7b651081b3af` 建立 `t2/gate8a-prep-commercial-operations`；只允许 CR、影响分析、领域设计、契约和验收准备。
- `T2-SAA-001`、`T2-SUB-001`、`T2-SVC-001` 必须保持 `DRAFT`，严格按 `SAA → SUB → SVC` 串行。项目发起人未再次确认前，禁止创建正式运行时模块、数据库迁移、Controller、Vue/Flutter 业务页面或后台任务。
- SaaS Owner 计划独占商户申请、套餐权益和商业租户生命周期编排；Foundation 继续独占技术租户、可信租户上下文、组织门店和 IAM/RBAC。SaaS 禁止直接写 RuoYi 系统表，tenant_id 只能由服务端 Foundation 分配。
- Subscription Owner 计划独占订阅期限、只追加状态事实和调度检查点，不得处理真实扣费或资金；只有 SAA 独立 `ACCEPTED` 且再次获批后才可启动 SUB 运行时。
- Service Owner 计划独占实施项目、服务目录、工单、检查项和附件引用；工单不得授予设备/外设命令、伙伴现场或生产变更权限，内部时间目标不得形成合同 SLA。只有 SAA、SUB 均独立 `ACCEPTED` 且再次获批后才可启动 SVC 运行时。
- 套餐权益和订阅授权必须由服务端执行，前端菜单隐藏不得代替授权。暂停、到期、停用或逻辑注销不得物理删除订单、资金、库存、成本、审计等历史，并须保留退款、查询、对账、备份恢复、法定导出、迁移和删除请求的受控能力。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、生产部署和商业声明保持 0。
- 完成后只提交三项独立 CR/依赖影响、领域边界、契约草案、测试矩阵、Gate 8A-Prep 评审报告和 SAA 下一步指令，等待项目发起人确认；不得自动启动任何运行时。

## 4.50 当前 T2 Gate 8A / Sprint S24-A 条件准入

- 项目发起人已接受 Gate 8A-Prep `CONDITIONAL PASS`，并授权从最终封存提交 `bcfcaa4621ea55c61bd1cd22fc355b5f74d8dae4` 建立 `t2/gate8a-sprint24a-saa001-runtime`。
- 本阶段唯一允许正式实现的需求是 `T2-SAA-001`；`T2-SUB-001`、`T2-SVC-001` 继续 `DRAFT`，不得创建其表、Controller、页面或任务。
- `jshpos-saas` 独占商户申请、版本化套餐权益、商业租户生命周期、配额、审计和 Outbox；Foundation/RuoYi 继续独占技术租户、可信上下文和 IAM/RBAC。
- SaaS 禁止直接写 RuoYi System 或其他 Owner 私有表；技术租户只能通过 Foundation `TenantProvisioningPort` 创建和停启，`tenant_id` 必须由服务端分配。
- 租户创建前的开户操作必须使用平台管理员权限；租户管理员、客户端 tenant_id、菜单可见性或本地时间均不得成为授权依据。
- 暂停、停用和逻辑注销不得物理删除历史，必须保留退款、查询、对账、审计、备份恢复、法定导出、迁移和删除请求的受控能力。
- 一次性开户密码、Secret、真实 PII、商户号、终端号和证书不得写入 SaaS 表、Git、日志、CI 或普通制品。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。
- 完成后必须提交 `T2-SAA-001` 独立周门禁报告并等待项目发起人确认；不得自动进入 SUB、SVC、完整 Alpha、现场试点或生产发布。

## 4.51 当前 T2 Gate 8A / Sprint S24-B 条件准入

- 项目发起人已于 2026-08-23 接受 `T2-SAA-001 CONDITIONAL PASS`，同意更新为 `ACCEPTED`，并授权从 SAA 最终封存提交 `abcf34ab2e99a75ba476fade6f203b38a2ed3c75` 建立 `t2/gate8a-sprint24b-sub001-runtime`。
- 本阶段唯一允许正式实现的需求是 `T2-SUB-001`；`T2-SVC-001` 继续 `DRAFT`，不得创建其运行时、迁移、Controller、页面或任务。
- `jshpos-subscription` 独占订阅头、版本化期限、只追加状态历史、调度检查点、通知意图、审计和 Outbox；它只能通过 SaaS 正式端口校验套餐快照和切换订阅访问模式，不得直接写 SaaS、RuoYi、Foundation 或其他 Owner 私有表。
- 订阅状态固定为 `DRAFT/PENDING_ACTIVATION/ACTIVE/GRACE_PERIOD/SUSPENDED/EXPIRED/TERMINATION_PENDING/TERMINATED/RESTORED`；续期、宽限、到期、暂停、恢复和终止必须使用稳定幂等键、内容摘要、乐观锁和只追加状态事实。
- 受控降级由服务端强制执行。`RECOVERY_ONLY` 与 `TERMINATED_RECOVERY` 只保留退款、支付退款查询、对账、审计、备份恢复、法定导出、数据迁移和删除请求；菜单隐藏或客户端时钟不得成为授权依据。
- 调度使用权威 UTC 时间、IANA 时区、持久化租约与检查点；重复任务、乱序、时钟偏移、服务重启和延迟扫描必须复用原业务身份并最终收敛，不得删除或回写历史期限。
- 本阶段禁止真实计费、自动扣款、支付 Provider、发票、应付、总账、税务、真实短信/邮件和资金沉淀；外部 `BLOCKED`、UAT/REL `DRAFT`、LIC/JSH `DEFERRED` 状态不变。
- 完成后必须提交《T2-SUB-001 独立周门禁报告》等待项目发起人确认；不得自动进入 SVC、完整 Alpha、现场试点或生产发布。

## 4.52 当前 T2 Gate 8A / Sprint S24-C 条件准入

- 项目发起人已于 2026-08-24 接受 `T2-SUB-001 CONDITIONAL PASS`，同意更新为 `ACCEPTED`，并授权从 SUB 最终封存提交 `ae0c145b82306dd08f9a0999ba3f030d54e8e3e9` 建立 `t2/gate8a-sprint24c-svc001-runtime`。
- 本阶段唯一允许正式实现的需求是 `T2-SVC-001`；完整 Alpha、现场试点、生产发布和其他新业务需求继续禁止。
- `jshpos-service` 独占版本化服务目录、实施项目与检查项、服务工单、认领租约、责任流转、处理记录、附件元数据、内部时间目标、审计和 Outbox；它只能读取 SaaS/Subscription 正式快照，不得修改租户、套餐、订阅、支付、设备或其他 Owner 私有事实。
- 实施项目状态固定为 `DRAFT/PREFLIGHTING/PREFLIGHT_FAILED/READY/IN_PROGRESS/BLOCKED/READY_TO_HANDOVER/HANDED_OVER/CANCELLED`；工单状态固定为 `OPEN/ASSIGNED/IN_PROGRESS/WAITING_INPUT/RESOLVED/CLOSED/REOPENED/CANCELLED`。必选检查未完成不得移交，关闭必须有解决摘要和独立复核，重开只追加历史。
- 附件正文只能进入服务端生成租户命名空间的受控对象存储键；数据库、日志、Git 和普通制品只能保存不透明引用、SHA-256、媒体类型、大小、保留/清理状态。上传、下载、清理每次都必须重新校验可信租户、门店范围和独立权限，短期链接不得超过 5 分钟。
- 认领和转派必须使用持久化租约、乐观锁、稳定幂等键和只追加责任历史；同键异内容拒绝，租约过期、并发认领、重启和重复任务必须收敛或失败关闭。
- 内部响应目标只用于运营告警，不形成合同 SLA、赔付或自动升级承诺；真实短信/邮件、呼叫中心、在线聊天、远程控制、AI 自动派单和第三方工单连接器不在本阶段。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。外部执行计数保持 0。
- 完成后必须提交《T2-SVC-001 独立周门禁报告》等待项目发起人确认；不得自动启动完整 Alpha、现场试点或生产发布。

## 4.53 当前 T2 Gate 8B-Prep 条件准入

- 项目发起人已于 2026-08-24 接受 `T2-SVC-001 CONDITIONAL PASS` 并授权更新为 `ACCEPTED`；接受范围只覆盖虚构商户、虚构租户、合成服务资料和内部软件执行，不代表合同 SLA、伙伴现场、完整 Alpha、生产或商业验收。
- Gate 8B-Prep 只能从 T2-SVC-001 最终封存提交 `16912142cadd7f3038a10e9deee02eb97ff44d3f` 建立 `t2/gate8b-prep-commercial-saas-operations-acceptance`；本阶段不得新增正式业务运行时、数据库迁移、后台业务页面、Flutter 业务页面或后台任务。
- 允许汇总 `T2-SAA-001/T2-SUB-001/T2-SVC-001` 的 RTM、Owner、API/事件、迁移和证据，并以测试范围代码通过三个正式 REST Controller 验证内部合成旅程；测试不得直接写数据库、调用 Mapper、绕过权限契约或伪造外部成功。
- Gate 8B-Prep E2E 的证据上限为 `INTERNAL_SYNTHETIC_API_JOURNEY`。Owner 状态机、持久化、租户隔离与失败恢复仍由三项已接受需求的原始测试和 MySQL 证据证明；聚合旅程不得替代完整 Alpha UAT。
- 套餐权益、订阅访问模式与 Service 授权必须服务端失败关闭，tenant_id 只来自可信上下文；稳定幂等、同键异内容拒绝、只追加历史和受控恢复边界不得因聚合验收放宽。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产和商业声明必须为 0。
- 完成后只提交《T2 Gate 8B-Prep 启动评审报告》与下一步操作指令并等待确认；不得自动进入运行时开发、完整 Alpha、现场试点或生产发布。

## 4.54 当前 T2 Gate 8B / Sprint S25 条件准入

- 项目发起人已于 2026-08-24 接受 Gate 8B-Prep `CONDITIONAL PASS`，并授权从最终封存提交 `72ff758f1b61e5638664c758cf5ca479b512ddf5` 建立 `t2/gate8b-sprint25-commercial-saas-operations-acceptance`。
- 本阶段唯一新增汇总需求为 `T2-E2E-005`；只允许修复正式 API 旅程发现的装配、授权、幂等、历史保留和可观测性 P0/P1 缺陷，不得以汇总验收名义新增业务状态机、Owner、表、页面或后台任务。
- `T2-SAA-001 → T2-SUB-001 → T2-SVC-001` 必须在正式 MySQL 8.4、Redis 7.4 和完整服务端运行时中经公开 HTTP API 执行；除空库基础 Schema 初始化外，旅程脚本不得连接数据库、访问 Mapper、写 Redis、调用测试后门或伪造 Owner 结果。
- 平台职责分离允许在默认平台租户中使用具名 `platform_admin` 角色；该角色不得在商户租户生效，也不得替代 Controller 权限、可信租户和 Owner 应用层授权。
- 证据上限为 `INTERNAL_COMMERCIAL_OPERATIONS_CANDIDATE`；P0/P1 必须为 0，失败 seed 必须固定保留，性能仅形成内部合成边界，不得形成生产容量或商业 SLA。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。
- 候选提交 `379197394a4c0934dac8a6d4ff1e10e87bdadde3` 的 GitHub Actions Run `32670082176` 已完成 10 个 Job 全绿，`T2-E2E-005` 仅更新为 `VERIFIED`；项目发起人确认前不得更新为 `ACCEPTED`，也不得启动 Gate 8C 或任何外部执行。
- 完成后必须提交《T2 Gate 8B / Sprint S25 商业 SaaS 运营内部汇总验收报告》等待项目发起人确认；不得自动进入外部执行、完整 Alpha、现场试点、生产发布或后续 Gate。

## 4.55 当前 T2 Gate 8C-Prep 条件准入

- 项目发起人已于 2026-08-24 接受 Gate 8B `CONDITIONAL PASS`，授权 `T2-E2E-005` 更新为 `ACCEPTED`，并授权从 Gate 8B 封存提交 `68d94211b93156d0d87139e4ab5bef421802ad95` 建立 `t2/gate8c-prep-v1-quality-release-audit`。
- Gate 8C-Prep 只允许对现有商业 V1 与 SaaS 运营代码做 P0/P1 安全、性能、可维护性和发布差距静态复核；不得修改正式运行时、数据库迁移、Controller、Vue/Flutter 页面、任务、依赖或业务行为。
- 质量整改候选使用 `T2-SEC-002 → T2-MTN-001 → T2-PERF-002 → T2-RDY-001` 串行依赖，全部保持 `DRAFT`；未经项目发起人再次确认不得进入 `READY/IN_PROGRESS` 或编码。
- 复核必须区分内部代码缺陷、内部证据缺口、外部执行阻断和商业发布阻断。开放 P0/P1 不阻断完成审计本身，但任何内部 P0 未关闭时不得形成内部发布候选，任何外部 P0、许可证、完整 Alpha 或发布阻断未关闭时不得宣称可生产或可商用。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。不得以本轮静态复核、文档或内部证据改变这些状态。
- Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。Secret、真实 PII、商户号、终端号和证书不得进入仓库、日志、CI 或普通制品。
- 完成后只提交《T2 Gate 8C-Prep 启动评审报告》和下一步操作指令等待确认；不得自动启动 Gate 8C 正式整改、完整 Alpha、现场试点或生产发布。

## 4.56 当前 T2 Gate 8C / Sprint S26-A 条件准入

- 项目发起人已于 2026-08-24 接受 Gate 8C-Prep `CONDITIONAL PASS`，并授权从封存提交 `d31bcb325dc94185ed94f87a9028fc381d1932ea` 建立 `t2/gate8c-sprint26a-sec002-runtime`。
- 本批唯一允许准入和实现的需求为 `T2-SEC-002`；只允许关闭 `G8C-SEC-P0-001`、`G8C-SEC-P1-002`、`G8C-SEC-P1-003` 三个既定安全发现，不得新增业务 Owner、状态机、表、迁移、页面或外部能力。
- 生产配置中的必需凭据和密钥必须来自受控 Secret 引用；可选外部集成默认关闭，缺少必需 Secret、使用已知默认值或扩大管理端点暴露时必须启动失败。
- Actuator 仅允许显式白名单端点，健康详情只能按授权显示；独立管理认证继续保留，拒绝访问只记录不含凭据和 PII 的安全审计信息。
- 服务附件必须在打开输入流前校验声明大小，并以固定缓冲区写入受控临时文件、计算 SHA-256 后同步上传；正文不得整包进入堆内存，成功、失败、超限、摘要或长度不一致时均须回收临时文件。
- `T2-MTN-001/T2-PERF-002/T2-RDY-001` 继续 `DRAFT`；外部四项继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-LIC-001/JSH-001` 继续 `DEFERRED`。
- 必须执行治理、Server、Web、Flutter 双平台、Android/Kotlin、MySQL、SQLite、安全、依赖、SBOM、许可证、覆盖率和既有 Gate 全量回归。不得自动重跑失败 Job、降低阈值、修改已发布迁移或以内部整改解除外部证据阻断。
- 完成后只可将 `T2-SEC-002` 更新为 `VERIFIED` 并提交独立周门禁报告等待项目发起人确认；不得自动进入可维护性、性能、发布整改、完整 Alpha 或生产发布。

## 4.57 当前 T2 Gate 8C / Sprint S26-B 条件准入

- 项目发起人已于 2026-08-24 接受 `T2-SEC-002 CONDITIONAL PASS`，授权其更新为 `ACCEPTED`，并授权从 SEC-002 最终封存提交 `eb2a8fec7c102da2db291c34822a01cced768c5d` 建立 `t2/gate8c-sprint26b-mtn001-runtime`。
- 本批唯一允许准入和实现的需求为 `T2-MTN-001`；只允许串行关闭 `G8C-MTN-P1-001..005` 五个既定可维护性发现，不得新增业务能力、状态机、表、迁移、Controller、页面旅程或外部适配能力。
- 串行顺序固定为：Service 对 SaaS 的只读端口化、OpenAPI 当前权威契约唯一化、Flyway 回调审计器修正、Foundation 锁定 SQL 迁入 XML、Flutter 结算应用服务与主页面按职责拆分。前项未通过定向验证，后项不得开始。
- 跨 Owner 调用只能经内层端口与适配器完成；OpenAPI 历史草案必须显式标记且不得参与当前运行时权威契约；审计器兼容合法回调时不得放宽版本迁移命名和已发布迁移保护。
- 锁定查询必须使用 Mapper XML、显式列、`resultMap` 与可信租户条件；Flutter 拆分必须保持 SQLite 单事务、幂等键、错误码、恢复语义、金额数量守恒、金标向量与故障 seed 不变。
- 本批不得修改任何已发布 MySQL/SQLite 迁移，不得改变资金、库存、租户或其他 Owner 事实，不得通过重构引入新依赖或扩大对外 API。
- `T2-PERF-002/T2-RDY-001` 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。
- 必须执行治理、Server、Web、Flutter 双平台、Android/Kotlin、MySQL、SQLite、契约唯一性、迁移审计、租户权限、安全、依赖、SBOM、许可证、覆盖率和既有 Gate 全量回归。不得自动重跑失败 Job、降低阈值、跳过测试、修改历史证据或创建绿色占位。
- 完成后只可将 `T2-MTN-001` 更新为 `VERIFIED` 并提交独立周门禁报告等待项目发起人确认；不得自动进入性能、发布整改、完整 Alpha、现场试点或生产发布。

## 4.58 当前 T2 Gate 8C / Sprint S26-C 条件准入

- 项目发起人已于 2026-08-24 接受 `T2-MTN-001 CONDITIONAL PASS`，授权其更新为 `ACCEPTED`，并授权从 MTN-001 最终封存提交 `262099bf788bcb6916af2480644883fa6c5aed49` 建立 `t2/gate8c-sprint26c-perf002-runtime`。
- 本批唯一允许准入和验证的需求为 `T2-PERF-002`；只允许关闭 `G8C-PERF-P1-001..002` 两项既定性能证据差距，以及修复本轮以固定负载稳定复现的 P0/P1 性能缺陷。任何新业务能力、状态机、表、迁移、Controller、页面旅程或外部适配能力均禁止。
- 必须先冻结正式栈容量模型、执行器规格、数据量、预热、采样、并发、持续运行、尾延迟、资源上限、背压/退化阈值和故障注入，再执行基线；不得先观察结果再放宽阈值，不得以自动重跑掩盖抖动。
- 正式栈至少覆盖 MySQL、Redis、完整 JAR/HTTP、Gate 7/8 已接受 Owner、POS 本地扫描/结算/同步，并记录 P50/P95/P99、吞吐、错误率、CPU、RSS/堆、GC、磁盘、连接与队列/积压；缺失测量、守恒失败、资源超限或错误率超限必须失败关闭。
- 时间指标只允许在冻结的执行器类别、工具链、数据集和负载模型内作为内部合成退化基线；跨执行器只比较正确性和证据结构。不得把 GitHub 执行器或本地容器结果写成真实设备、生产容量、商业 SLA 或扩容承诺。
- 不得修改任何已发布 MySQL/SQLite 迁移，不得改变资金、库存、租户或其他 Owner 事实，不得新增依赖。若需调整运行时代码，必须先保留失败证据、增加回归测试并证明行为、契约、数据主权与资源安全边界不变。
- `T2-RDY-001` 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。
- 必须执行治理、Server、Web、Flutter 双平台、Android/Kotlin、MySQL、SQLite、正式运行栈性能、故障退化、租户权限、安全、依赖、SBOM、许可证、覆盖率和既有 Gate 全量回归。完成后只可将 `T2-PERF-002` 更新为 `VERIFIED` 并提交独立周门禁报告等待确认；不得自动进入 `T2-RDY-001`、完整 Alpha、现场试点或生产发布。

## 4.59 当前 T2 Gate 8C / Sprint S26-D 条件准入

- 项目发起人已于 2026-08-24 接受 `T2-PERF-002 CONDITIONAL PASS`，授权其更新为 `ACCEPTED`，并授权从 PERF-002 最终封存提交 `721130ab57a2fe2b2f024150d85e237491e5b34c` 建立 `t2/gate8c-sprint26d-rdy001-runtime`。
- 本批唯一允许准入和验证的需求为 `T2-RDY-001`；只允许完成内部发布物清单、摘要与合成签名、SBOM/许可证清单、部署预检、运维证据、发布 Go/No-Go 和既定 `G8C-REL-*` 发现的内部处置。禁止新增业务能力、表、迁移、依赖、Controller、页面旅程或外部适配。
- 内部候选必须绑定完整 commit、CI Run、构建工具链、制品 SHA-256、组件 SBOM/许可证、前向迁移证据、配置 Schema、运行手册和证据索引；缺件、重复件、摘要漂移、签名失败、私钥泄漏、生产默认 Secret 或来源不明必须失败关闭。
- 仅允许用单次 CI 内生成且不上传私钥的临时 Ed25519 密钥签署内部清单；该签名不等于 APK 生产签名、KMS、代码签名或商业发布授权。Debug/未生产签名 APK 必须标记 `productionEligible=false`，禁止改名或包装为生产制品。
- 自有 `pos_device_adapter` 的许可证和变更日志占位必须被真实内部所有权通知与版本记录替换，但不得据此关闭 Aviator、simple-http、MySQL Connector/J。`T2-LIC-001` 继续 `DEFERRED / NO-GO COMMERCIAL`，三项关闭证据仍为 0/3。
- `G8C-REL-P0-001/P0-002/P1-003/P1-004` 均不得被内部合成证据伪造为商业关闭；分别保留 `DEFERRED_BLOCKER/BLOCKED_EXTERNAL/ASSUMPTION_BLOCKED/DRAFT_BLOCKER`，同时验证内部候选可以生成明确、机器可读且失败关闭的 `NO_GO` 决策。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、真实设备/外设命令、伙伴现场、完整 Alpha、生产部署、商业 tag、商业声明和 SLA 必须为 0。
- 必须执行治理双平台、Server、Web、Flutter 双平台、Android/Kotlin、MySQL、SQLite、部署预检、恢复/升级回退、安全、依赖、SBOM、许可证、覆盖率、发布制品装配、签名验真和既有 Gate 全量回归。完成后只可将 `T2-RDY-001` 更新为 `VERIFIED` 并提交独立周门禁报告等待确认；不得自动启动外部执行、完整 Alpha、现场试点或生产发布。

## 4.60 当前 T2 Gate 8D-Prep 条件准入

- 项目发起人已于 2026-08-24 接受 `T2-RDY-001 CONDITIONAL PASS`，授权其更新为 `ACCEPTED`；接受上限仍为 `INTERNAL_RELEASE_READINESS_CANDIDATE`，不代表真实签名/KMS、生产 PITR/灾备、外部执行、完整 Alpha、生产或商业 SLA。
- Gate 8D-Prep 只能从 RDY-001 最终封存提交 `bd1dee42bacfb75874d601e16828c8f82720986b` 建立 `t2/gate8d-prep-external-p0-license-alpha-admission`；只允许受控材料离线核验、商业许可证关闭证据复核、UAT/REL 启动条件冻结和治理证据，不得修改运行时、迁移、依赖、业务能力或外部适配器。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 必须逐轨、逐项核验不透明引用、来源、授权签署、适用产品/型号与版本、范围、SHA-256、保管、到期、轮换、删除和吊销规则；缺少真实受控材料时必须维持 `NOT_ACHIEVED/BLOCKED/NO_GO`，不得用公开资料、内部合成证据或历史模板创建绿色占位。
- `T2-LIC-001` 必须分别核验 Aviator、simple-http、MySQL Connector/J 的精确使用清单、替换回归或适用分发模式的书面法务批准、最终 SBOM/许可证/NOTICE/发布 BOM。三项未全部 `CLOSED` 时保持 `DEFERRED`，商业发布固定 `NO_GO`。
- `T2-UAT-001` 只有在四条外部轨分别达到 `VERIFIED_DOCUMENT`、取得独立执行批准、具名 RACI/隔离环境/停止开关/恢复点/数据清除方案就绪且 P0/P1 为 0 后，才可另行评审启动；本阶段继续 `DRAFT`，完整 Alpha 运行数为 0。
- `T2-REL-001` 只有在完整 Alpha `ACCEPTED`、许可证 3/3 关闭、真实签名与密钥边界、生产恢复/回退、发布物和角色联合签署齐备后，才可另行准入；本阶段继续 `DRAFT`，生产部署、商业 tag、商业声明和 SLA 为 0。
- Secret、商户号、终端号、证书、真实 PII、未脱敏商户数据和受控原文不得进入 Git、普通日志、CI 或普通制品；仓库只能保存经批准的不透明引用和不含敏感内容的验证元数据。
- 完成后只能提交五条独立准入/关闭报告和《T2 Gate 8D-Prep 启动评审报告》等待项目发起人确认；不得自动进入 Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha、生产发布或商业可用声明。

## 4.61 当前 T2 Gate 9A-Prep 商业 V1 内部产品完整性审计条件准入

- 项目发起人已于 2026-08-24 明确暂不处理商业许可证、真实支付、真实硬件、真实外设和设计伙伴，授权从 Gate 8D-Prep 最终封存提交 `4ba20f8bb9bfdc36f4fee1b831ca35c7b54b9533` 建立 `t2/gate9a-prep-internal-product-completeness-audit`，只做现有内部业务代码完整性审计与缺陷修复准备。
- 本阶段唯一准入需求为 `T2-CMP-001`。必须覆盖当前 87 项 `ACCEPTED` 需求和全部 22 个正式 Owner 模块，建立 Requirement 到页面、API、应用、领域、持久化、MySQL/SQLite、事件/Outbox、权限审计、测试和 CI 的机器可追踪链路。
- 历史 Gate 6G 审计器和证据必须保持不可变；其 15 Owner 快照不得继续作为当前完整性结论。新审计器必须显式覆盖 Catalog、Costing、Foundation、Integration、Inventory、Member、Migration、Onboarding、Operations、Order、Payment、Procurement、Promotion、Release、Reporting、Resilience、Returns、SaaS、Service、Subscription、Sync、Transfer 共 22 个 Owner。
- 审计必须区分：测试夹具、明确失败关闭边界、平台兼容分支、未批准演示/临时生产路径和真实缺陷。不得把 `Locked`、`InMemory`、`FAKE_TEST` 或 `UnsupportedOperationException` 的文本命中直接等同于业务缺陷，也不得用宽泛白名单隐藏未分类实现。
- 允许修复审计工具、覆盖矩阵、治理契约和证据生成器；本准备阶段不得修改业务运行时、已发布迁移、依赖、Controller、Vue/Flutter 业务页面或后台任务。审计发现的既有需求 P0/P1 必须进入缺陷账并按批次等待项目发起人确认后修复。
- 新业务能力必须先分配唯一 Requirement ID、提交 CR、完成影响分析和项目发起人确认；不得借“缺陷修复”扩大商业 V1 范围。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-LIC-001/JSH-001` 继续 `DEFERRED`；Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha 和生产部署继续为 0。
- 完成后只允许提交完整性审计报告、缺陷账、页面/API 覆盖矩阵、分批修复计划和第一批正式修复启动指令，等待项目发起人确认；不得自动进入运行时缺陷修复、完整 Alpha、现场试点或生产发布。

## 4.62 当前 T2 Gate 9B / Sprint S27-A 正式 API 与 OpenAPI 一致性修复条件准入

- 项目发起人已于 2026-08-24 接受 `T2-CMP-001 STATIC_GOVERNANCE_AND_REPOSITORY_AUDIT CONDITIONAL PASS`，授权其更新为 `ACCEPTED`，并授权从 Gate 9A-Prep 最终封存提交 `f708271e977f995e83a24fe398a1bd658726fd09` 建立 `t2/gate9b-sprint27a-api-openapi-alignment`。
- 本批只允许修复 `G9A-API-P1-001`，复用已接受的 `T2-API-001`；不得新增 Requirement ID、业务能力、Controller、页面旅程、状态机、表、任务、依赖或数据库迁移。
- 必须冻结 300 项 Controller、257 项当前 OpenAPI、Vue/Flutter 客户端调用以及 64 项 Controller 无当前 OpenAPI、21 项当前 OpenAPI 无精确 Controller 的基线；每项差异必须归为补齐当前契约、修正实现、历史草案/替代契约或经审批删除的孤立能力。
- 当前正式 Controller、已接受 Owner 行为和客户端调用共同确定当前契约；优先修正 OpenAPI。确需修改实现时必须先增加失败回归，并证明不改变权限、可信租户、资金、库存、幂等、错误和失败关闭语义。
- 当前 OpenAPI 必须使用机器可发现的 `openapi-*.yaml`，每项操作具有全局唯一 `operationId`；历史草案必须显式声明 `HISTORICAL_DRAFT_NON_RUNTIME` 和有效替代契约，禁止为了归零静默删除能力或把草案提升为当前。
- Gate 9A 原始报告、缺陷账和历史 Gate 6G 审计器保持不可变；Gate 9B 必须建立独立 85 项分类账、闭环证据和周门禁报告。退出时 Controller/OpenAPI 必须达到 300/300、双向差异 0/0、客户端调用全部有正式服务端实现。
- 必须执行治理、Server、Web、Flutter 双平台、Android/Kotlin、MySQL、SQLite、权限、租户、错误码、幂等、兼容、安全、依赖、SBOM、许可证、覆盖率及既有 Gate 全量回归；不得降低阈值、跳过失败测试、自动重跑掩盖 Flaky 或修改已发布迁移。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。
- 完成后只可提交《G9A-R1 正式 API 契约修复独立周门禁报告》等待项目发起人确认；不得自动进入 G9A-R2、完整 Alpha、现场试点或生产发布。

## 4.63 当前 G9A-R2 生产装配与演示面收敛准备阶段条件准入

- 项目发起人已于 2026-08-24 接受 `G9A-API-P1-001 CONDITIONAL PASS` 并确认其
  `CLOSED_IN_GATE9B`，授权从 Gate 9B 最终封板提交
  `53e540fd14559e7ae0f907b244b0dbac37167cfe` 建立
  `t2/gate9b-sprint27b-g9a-r2-prep`。
- 本阶段只允许复用 `T2-CORE-001/T2-SEC-002/T2-RDY-001` 对
  `G9A-ASM-P1-001` 做静态装配审计、构建/SBOM/路由/初始化影响验证、保留能力决策、
  回退设计、测试矩阵和启动评审；不得新增 Requirement ID 或业务能力。
- 必须分别核验 `ruoyi-demo` 在 `ruoyi-admin`、默认 Maven reactor、JAR、Springdoc、
  聚合 SBOM、Vue 动态视图、编译产物、四方言初始化菜单/角色绑定/测试表中的实际装配。
- `ruoyi-generator`、`ruoyi-workflow`、`ruoyi-job` 等非商业 V1 平台能力必须逐项说明
  保留价值、默认开关、商业 JAR/Web/菜单/SBOM 边界和重新启用准入；不得仅隐藏菜单。
- `G9A-ASM-P1-001` 在准备阶段继续 `OPEN`。本阶段不得修改 POM、运行时配置、Vue 页面、
  初始化 SQL、已发布迁移、Controller、领域代码、依赖锁文件或正式发布物。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001`
  继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、
  设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。
- 完成后只提交《G9A-R2 启动评审报告》和下一步正式整改指令等待项目发起人确认；
  不得自动进入 G9A-R2 正式编码、G9A-R3、完整 Alpha、现场试点或生产发布。

## 4.64 当前 G9A-R2 生产装配与演示面收敛正式整改条件准入

- 项目发起人已于 2026-08-24 接受 G9A-R2 准备阶段 `CONDITIONAL PASS` 和 ADR-070
  的商业默认装配建议，授权从准备阶段最终封存提交
  `b9333b85f1b46ac444b83346a6a3d44204e7d723` 建立
  `t2/gate9b-sprint27b-g9a-r2-runtime`。
- 本批只允许关闭 `G9A-ASM-P1-001`，复用已接受的
  `T2-CORE-001/T2-SEC-002/T2-RDY-001`；不得新增 Requirement ID、业务能力、领域状态机、
  资金/库存/租户行为或已发布迁移。
- 必须先建立失败回归，再按 Maven/JAR、Web/路由、MySQL/Oracle/PostgreSQL/SQL Server
  初始化、SBOM/配置、启动和完整 CI 串行收敛；前一面未验证不得将后一面作为缺陷关闭证据。
- 商业无参数默认构建必须移除 `ruoyi-demo`；`ruoyi-generator`、`ruoyi-workflow`、
  `ruoyi-job` 服务端源码只允许保留在默认 reactor 和商业 JAR/Web/菜单/SBOM 之外。
  仅隐藏菜单、关闭按钮或依赖前端权限均不构成关闭。
- `ruoyi-demo` 活动生产源码和 Vue 演示面按 ADR-070 删除，必要时仅从本基线 Git 历史恢复；
  `src/test` 纯测试夹具和已接受 Owner 的虚构合成数据不得按名称误删。
- 四方言新装初始化源必须移除演示菜单、角色绑定、测试表和未准入平台菜单；既有环境只允许
  带冻结目标、摘要校验、审计与失败关闭的前向清理，禁止改写已发布 Flyway/SQLite 迁移。
- 正式 300 项 Controller/OpenAPI、22 个 Owner、租户权限、安全、Server/Web/Flutter、
  Android/Kotlin、MySQL/SQLite、SBOM、许可证、覆盖率和既有 Gate 必须完整回归且不得降级。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001`
  继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、
  设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。
- 完成后只提交《G9A-R2 生产装配与演示面收敛独立周门禁报告》等待项目发起人确认；
  不得自动进入 G9A-R3、完整 Alpha、现场试点或生产发布。

## 4.65 当前 G9A-R3 页面状态、权限与失败恢复准备阶段条件准入

- 项目发起人已于 2026-08-24 接受 `G9A-ASM-P1-001 CONDITIONAL PASS` 并确认其
  `CLOSED_IN_GATE9B`，授权从 G9A-R2 最终封板提交
  `d1947139a7538b9724dcec236d4ded9255adc74c` 建立
  `t2/gate9b-sprint27c-g9a-r3-prep`。
- 本阶段只允许审计 `G9A-UI-P1-001`，复用已接受的 `T2-UX-001/T2-ADM-001/
  T2-ADM-002/T2-POS-007/T2-POS-008/T2-POS-009`；不得新增 Requirement ID、业务能力、
  页面运行时、依赖、数据库表、Controller、任务或已发布迁移。
- 必须冻结 Gate 9A 已登记的 20 个 Vue 页面/面板与 6 个 Flutter 页面，逐页核验路由或父级
  装配、按钮、展示权限、服务端最终授权、数据范围、加载、空态、错误码、重复点击、离线、
  业务日切换、失败恢复和直接组件/Widget 测试证据。静态字符串断言不得冒充交互测试。
- 页面路由或导航不可达、权限按钮缺失、客户端租户声明、重复业务命令、错误被吞掉、空白页、
  `UNKNOWN` 重新生成命令或无可执行恢复路径，必须登记为整改缺口，不得用全局拦截器或服务端
  测试静默标记为页面通过。
- 准备阶段只形成页面清单、审计矩阵、测试缺口、串行修复计划、验收标准和启动评审报告；
  `G9A-UI-P1-001` 继续 `OPEN`，未经项目发起人再次确认不得进入页面正式整改或 G9A-R4。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001`
  继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、
  设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。

## 4.66 当前 G9A-R3A 主路径页面正式整改条件准入

- 项目发起人已于 2026-08-24 接受 G9A-R3 准备阶段 `CONDITIONAL PASS` 和 ADR-071，
  授权从准备阶段最终封存提交 `5302e2f8a020b5a058807d53312741e433c3ddde` 建立
  `t2/gate9b-sprint27c-g9a-r3a-runtime`；ADR-071 更新为 `Accepted`。
- 本批只允许整改 `VUE-01/02/03/04` 与 `FLT-03/04/06` 对应的既有
  `G9A-UI-P1-001` 缺口，复用 `T2-UX-001/T2-ADM-001/002/T2-POS-007/008/T2-PAY-004`；
  不得新增 Requirement ID、业务能力、Controller、API、表、任务或迁移。
- 必须先增加失败回归，再按路由、按钮最小权限、加载/空态/安全错误、关联标识、单航班、
  离线、可信业务日和原幂等键恢复串行整改。Vue 新增挂载测试依赖前必须记录精确版本、用途、
  许可证、替代方案和安全结论，并纳入锁文件、SBOM、漏洞和许可证门禁。
- Vue 展示权限必须与正式 Controller 最小权限一致，服务端可信 tenant 与门店范围继续最终授权；
  页面不得复制 Owner 算法或用菜单/按钮隐藏替代服务端授权。
- `PosTenderPage` 只能通过可信会话以及冻结订单、班次、门店和业务日上下文进入；页面不得自行
  声明 tenant_id、应收或支付成功。电子支付份额必须以 `BLOCKED/UNAVAILABLE` 失败关闭，
  UNKNOWN 只能查询原计划，禁止生成替代命令。
- 不得新增 Provider SDK/HTTP、真实网络调用，不得直接访问 Mapper/SQLite/MethodChannel，
  不得修改已发布迁移。七页必须有 Vue 挂载组件或 Flutter Widget 交互证据并逐项通过十二维验收。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续
  `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。完成后只提交 G9A-R3A 周门禁报告等待确认；
  不得自动进入 G9A-R3B、G9A-R4、完整 Alpha、现场试点或生产发布。

## 4.67 当前 G9A-R3B Owner 运营页面整改准备阶段条件准入

- 项目发起人已于 2026-08-25 接受 G9A-R3A `CONDITIONAL PASS`；该接受只覆盖
  `VUE-01/02/03/04` 与 `FLT-03/04/06` 的内部页面交互证据，整体
  `G9A-UI-P1-001` 继续 `OPEN`。本阶段从 G9A-R3A 最终治理提交
  `838bdacdfdd3e4113c6cf7d43cf4ad024d63ff14` 建立独立准备分支。
- 本阶段只允许审计和冻结 `VUE-05..15`：业务迁移、促销与会员、库存成本、会员权益、
  发布、补货、供应链、日结、异常中心、批次效期和门店开通；不得修改页面运行时、API、
  Controller、Owner 领域代码、依赖、数据库表、任务或已发布迁移。
- 必须逐页形成可复现失败 seed，核对完整父子装配链、Controller 最小权限、可信租户与门店
  范围、加载/空态/安全错误、单航班、原幂等键恢复、危险操作确认和挂载组件测试。审计证据
  错误必须显式纠偏，不得为了归零而新增重复路由或删除正式能力。
- 必须冻结 Owner 不变量保护矩阵；后续页面整改只能调用既有正式 API，不得复制促销、会员、
  库存、成本、采购、调拨、发布、日结、异常、批次或开店算法，不得跨 Owner Mapper，
  不得由前端提交 tenant_id、金额、数量、成本、资金结果或 Owner 最终状态作为权威事实。
- 本阶段只形成治理记录、失败 seed、串行修复计划、测试矩阵、专用审计脚本、CI 和启动评审
  报告；`G9A-UI-P1-001` 继续 `OPEN`。未经项目发起人再次确认不得进入 R3B 页面正式整改、
  G9A-R3C、G9A-R4、完整 Alpha、现场试点或生产发布。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001`
  继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、
  设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。

## 4.68 当前 G9A-R3B Owner 运营页面正式整改条件准入

- 项目发起人已于 2026-08-25 接受 G9A-R3B 准备阶段 `CONDITIONAL PASS`，授权从
  `4e8a9f2b1dd52ce6b198bd3a25328e2a80330a71` 建立
  `t2/gate9b-sprint27d-g9a-r3b-runtime`，只整改 `VUE-05..15` 对应的既有
  `G9A-UI-P1-001` 缺口；复用已接受 Requirement 和 ADR-071，不新增业务能力或
  Requirement ID。
- 必须按 `R3B-R0` 至 `R3B-R11` 严格串行，先以可复现失败测试冻结共享状态、最小权限、
  原幂等键、单航班、危险操作确认和挂载行为，再进行最小页面修复；前一步未独立验证，
  后一步不得铺开。
- `VUE-08` 的权益预检、会员价预检/审批/发布必须使用与正式 Controller 对齐的最小权限；
  `VUE-08/13/14` 的未知写结果必须保留原对象、动作和幂等键，只允许查询或观察原命令，
  禁止生成替代命令。
- 十一个页面必须分别呈现加载、空态、安全错误、关联标识、单航班、危险操作确认和可执行
  恢复；前端不得复制 Owner 算法、跨 Owner Mapper、提交 tenant_id/金额/成本/资金结果作为
  权威事实，页面权限不得替代服务端最终授权。
- 本批禁止修改服务端 Controller/API、Owner 领域代码、依赖、MySQL/SQLite 表和已发布迁移；
  只允许 Vue 页面、共享页面状态与交互回归，以及对应治理、契约、CI 和证据变更。
- 本批完成后只能形成 `G9A-R3B VERIFIED_CANDIDATE`；由于 R3C 和 26 页联合验收尚未执行，
  `G9A-UI-P1-001` 继续 `OPEN`。未经项目发起人确认不得进入 R3C、R4、完整 Alpha、现场试点
  或生产发布。
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001`
  继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。Provider 网络、真实资金、
  设备/外设命令、伙伴现场、完整 Alpha、生产部署和商业声明必须为 0。

## 5. 工程与测试规则

- 服务端采用 RuoYi-Vue-Plus 模块化单体，不得把领域逻辑写入 Controller、通用工具类或框架系统模块。
- 自研内部业务方法出现 5 个及以上业务参数时，默认必须改用不可变、具名且职责内聚的参数对象；不足 5 个但存在多个同类型参数、两个及以上布尔开关或一组经常共同变化的可选参数时，也应提前封装。应用层使用 `Command/Query`，领域层使用值对象或具名参数对象，只有跨进程/接口边界才称 DTO；禁止用 `Map`、`Object[]`、万能 DTO 或无关字段拼盘规避参数数量。可信租户、权限和操作者上下文不得混入客户端 DTO 或由客户端构造。框架规定签名、第三方接口、序列化模型及依赖注入构造器不机械套用本阈值，但必须在评审中说明例外；构造器依赖过多应优先拆分职责。
- 新增业务表在编码前必须分别声明数据访问策略 `CRUD_ENTITY/CONTROLLED_WRITE/APPEND_ONLY/READ_PROJECTION` 和 SQL 模式 `MP/XML/HYBRID`。普通主数据、配置和普通可变实体默认使用基础设施层持久化实体、`BaseMapper` 与类型安全 Lambda Wrapper；同一 Mapper 可以同时声明由 XML 实现的复杂方法，只用 XML 的普通可变实体仍登记为 `CRUD_ENTITY + XML`。状态条件更新只开放具名受控能力，支付、退款、库存/成本/在途账本、历史、审计、Outbox/Inbox 和幂等事实禁止暴露通用更新或删除；严格 `APPEND_ONLY` 行不得更新，Outbox/Inbox 若在同一行迁移投递状态必须登记为 `CONTROLLED_WRITE`，或拆成只追加载荷与受控投递状态。SQL 模式不得扩大访问策略授权。ADR-033 生效前已完成合法准入的旧三类登记（含进行中 Gate）作为兼容历史证据保留，不因本 CR 批量改写；旧 `XML_ONLY` 必须结合原模型与 `mutation_boundary` 兼容解释为 `CRUD_ENTITY`、`CONTROLLED_WRITE` 或 `APPEND_ONLY`，新登记立即使用双维度，既有表下一次改变访问边界或 SQL 组合时转换。
- `record` 优先用于不可变的 DTO、Command、Query、值对象、只读投影和 Mapper 参数，不作为 MyBatis-Plus 持久化实体或有生命周期聚合的默认实现。持久化实体与领域聚合必须分离；应用层不得直接依赖基础设施 Mapper，Repository/查询端口的接口位于内层、实现在基础设施层。
- 新增或实质修改的持久化实体必须有中文类级注释，所有映射业务字段必须说明业务含义；`record` 必须有中文类型说明并用 `@param` 说明每个组件。新增业务表及每个字段必须写有效中文 `COMMENT`，金额、数量、币种、时间、状态、ID、租户、哈希和版本必须注明单位、来源或约束。已发布 Flyway 迁移严禁改写，只能通过独立前向迁移补充数据库注释，并评估元数据锁、兼容和回退。
- 服务端正式模块允许并优先复用当前基线已经集成的 MyBatis-Plus：单表、条件明确且无需特殊执行计划的简单 CRUD 使用类型安全的 Lambda Wrapper/BaseMapper；多表关联、聚合、子查询、窗口函数、复杂动态条件、锁与执行计划敏感 SQL 必须进入 Mapper XML，显式字段、`resultMap`、租户条件和排序规则。普通 `CRUD_ENTITY` Mapper 可采用 `HYBRID`，即继承 `BaseMapper` 的同时声明同 namespace XML 方法；禁止 `SELECT *`，禁止把复杂 SQL 塞进注解或超长 Wrapper。
- MyBatis-Plus 与 Mapper XML 都只能位于持久化适配边界；Controller 禁止直接调用 Mapper，领域对象禁止依赖 MyBatis，核心模块通过 Repository/应用端口访问持久化。MyBatis 租户拦截器不是唯一授权措施，Wrapper、XML、原生 SQL、批处理和任务均须接受租户逃逸测试。
- 允许使用 Hutool 及其他成熟稳定组件解决通用技术问题，并优先复用仓库现有 BOM/模块；不得为了少量调用引入重复依赖或自造通用轮子。新增或扩展依赖必须记录用途、版本、许可证、替代方案和安全风险，通过依赖、SBOM、漏洞及许可证门禁，禁止引入 `hutool-all`。
- Hutool/通用组件不得代替领域模型和安全设计：金额、数量、业务时区、状态机、租户授权、幂等、密码学、支付签名、HTTP 超时重试和敏感对象映射必须使用项目明确的值对象、策略或经评审的专用能力；禁止用 Bean 拷贝绕过聚合状态迁移。
- 核心代码和实体必须具有有效中文注释：聚合、实体、值对象、状态机、领域/应用服务、Repository/Mapper、事务边界、外部契约及复杂 SQL 应说明业务用途、数据主权、关键不变量、单位、失败语义或并发语义；实体类和非显然业务字段、状态枚举值必须说明含义。标识符继续使用英文，禁止逐行翻译代码、注释显然的 getter/setter 或保留与实现不一致的注释。
- Web 路由权限只控制展示，最终授权必须由服务端执行。
- Flutter UI/业务层禁止直接调用 `MethodChannel`；所有设备能力通过 `pos_device_adapter` 契约。
- API 优先维护 OpenAPI，事件和连接器对象必须有版本化 Schema 与契约测试。
- 新依赖必须记录用途、版本、许可证、替代方案和安全风险；禁止浮动版本进入发布分支。
- 禁止把密钥、口令、Token、支付报文或未脱敏个人信息提交到仓库和日志。
- 测试必须覆盖成功、认证、授权、租户越权、参数校验、幂等、重复、乱序、超时、崩溃恢复和回滚中与风险相匹配的部分。
- 修复缺陷时必须先补回归测试；不能以“无法复现”直接关闭。

## 6. Definition of Done

功能只有同时满足以下条件才可标记完成：

- 代码、Schema、迁移、API/事件文档和 RTM 同步更新；
- 单元、集成、契约、E2E 或实机测试与风险匹配且有证据；
- 租户、权限、审计、幂等、脱敏、异常恢复已验证；
- 监控、告警、Runbook、灰度、回滚和数据修复已覆盖；
- `docs/governance/rtm.csv` 已关联实现和测试证据；
- 不存在未批准的范围扩张或未记录 ADR；
- 资金、库存、租户、离线和安全相关变更已由独立负责人复核。

## 7. 每次交付报告

每次实现必须报告：需求 ID、依据文档、变更文件、行为变化、测试结果、租户/权限/幂等处理、迁移/回滚、未解决风险和 RTM 状态。没有可复核证据不得声称“已完成”或“可商用”。
