# 鲸熵汇收银系统研发强制规则

本仓库用于可商业落地的多租户收银系统。任何人工或 AI 变更都必须遵守本文件；目录内若有更具体的 `AGENTS.md`，只允许收紧规则，不允许降低本文件的资金、库存、租户、安全和验收底线。

## 1. 需求权威与冲突处理

- 范围权威：`docs/连接器型商业收银经营平台_商业可落地优化方案_V3.0.md` 的商业 V1 边界。
- 业务行为权威：`docs/详细设计/31` 至 `39`。
- 技术实现权威：`docs/连接器型商业收银经营平台_技术架构与开发规范_V1.0.md`。
- 商业验收权威：`docs/详细设计/40_商业V1验收测试计划_V1.0.md`。
- `完整功能规划_V2.0` 是全景 Backlog，不代表全部进入商业 V1。
- 已批准变更必须记录在 `docs/governance/change-log.md`，架构决策必须记录为 ADR。
- 发现冲突、缺失或存在多种会改变资金/库存/租户事实的解释时，停止该部分实现并提出决策，不得自行猜测。

## 2. 开发准入

每个功能进入编码前必须具备：

1. 唯一 `requirement_id`，并登记在 `docs/governance/rtm.csv`；
2. 来源文档与章节、范围、非目标和可执行验收标准；
3. 涉及核心业务时，明确状态机、不变量、权限、审计、幂等和失败补偿；
4. 涉及跨模块时，先批准 API/事件/数据契约和数据主权；
5. 涉及数据库时，先定义 Flyway 迁移、兼容、容量和回滚/前向修复；
6. 涉及设备时，明确认证机型、SDK、实机和模拟器用例。

## 3. 不可突破的领域底线

- `tenant_id` 必须由可信上下文注入；客户端提交的租户标识不得直接成为授权依据。
- RuoYi 平台主键沿用 `BIGINT`；离线可创建的业务实体使用 ULID；业务 `tenant_id` 为 `VARCHAR(20)`。
- 结算金额使用最小货币单位整数并保存币种；单价、成本、税率和中间值使用 `BigDecimal/DECIMAL`，禁止 `float/double`。
- 订单、支付、退款、库存、积分和储值事实不得物理删除或用覆盖更新抹去历史，只能以状态迁移、冲正或新账本记录修正。
- 支付 `UNKNOWN` 必须查询或对账确认，禁止自动再次扣款。
- 库存只能通过不可变账本变化；可用量、在手量和预占量必须可重算、可对账。
- 优惠分摊必须金额守恒；退款沿用成交快照，不得按当前促销重算。
- POS 本地交易、支付意图和 Outbox 必须在同一个 SQLite 事务中提交；同步必须幂等并允许重复和乱序。
- Redis、搜索、报表和消息系统不得成为订单、支付或库存唯一事实来源。
- 核心系统不得针对鲸熵汇、美团、抖音、京东或其他渠道写特殊分支；渠道差异只能进入连接器与映射层。

## 4. T0 边界

T0 只允许：治理、ADR、RTM、工程骨架、契约骨架、开发环境、CI、健康检查、模拟器和构建验证。

T0 禁止：正式交易逻辑、真实支付、库存扣减、促销计算、生产密钥、生产数据和未经批准的 V1 外功能。

## 4.1 当前 T1-Prep 阶段边界

- 当前唯一技术基线为 annotated tag `t0-baseline-2026-08-16`；T1-Prep 变更必须从该 tag 建立独立评审分支。
- T1-Prep 只允许项目章程、范围与非目标、RTM、ADR、PoC 方案、选型输入、测试与故障注入计划、CI/证据规范、风险和启动评审材料。
- 未经项目发起人明确 Go 确认，不得进入 T1 PoC 编码；不得开发订单、支付、退款、库存、促销等正式商业业务。
- 真实设备型号/SDK、至少一个真实支付沙箱及设计伙伴未落实的相关需求必须标记 `BLOCKED`；允许使用 Fake 验证契约，但 Fake 证据不得替代实机、支付沙箱或商业验收。
- 鲸熵汇连接器资料按项目发起人指令延后收集，在收银系统主体完成前保持 `DEFERRED`；不得猜测其接口、鉴权、限流或回调行为。
- 禁止真实支付、生产密钥、生产终端证书和未脱敏商户数据进入 T1 环境、仓库、日志、制品或测试证据。
- Aviator、simple-http、MySQL Connector/J 的法务或替换事项不阻断 T1-Prep 和风险 PoC，但仍是商业发布前强制阻断项，不得静默关闭。
- GitHub 不升级套餐；继续使用仓库自有依赖差异、SBOM、漏洞、许可证、密钥和 IaC 补偿门禁。

## 4.2 当前 T1 Week 1 条件准入

- 项目发起人已于 2026-08-16 确认 `CONDITIONAL GO`；只允许 RTM 中已经通过准入并转为 `IN_PROGRESS` 的 `STATIC/FAKE` 风险 PoC。
- Week 1 仅允许稳定契约、合成数据、Fake Provider/Device、可复现故障夹具、隔离 PoC 探针、T1 CI 与证据；所有探针必须位于可整体删除的 `poc/`、测试或专用契约目录。
- `T1-HWD-002`、`T1-PRN-001`、`T1-SCN-001`、`T1-SCL-001`、`T1-IO-001`、`T1-PAY-002`、`T1-PAR-001` 继续 `BLOCKED`，不得以模拟器/Fake 关闭。
- 支付候选池为十家，T1 只从中评选五家运行 Fake 契约并最多准入一家真实沙箱；当前 Week 1 禁止沙箱调用和真实支付。
- Week 1 完成后必须提交周门禁报告并再次等待项目发起人确认；不得自动进入 Week 2 或 T2。

## 5. 工程与测试规则

- 服务端采用 RuoYi-Vue-Plus 模块化单体，不得把领域逻辑写入 Controller、通用工具类或框架系统模块。
- Web 路由权限只控制展示，最终授权必须由服务端执行。
- Flutter UI/业务层禁止直接调用 `MethodChannel`；所有设备能力通过 `pos_device_adapter` 契约。
- API 优先维护 OpenAPI，事件和连接器对象必须有版本化 Schema 与契约测试。
- 新依赖必须记录用途、版本、许可证、替代方案和安全风险；禁止浮动版本进入发布分支。
- 禁止把密钥、口令、Token、支付报文或未脱敏个人信息提交到仓库和日志。
- 测试必须覆盖成功、认证、授权、租户越权、参数校验、幂等、重复、乱序、超时、崩溃恢复和回滚中与风险相匹配的部分。
- 修复缺陷时必须先补回归测试；不能以“无法复现”直接关闭。

## 6. Definition of Done

功能只有同时满足以下条件才可标记完成：

- 代码、Schema、迁移、API/事件文档和 RTM 同步更新；
- 单元、集成、契约、E2E 或实机测试与风险匹配且有证据；
- 租户、权限、审计、幂等、脱敏、异常恢复已验证；
- 监控、告警、Runbook、灰度、回滚和数据修复已覆盖；
- `docs/governance/rtm.csv` 已关联实现和测试证据；
- 不存在未批准的范围扩张或未记录 ADR；
- 资金、库存、租户、离线和安全相关变更已由独立负责人复核。

## 7. 每次交付报告

每次实现必须报告：需求 ID、依据文档、变更文件、行为变化、测试结果、租户/权限/幂等处理、迁移/回滚、未解决风险和 RTM 状态。没有可复核证据不得声称“已完成”或“可商用”。
