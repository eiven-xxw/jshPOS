<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jingshanghui.pos.order.infrastructure.persistence.mapper.StoreOnboardingShiftMapper">
    <select id="countOpenOrClosing" resultType="int">
        SELECT COUNT(*) FROM shf_shift
        WHERE tenant_id=#{tenantId} AND store_id=#{storeId} AND status IN ('OPEN','CLOSING')
    </select>
</mapper>
