# CR-T2G10A-017：RPT-SALES 精确整改运行时准入

- 日期：2026-08-26
- 状态：`APPROVED_IN_PROGRESS`
- Finding：`G10A-SQL-P2-001`（继续 `OPEN`）
- 复用需求：`T2-RPT-001`、`T2-API-001`
- 决策：ADR-074
- 起点：`972fcfafdf131a555e47f5644e0ae914ce6297fb`

## 批准范围

项目发起人接受 `CR-T2G10A-013/014/015` 的兼容方向，但本批只授权实施
`CR-T2G10A-013`：

1. 先保留 `R2R2R2-F01/F04/F07/F08/F09/F12` 的可复现红基线；
2. 新增 Reporting Owner 销售批量读取端口；
3. 新增独立 v2 销售日报 keyset 分页，旧 v1 列表契约保持冻结；
4. 销售安全导出改为固定批次流式生成，并保存请求摘要、已确认字节偏移和签名游标；
5. 在 MySQL 8.4.11 固定 10k/100k 分布执行计划、查询次数、守恒和租户攻击取证。

## 不变量与停止线

- `tenant_id` 只来自可信上下文，客户端没有租户参数；门店范围由服务端逐项授权。
- 游标绑定租户、筛选摘要、投影版本和复合排序键；篡改或换筛选必须使用稳定错误码失败关闭。
- 旧接口、金额字段、投影事实、排序业务含义、导出审批与审计保持兼容。
- `RPT-INVENTORY`、`RPT-PAY-REC` 和其他九项查询不得提前整改。
- 不改变资金、库存、支付、同步、事件 Schema 或其他 Owner 事实。
- 不修改任何已发布迁移。若计划需要索引，立即停止索引实施，提交独立索引 CR 和唯一前向迁移。
- 本批不得关闭 `G10A-SQL-P2-001`，不得进入 `G10A-RES-P2-001`、R3、完整 Alpha 或生产。

## 回退

关闭 v2 路由与销售流式导出装配即可回退到冻结的 v1 查询和旧导出兼容路径；不得删除报表事实、
导出申请、审计或历史制品记录。已经写入的临时流式文件必须按受控目录和清理规则处置。

