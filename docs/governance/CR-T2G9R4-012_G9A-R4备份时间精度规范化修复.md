# CR-T2G9R4-012：G9A-R4 备份时间精度规范化修复

## 1. 触发证据

- 候选提交：`a7a372559fe1f424ee86f350979ce17ed4bd0df7`
- GitHub Actions Run：`32895617666`
- 失败 Job：`formal-runtime`（`97960016014`）
- 三条 Flutter 正式 JAR 旅程均完成，便利店/零食折扣店/社区超市 Outbox 分别以 `10/10/12` 条全部 ACK；首条正式合成恢复在创建成功后稳定失败为 `BAK-RST-008: 清单摘要不匹配`。

正式表 `bak_backup_set.point_in_time`、`latest_included_fact_at` 和 `immutable_until` 使用 MySQL `DATETIME(3)`。创建应用服务此前以 Java `Instant` 的纳秒精度生成幂等摘要、源快照和规范清单，目录持久化后只保留毫秒；恢复从数据库回读并重算清单时因此得到不同摘要。单元测试原先使用整秒时间，未覆盖该正式持久化边界。

## 2. 边界判断

该缺陷属于已接受 `T2-BAK-001` 的持久化一致性问题，并由 `G9A-E2E-P1-001` 正式栈旅程揭示。修复不新增业务能力、Requirement ID、API、表或迁移，不改变资金、库存、租户或恢复证据等级。

仅修改清单字符串或在验收脚本中舍弃小数会使备份源载荷、目录事实和恢复目标继续使用不同时间，不能形成可重算闭环，因此不准入。

## 3. 批准的最小修复

- 创建命令先按既有规则校验，再把恢复点、最后事实时间和不可变保留截止统一截断到 MySQL `DATETIME(3)` 的毫秒精度；
- 对规范化后的命令再次校验时间不变量，防止截断后产生非法边界；
- 幂等请求摘要、目录预留、备份源捕获、对象保留期、规范清单和最终返回事实全部使用同一规范化命令；
- 毫秒以下差异不再形成数据库无法持久化的不同业务身份，同幂等键仍对所有可持久化字段严格拒绝异内容；
- 新增模拟 MySQL 毫秒回读的回归：修复前稳定红于创建结果精度并可进一步复现清单摘要不匹配，修复后创建、源捕获、数据库回读和九项恢复共用同一时间轴。

## 4. 验收与证据边界

- 不修改已发布 Flyway 迁移或 `DATETIME(3)` 列；
- `jshpos-resilience` 单元、覆盖率和依赖模块完整 `verify` 必须保持原阈值通过；
- 新候选必须从头复跑治理双平台、Server、Web、Flutter 双平台、Android/Kotlin、MySQL/SQLite、安全、SBOM、许可证、证据聚合和正式运行栈；不得重跑掩盖原失败；
- 三业态、22 Owner、12 组数据守恒和 12 个固定故障 seed 必须在同一提交、同一运行窗口重新形成证据；
- `productionBackupOrKms`、Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha 和生产执行继续为 0；
- `T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`，`T2-UAT-001/REL-001` 继续 `DRAFT`，`T2-LIC-001/JSH-001` 继续 `DEFERRED`；
- `G9A-E2E-P1-001` 在完整 CI 全绿及项目发起人确认前继续 `OPEN`。
