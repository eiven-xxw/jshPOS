# CR-T2G9R3-023：G9A-R3D 全部 26 页正式联合验收

## 1. 授权与目标

- 授权日期：2026-08-25；
- 授权基线：`b0762440c13dac5197cf5b716081278b0484407f`；
- 工作分支：`t2/gate9b-sprint27g-g9a-r3d-runtime`；
- Finding：`G9A-UI-P1-001`，在项目发起人确认前继续 `OPEN`；
- 目标：只关闭准备阶段冻结的 `R3D-JNT-P1-001/002/003` 三条联合证据缺口。

## 2. 允许范围

1. 同一 Router/可信上下文挂载 `VUE-01..16`；
2. 同一平台角色和租户范围挂载 `VUE-17/19/18/20/09`；
3. 同一可信 POS 会话遍历 `FLT-04/03/06/05/02/01`；
4. 联合失败回归证明必要时，对获批页面或会话壳实施最小修复；
5. 治理、测试、证据、CI 和周门禁报告。

## 3. 禁止范围

- 新业务能力、Requirement ID、服务端 API/Controller、Owner 领域算法、依赖、数据库或迁移；
- Provider 网络、真实资金、真实设备/外设命令、伙伴现场、完整 Alpha、生产部署或商业声明；
- 把单页证据拼接成联合通过，或用 Fake 提升外部证据等级。

## 4. 串行门禁

`R3D-R0 → R3D-R1 → R3D-R2 → R3D-R3 → R3D-R4`。前项未独立验证，后项不得铺开。
若联合回归需要越过允许范围，结论必须为 `NO-GO` 并另行提交影响评审。

## 5. Go/No-Go

- Go：26 页身份及历史摘要不漂移，三条联合旅程真实挂载，开放 P0/P1 为 0，完整 CI 通过；
- No-Go：租户/门店/业务日漂移、原幂等键丢失、权限放宽、外部成功伪造、越界变更或证据缺失。

当前决策：`CONDITIONAL GO / RUNTIME AUTHORIZED`。
