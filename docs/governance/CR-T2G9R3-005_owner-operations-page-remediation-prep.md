# CR-T2G9R3-005：G9A-R3B Owner 运营页面整改准备

- 状态：APPROVED_PREP_ONLY
- 日期：2026-08-25
- 发起人：项目发起人
- 基线：`838bdacdfdd3e4113c6cf7d43cf4ad024d63ff14`
- 分支：`t2/gate9b-sprint27d-g9a-r3b-prep`
- 复用需求：`T2-ADM-002`、`T2-DMT-001`、`T2-PRM-001/002`、`T2-INV-001`、
  `T2-CST-001`、`T2-MEM-003`、`T2-UPG-001`、`T2-RPL-001`、`T2-PUR-001`、
  `T2-TRF-001`、`T2-CLS-001`、`T2-EXC-001`、`T2-LOT-001`、`T2-ONB-001`
- 缺陷：`G9A-UI-P1-001`

## 已批准范围

1. 记录 G9A-R3A 已被项目发起人接受，但整体页面缺陷继续 `OPEN`；
2. 只冻结并审计 VUE-05 至 VUE-15 的路由/父子装配、按钮、Controller 最小权限、
   数据范围、加载/空态/安全错误、单航班、原幂等键恢复、危险操作确认和直接挂载测试；
3. 为每页形成可复现失败 seed、Owner 不变量、串行修复顺序和量化验收矩阵；
4. 允许新增治理文档、机器可读契约、只读审计脚本和专用准备 CI；
5. 纠正旧页面清单中 VUE-10 只检查直接父组件导致的可达性误判，但不得借此修改
   Gate 9A 或 G9A-R3 的不可变历史证据。

## 明确非目标

- 不修改 Vue/Flutter 页面、API 客户端、服务端、Owner、依赖、数据库或迁移；
- 不新增 Requirement ID、业务能力、路由、菜单、Controller、API、任务或测试后门；
- 不解除外部 `BLOCKED`、UAT/REL `DRAFT`、LIC/JSH `DEFERRED` 状态；
- 不进入 G9A-R3B 运行时、G9A-R3C/R4、完整 Alpha、现场试点或生产发布。

## 评审门槛

- 11 个页面及完整父子装配链可重复审计，当前页面源文件摘要已冻结；
- 所有失败 seed 均能在当前基线上稳定复现，并明确预期修复证据；
- Owner 不变量、权限与原幂等键边界逐页覆盖；
- 准备分支运行时、依赖、迁移、RTM 和外部执行变化均为 0；
- 准备 CI 全绿后只形成 `PREP CONDITIONAL PASS`，等待项目发起人确认。
