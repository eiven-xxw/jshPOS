# CR-T2G8C-023：RDY001 第三轮候选租户元数据白名单滞后

## 结论

修复提交 `940532a7ac1c798c1657db27023b8dfd21d8d28e` 的 GitHub Actions Run
`32714734191` 保留为第三轮失败证据。V1—V86 与正式集成回调已经全部执行成功，287 张
业务表的主键、中文说明和数量门槛通过；随后旧的“无 tenant_id 表”冻结白名单只登记
备份控制面五表，正确拒绝了 Gate 8A 新增的七张平台全局或租户派生表。

## 处置

发布验收显式登记以下非直接租户列边界：SaaS 平台套餐/权益定义、平台授权域幂等结果、
订阅全局调度检查点，以及通过 `sub_subscription` 父聚合派生可信租户的期限事实。同时新增
外键和 `authority_scope` 元数据断言，防止白名单成为租户隔离旁路。

## 边界

只更新发布验收的冻结元数据目录和派生关系验证；不修改表、迁移、生产查询、权限、业务
事实或阈值。失败 Job `97393795481` 不重跑，下一提交必须从头执行完整工作流。
T2-RDY-001 保持 `VERIFIED` 候选，外部阻断和零执行状态不变。
