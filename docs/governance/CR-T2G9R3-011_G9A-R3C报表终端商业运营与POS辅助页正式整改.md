# CR-T2G9R3-011：G9A-R3C 报表、终端、商业运营与 POS 辅助页正式整改

## 决策

项目发起人授权从 `35cc22316faf5389844dfeeee3dfa840ce06cdf8` 建立独立分支，按
`R3C-R0 → R9` 串行修复 `VUE-16..20`、`FLT-01/02/05` 的既有
`G9A-UI-P1-001` 页面缺口。复用既有 Requirement 与 ADR-071，不分配新 Requirement ID。

## 允许变更

- Vue：报表、SaaS、服务、订阅、终端页面及其直接挂载测试；
- Flutter：换货、原单退货退款、班次现金辅助页及其 Widget 测试；
- 共享页面原操作身份管理、终端客户端可选幂等键参数；
- 对应治理契约、专用审计、CI、证据和报告。

## 不变量

- 服务端最终授权、Owner 数据主权和既有状态机不变；
- UNKNOWN/离线/超时只恢复原命令，不生成替代资金或业务命令；
- Vue 不计算报表、套餐、订阅或终端权威事实，Flutter 不拼装换货、退款或钱箱事实；
- 终端轮换的客户端参数扩展向后兼容，不新增或修改服务端端点；
- 服务端、依赖、MySQL/SQLite 迁移与 RTM 不变。

## 验收

八页必须具备十二维矩阵和直接交互证据；八组失败 seed 全部关闭；批次 P0/P1 为 0；
治理、Server、Web、Flutter 双平台、Android/Kotlin、MySQL/SQLite、安全、SBOM、许可证、
覆盖率和证据聚合必须全绿。最高结论为 `VERIFIED_CANDIDATE`，整体
`G9A-UI-P1-001` 继续 `OPEN`。

## 回退

按 R0—R9 的独立提交逆序回退页面变更。不得回写业务事实、修改已发布迁移或删除历史证据。
若远端 Flutter、Android 或安全门禁失败，保留失败 Run 并仅在本 CR 范围内修复后完整复跑。
