# CR-T2G9R4-015：G9A-R4 调拨约束前向修复批准与实施

## 1. 批准记录

- 日期：2026-08-26
- 状态：`APPROVED_FORWARD_REPAIR_IN_PROGRESS`
- 项目发起人已确认 `CR-T2G9R4-014`，批准唯一 Flyway 前向迁移
  `V202608260087__g9a_r4_transfer_outbox_version_constraint.sql`。
- Finding：`G9A-E2E-P1-001` 继续 `OPEN`，完整 CI 和项目发起人最终确认前不得关闭。

## 2. 唯一批准变化

1. 新增 V87，将 `ck_trf_outbox_version` 从 `aggregate_version > 0` 修正为
   `aggregate_version >= 0`；
2. 只更新必要的迁移策略测试、MySQL 8.4 升级测试、Release 迁移目录断言与
   G9A-R4 精确白名单；
3. V18 及其他已发布迁移保持字节不变；Transfer Java 状态机、聚合版本、API、
   事件 Schema、库存和成本事实保持不变；
4. 不新增业务能力或 Requirement ID。

## 3. 测试顺序与退出门禁

- 已先增加静态失败回归；在 V87 不存在时精确失败为
  `approved V87 forward repair: Expecting actual not to be null`；
- V87 后必须验证版本 0 与正版本可写、负版本拒绝、唯一键和既有不可变保护不退化；
- Release 测试必须先迁移到 V86 并重现版本 0 被拒绝，再单独执行 V87，验证只执行
  1 条、第二次执行 0 条并通过 Flyway validate；
- 完整 G9A-R4 CI 必须从新提交重跑，不得重跑失败 Job、降低阈值或创建绿色占位；
- `T2-PAY-002/HWD-001/PRN-001/PAR-001`、`T2-UAT-001/REL-001`、
  `T2-LIC-001/JSH-001` 及全部零执行边界保持不变。
