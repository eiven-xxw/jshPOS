# CR-T2G9R3-029：G9A-R3D 封板证据准入缺口修复

## 失败事实

封板证据回填提交 `6b4c4a040d39526a3134434396fa0bdaa202720a` 触发的 GitHub Run
`32833628390` 在 Ubuntu 与 Windows 治理作业中失败关闭。范围检查识别到新提交的
`CR-T2G9R3-028` 未进入 G9A-R3D 治理文件允许清单，其余作业依赖治理成功而被跳过。

## 根因

提交前本地检查发生在 `CR-T2G9R3-028` 仍为未跟踪文件时；基于
`git diff --name-only <baseline>` 的检查不会枚举未跟踪文件。文件提交后，远端检查正确
识别该治理证据不在允许清单并拒绝继续。

## 最小修复

- 仅将 `CR-T2G9R3-028_` 与本失败记录 `CR-T2G9R3-029_` 加入本批治理文件允许清单；
- 不修改业务代码、API、Controller、依赖、数据库、迁移、测试或阈值；
- 保留失败 Run，不重跑失败 Job，新提交从头执行完整 CI；
- `G9A-UI-P1-001`、外部状态和零执行边界保持不变。
