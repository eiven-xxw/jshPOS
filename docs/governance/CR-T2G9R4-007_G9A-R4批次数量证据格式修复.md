# CR-T2G9R4-007：G9A-R4 批次数量证据格式修复

## 1. 触发证据

- 候选提交：`9e022302551bfd334db74f077b62b7cc227202f2`
- GitHub Actions Run：`32888864903`
- 失败 Job：`flutter (ubuntu-latest)`（`97935810998`）
- 结果：Dart 3.13.0 格式门禁报告 `g9a_r4_formal_jar_e2e_test.dart` 会变化并失败关闭。

## 2. 根因与修复

本地首次使用 `dart format --output=none --set-exit-if-changed`；该模式只检查并报告差异，不写回文件。现在使用同版本 Dart 的写回模式机械格式化该文件，再执行 CI 同款检查命令，`lib test` 共 134 个文件为零差异。

本变更不修改运行时、测试语义、阈值、业务事实、API 或迁移。原失败 Run 保留，新提交必须从头执行完整 CI；`G9A-E2E-P1-001` 继续保持 `OPEN`。
