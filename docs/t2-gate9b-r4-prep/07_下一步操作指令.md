# G9A-R4 正式整改下一步操作指令（待项目发起人确认）

```text
我确认《G9A-R4 启动评审报告》，接受准备阶段 CONDITIONAL PASS。

按 CONDITIONAL GO 启动 G9A-R4 正式运行栈跨 Owner 内部 E2E 完整性整改。

以 G9A-R4 准备阶段最终封存提交
[填写 G9A-R4 Prep 最终封存提交]
作为起点创建独立分支：
t2/gate9b-sprint27i-g9a-r4-runtime

本阶段只允许关闭 G9A-E2E-P1-001，复用 T2-E2E-004、T2-E2E-005、
T2-INT-001，不新增业务能力或 Requirement ID。

严格按 R4-R0 → R4-R1 → R4-R2 → R4-R3 → R4-R4 → R4-R5 串行。
先补四个可复现失败测试，再建立同一提交、同一运行窗口的正式 MySQL、Redis、
商业 JAR、HTTP/OpenAPI、Vue dist、Flutter POS 与文件 SQLite。Flutter 必须连接
该正式 JAR，不得使用测试内置 HTTP Server 冒充正式服务端。

经正式 API、应用端口、事件与 Inbox/Outbox 串联商户开户/订阅/服务和便利店、
零食折扣店、社区超市三条旅程，覆盖全部 22 Owner；禁止直接写业务数据库、跨 Owner
Mapper、Mock/InMemory Owner、前端重算或伪造外部成功。

执行冻结的固定故障 seed 与 12 组数据守恒。UNKNOWN 只能观察原命令；重复、ACK 丢失、
重启和部分 Owner 失败必须复用原身份收敛。每批只允许修复失败回归证明的既有缺陷；
如需要新业务、改变资金/库存/租户语义或修改已发布迁移，停止并提交独立 CR。

T2-PAY-002/HWD-001/PRN-001/PAR-001 继续 BLOCKED；T2-UAT-001/REL-001
继续 DRAFT；T2-LIC-001/JSH-001 继续 DEFERRED。不得进行 Provider 网络、真实资金、
真实设备/外设、伙伴现场、完整 Alpha 或生产发布。

完成后提交《G9A-R4 正式运行栈跨Owner内部E2E独立周门禁报告》等待我确认。
未经确认不得将 G9A-E2E-P1-001 更新为 CLOSED_IN_GATE9B，不得自动进入完整 Alpha
或生产发布。
```
