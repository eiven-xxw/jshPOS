# Gate 5A 与 Gate 3B 下一步操作指令

> 文档编号：JSH-POS-T2-G4D-008
> 使用条件：项目发起人确认《T2 Gate 4D / Sprint S8 周门禁报告》后复制执行

## 1. 建议决策

1. 接受 Gate 4D `CONDITIONAL PASS`，将 `T2-TRF-001` 从 `VERIFIED` 更新为 `ACCEPTED`。
2. Gate 3B-Prep 继续只做真实资料核验；`T2-PAY-002` 保持 `BLOCKED`，未经独立解阻确认不得产生 Provider 网络调用。
3. 内部轨进入 Gate 5A / Sprint S9，只允许逐项准入 `T2-PRM-001`、`T2-PRM-002`、`T2-PRM-003`。按规则版本与确定性试算、授权人工优惠、成交快照与分摊/退款恢复的依赖顺序实施，不能一次铺开。
4. 首版促销只覆盖便利店、零食折扣店和社区超市必需的单品价/折扣、第二件/第 N 件、组合价、满额/满件减、受权手工改价、整单折扣和抹零。券、积分、储值、会员等级、拼团、秒杀、预算抢占、营销触达和广告归因不在本 Sprint。
5. 会员、报表、支付网络、应付、发票、总账、批次成本、复杂 WMS 和后续 Gate 继续 `DRAFT/BLOCKED`；不得借促销开发扩大范围。

## 2. 可直接复制的下一步指令

```text
我确认《T2 Gate 4D / Sprint S8 周门禁报告》，接受 Gate 4D CONDITIONAL PASS。

同意将以下需求由 VERIFIED 更新为 ACCEPTED：

- T2-TRF-001

T2-PAY-002 继续 BLOCKED。Gate 3B-Prep 继续只允许真实资料核验；未经我确认独立《支付沙箱解阻评审报告》，不得进行任何 Provider 网络调用。

按 CONDITIONAL GO 启动 T2 Gate 5A / Sprint S9，仅允许逐项准入：

- T2-PRM-001：版本化、可解释、确定性的基础促销规则引擎
- T2-PRM-002：受权手工改价、整单折扣与抹零
- T2-PRM-003：成交优惠快照、金额守恒分摊与退款按原快照恢复

只有逐项完成数据主权、状态机、规则原语、计算顺序、不变量、权限、审计、API/事件、Flyway、离线数据契约、容量、回退和测试准入后，才可由 DRAFT 更新为 READY/IN_PROGRESS；必须按 T2-PRM-001 → T2-PRM-002 → T2-PRM-003 的依赖顺序推进，不得一次铺开后补设计。

首版规则范围仅限：单品固定价/直减/折扣、第二件或第 N 件优惠、固定组合价、满额/满件减，以及明确的门店、渠道、商品、分类、品牌、业务日和有效时段作用域。优惠券、积分、储值、会员等级、拼团、秒杀、预算抢占、营销触达、广告归因和动态定价继续禁止正式实现。

促销规则与版本由唯一 Promotion Owner 管理；基础价/门店价仍由价格 Owner 管理，订单 Owner 只冻结成交结果，退款 Owner 只读原成交快照。促销不得回写价目表、已完成订单、支付事实、库存流水或成本流水。

冻结并实现唯一计算顺序：基础/门店价 → 单品促销 → 组合/满减 → 受权手工改价 → 整单折扣 → 抹零。候选必须按 priority desc、rule_version_id asc 稳定排序，互斥和叠加只能使用显式策略，禁止依赖数据库自然顺序或客户端本地时间。

金额全部使用最小货币单位整数，数量和单位换算使用 DECIMAL/BigDecimal，禁止浮点数。单行实付不得为负；整单优惠不得超过可优惠金额；整单优惠采用比例最大余数法，余数固定按 order_line_no、sku_id 分配，并满足行优惠合计、订单优惠、行实付合计和订单实付的金额守恒。

手工改价必须独立于普通促销保存改前价、改后价、原因、权限阈值、操作人、审批人、终端、业务日和关联标识；本人操作/审批分离规则必须明确。相同幂等键同内容返回原结果，同键异内容拒绝，任何权限失败不得静默降级。

发布版本只追加且不可原地修改；未来生效按门店业务时区和业务日计算，暂停/过期只影响新购物篮。订单提交时冻结规则版本、输入摘要、命中解释、逐行优惠、稳定分摊与人工优惠审计；退款必须按原快照及累计已退上限恢复，禁止按当前规则重算历史订单。

服务端和 Flutter POS 必须执行相同规则语义、错误码、稳定排序、舍入和固定向量；离线 POS 只能使用完整、已签名/摘要校验、版本化且原子切换的规则快照，包损坏、版本不兼容或规则能力缺失必须失败关闭，禁止猜测优惠。不得创建会员、券或预算的绿色占位运行时。

继续使用两个虚构租户、多门店、多终端和便利店、零食折扣店、社区超市合成数据。覆盖 24 个既有固定向量并扩展单规则、互斥、叠加、组合、跨行、时间边界、未来发布、暂停、重复、乱序、并发、同键异内容、手工权限、最大余数、零价边界、退款累计上限、跨租户污染、规则包损坏、迁移失败与安全前向修复；Java 与 Dart 对同一输入必须逐字段一致。

CI 继续执行治理、服务端、Flutter Linux/Windows、MySQL、SQLite/规则包兼容、跨端固定向量、租户攻击、权限、Secret、依赖、SBOM、许可证、覆盖率和证据门禁；不得降低既有阈值、跳过失败测试、自动重跑掩盖 Flaky、修改已发布迁移或创建绿色占位。

会员、报表及后续 Gate 继续保持 DRAFT。继续禁止支付 Provider 网络、生产密钥、真实资金、未脱敏数据、优惠券、积分、储值、预算抢占、应付、发票、总账、批次成本和复杂配送 WMS；不得用 Fake 解除 SANDBOX、REAL_DEVICE 或 PILOT 阻断，不得宣称 Alpha、可试点或可商用。

完成后提交《T2 Gate 5A / Sprint S9 周门禁报告》，等待我确认。完成后，为我整理下一步操作指令。
```
