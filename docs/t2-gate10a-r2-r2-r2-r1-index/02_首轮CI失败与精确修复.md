# 首轮 CI 失败与精确修复

- 候选：`d4d59d2c11d9a29818597ff8b00415aff9f6b371`
- Run：`32982737623`
- 失败 Job：`mysql84 / 98223274934`

MySQL 8.4.11 已完成空库到 V88、V87 到 V88、Flyway validate、重复执行零迁移；10k/100k
执行计划均选择 `idx_rpt_sales_keyset` 且 `fullScanObserved=false`。失败来自测试使用
`explainJson.contains("filesort")`，把 MySQL JSON 中明确的 `using_filesort:false` 误判为 true。

修复仅让测试递归解析 JSON，并且只有字段名包含 `filesort` 且布尔值明确为 `true` 时才判定失败。
V88、V1—V87、生产 SQL/Mapper、API、事件及业务语义均未改变；不重跑失败 Job，新提交从头复跑。
