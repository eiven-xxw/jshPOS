# 准入范围与先红后绿记录

## 准入

- 起点：`53bcf39d868092142e6464b8c9db730b0074dd2a`
- 分支：`t2/gate10a-r2-r2-r2-r1-index-runtime`
- CR：`CR-T2G10A-018 = CONDITIONAL GO`
- Finding：`G10A-SQL-P2-001 = OPEN`
- 资源 Finding：`G10A-RES-P2-001 = PREPARED`

唯一运行时变化是新增
`V202608260088__reporting_sales_keyset_index.sql`，为 `rpt_sales_daily` 增加获批七列
`idx_rpt_sales_keyset`。V1—V87、SQL/Mapper、API、事件和业务语义均冻结。

## 失败红基线

先增加 `ReportingSalesKeysetIndexMigrationPolicyTest`，在 V88 尚不存在时执行，精确失败：

```text
[approved V88 RPT-SALES keyset index migration]
Expecting actual not to be null
```

随后加入 V88，同一测试转绿。MySQL 专项仍会在临时 V87 数据库保存
`fullScanObserved=true`、`filesortObserved=true` 的执行计划，再只执行 V88 并验证 10k/100k
计划转绿，避免测试在没有重现原问题时伪造通过。

## 回退

应用 SQL 与 v1/v2 契约未变化，即使未建立新索引也保持功能正确；V88 失败时停止发布并保留 V87。
已成功建立的索引不自动下行删除，任何删除必须另行 CR，以免恢复窗口重新引入全扫。
