# 下一步操作指令

建议项目发起人复制确认：

> 我确认《G10A-SQL-P2-001 RPT-SALES 索引独立周门禁报告》，接受
> Gate 10A-R2-R2-R2-R1-INDEX `CONDITIONAL PASS`。
>
> 同意确认 CR-T2G10A-018 授权的 V88 索引子批已完成内部验证。明确该接受只覆盖
> MySQL 8.4.11、固定虚构数据和内部 CI 下的 V88 前向迁移、RPT-SALES 执行计划、
> 查询数、数据完整性与租户边界；不代表生产容量、完整 Alpha、生产、商业验收或商业 SLA。
>
> `G10A-SQL-P2-001` 继续 `OPEN`，`G10A-RES-P2-001` 继续 `PREPARED`。
>
> 按 `CONDITIONAL GO` 启动 Gate 10A-R2-R2-R2-R2：RPT-INVENTORY 精确整改准备。
> 仅允许复用已接受的 CR-T2G10A-014 兼容性方向，冻结 RPT-INVENTORY 当前契约、
> Owner 批量读取边界、版本化 keyset 分页、受控流式导出、查询数、10k/100k MySQL
> 执行计划、权限/租户边界、故障 seed 和回退方案；先形成失败测试、影响分析、
> 候选 SQL/索引对比及启动评审报告。
>
> 准备阶段不得修改生产 SQL、Mapper/XML、索引、数据库对象、依赖、API、事件或已发布迁移，
> 不得改变库存、成本、租户或同步语义。如需索引，必须另行提交独立索引 CR 和唯一前向迁移，
> 未经确认不得实施。
>
> RPT-PAY-REC、RES、R3、完整 Alpha、生产发布及所有外部执行继续禁止。完成后提交
> 《RPT-INVENTORY 精确整改启动评审报告》等待我确认，不得自动进入运行时整改。
