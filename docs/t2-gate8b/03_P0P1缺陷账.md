# P0/P1 缺陷账

| ID | 级别 | 发现 | 根因 | 修复 | 回归 | 状态 |
|---|---|---|---|---|---|---|
| G8B-P0-001 | P0 | 正式 API 无法创建第二名合规 SaaS 审批人 | 平台管理员只认 `user_id=1` 内置超管，而内置超管不可复制；职责分离导致流程死锁 | 默认平台租户增加具名 `platform_admin` 角色识别；商户租户同名角色失败关闭 | `RuoYiPlatformPrivilegeSourceTest` 与正式运行时独立审批旅程 | FIXED_VERIFIED |
| G8B-P1-002 | P1 | [候选 Run 32664464732](https://github.com/eiven-xxw/jshPOS/actions/runs/32664464732) 的正式 API 旅程在登录后首个受保护请求失败 | 旅程客户端仅在登录正文传入 `clientId`，未像正式 Vue 客户端一样在后续请求携带 `clientid` Header，触发令牌客户端身份校验失败 | 所有正式 API 请求固定携带与令牌一致的 `clientid`；治理门禁静态核验该协议 | 正式 MySQL/Redis API 旅程全量重跑，不允许单 Job 重跑掩盖失败 | FIXED_VERIFIED |
| G8B-P1-003 | P1 | [候选 Run 32665243610](https://github.com/eiven-xxw/jshPOS/actions/runs/32665243610) 在创建套餐权益版本时失败 | 旅程客户端使用 ISO `T` 分隔符，而正式服务端全局 `LocalDateTime` 契约为 `yyyy-MM-dd HH:mm:ss`，请求被正确拒绝 | 统一用正式日期时间契约发送 UTC 无时区值；治理门禁固定校验格式 | 正式 MySQL/Redis API 旅程全量重跑，不允许单 Job 重跑掩盖失败 | FIXED_VERIFIED |
| G8B-P1-004 | P1 | [候选 Run 32665726977](https://github.com/eiven-xxw/jshPOS/actions/runs/32665726977) 在创建商户申请时失败 | 合成申请编码包含连字符，不符合正式 SaaS 编码只允许大写字母、数字与下划线的领域契约 | 合成申请编码改为 `GATE8B_APP_001`；治理门禁固定校验编码向量 | 正式 MySQL/Redis API 旅程全量重跑，不允许单 Job 重跑掩盖失败 | FIXED_VERIFIED |
| G8B-P0-002 | P0 | [候选 Run 32666499899](https://github.com/eiven-xxw/jshPOS/actions/runs/32666499899) 在正式技术租户创建时失败 | 空环境只装配 RuoYi 主 Schema；租户创建会同步工作流定义，但 `flow_category` 等官方工作流基础表未装配 | 正式运行环境按权威顺序导入 `ry_vue_5.X.sql` 与 `ry_workflow.sql`，保留正式租户创建和工作流同步，不通过关闭功能绕过 | 空 MySQL/Redis 环境经正式 API 完成技术租户创建及后续全旅程；治理门禁固定校验基础 Schema 顺序 | FIXED_VERIFIED |
| G8B-P1-005 | P1 | Run 32666499899 的正式运行日志记录了合成开户 `bootstrapPassword` 请求字段 | RuoYi 通用日志排除字段仅覆盖普通密码命名，未覆盖 SaaS 开户凭据字段 | 将 `bootstrapPassword` 纳入通用请求/操作日志排除契约，新增单测并在正式运行 Job 对三类受控凭据执行零命中门禁 | `SensitiveRequestLoggingContractTest`、正式旅程日志凭据零命中及完整安全门禁 | FIXED_VERIFIED |
| G8B-P0-003 | P0 | [候选 Run 32667029044](https://github.com/eiven-xxw/jshPOS/actions/runs/32667029044) 在导入工作流 Schema 时失败 | 基础初始化为 `sys_menu` 前向增加 `route_name`，工作流种子仍采用无列名的 20 列位置插入，空库装配产生 20/21 列不匹配 | 工作流菜单种子全部改为显式列清单并由默认值承载 `route_name`；不关闭工作流、不绕过正式租户创建 | MySQL 8.4 空库顺序导入基础/工作流 Schema，随后完成正式技术租户创建及全 API 旅程 | FIXED_VERIFIED |
| G8B-P1-006 | P1 | Run 32667029044 在 Schema 导入阶段退出后，运行时 Artifact 因目录内无文件而上传失败 | 证据目录虽已创建，但首个持久文件在服务端启动后才生成 | 初始化开始即创建 `schema-bootstrap.log`，Schema 错误输出纳入 `always()` 制品 | 任一 Schema 初始化失败均能留下非空运行时 Artifact；成功路径同时保留初始化和应用日志 | FIXED_VERIFIED |
| G8B-P0-004 | P0 | [候选 Run 32667634100](https://github.com/eiven-xxw/jshPOS/actions/runs/32667634100) 在正式创建订阅时拒绝平台超管的 `subscription:create` | RuoYi 超管仅持有三段通配符 `*:*:*`，Sa-Token 不会用它匹配 Owner 的两段权限 `subscription:create` | 平台权限装配同时授予三段 `*:*:*` 与两段 `*:*` 通配符；不修改 Subscription Owner 权限、不绕过 Controller 授权 | Sa-Token 原生通配符契约测试及正式平台超管经 HTTP 创建订阅的完整旅程 | FIXED_VERIFIED |
| G8B-P0-005 | P0 | [候选 Run 32668382679](https://github.com/eiven-xxw/jshPOS/actions/runs/32668382679) 在正式创建门店时因审计摘要失败并回滚 | `AuditSanitizer` 自建 Jackson 实例，未继承应用已注册的 Java Time 模块，无法规范化门店 `LocalTime` 业务日起点 | 注入应用 `ObjectMapper`，复制后保留确定性排序；审计仍与门店事务一起失败关闭，不跳过审计 | `LocalTime` 审计单测、Foundation 全回归及正式 API 门店创建与后续 Service 旅程 | FIXED_VERIFIED |
| G8B-P1-007 | P1 | [候选 Run 32668906566](https://github.com/eiven-xxw/jshPOS/actions/runs/32668906566) 在新租户创建独立服务工单复核账号前失败 | 旅程夹具假定租户管理员角色键为 `tenant_admin`，与 RuoYi 正式 `TenantConstants.TENANT_ADMIN_ROLE_KEY=admin` 契约不一致 | 以具名常量对齐正式角色键，并继续经受保护的角色查询 API 解析角色 ID；不硬编码 ID、不修改生产授权规则 | 治理契约断言及正式新租户角色查询、复核账号创建和后续 Service 全旅程 | FIXED_VERIFIED |
| G8B-P0-006 | P0 | [候选 Run 32669490904](https://github.com/eiven-xxw/jshPOS/actions/runs/32669490904) 已完成 SAA/SUB 与租户初始化，但正式 JAR 对 `/api/v1/service/catalogs` 返回地址不存在 | `jshpos-service` 虽已进入 Maven reactor、启动包依赖和自动配置清单，但配置类仍是普通 `@Configuration` 且未扫描 Owner 组件，导致 Controller/应用服务未注册 | 将模块入口改为 Spring Boot `@AutoConfiguration`，显式扫描 Service Owner 包并保留独立 MapperScan；不新增测试端点或绕过正式 API | 自动配置注解契约测试、治理门禁及正式 JAR 经 HTTP 完成 Service 目录/项目/工单/附件旅程 | FIXED_VERIFIED |

当前未引入新业务能力、数据库迁移或依赖。上述 6 个 P0 与 6 个 P1 已由候选提交 `379197394a4c0934dac8a6d4ff1e10e87bdadde3` 的完整 Run `32670082176` 验证关闭；所有失败 Run 继续保留。若后续发现新 P0/P1，仍须登记、先补回归测试并完整重跑，不得删除失败证据。
