# P0/P1 缺陷账

| ID | 级别 | 发现 | 根因 | 修复 | 回归 | 状态 |
|---|---|---|---|---|---|---|
| G8B-P0-001 | P0 | 正式 API 无法创建第二名合规 SaaS 审批人 | 平台管理员只认 `user_id=1` 内置超管，而内置超管不可复制；职责分离导致流程死锁 | 默认平台租户增加具名 `platform_admin` 角色识别；商户租户同名角色失败关闭 | `RuoYiPlatformPrivilegeSourceTest` 与正式运行时独立审批旅程 | FIXED_PENDING_FULL_CI |
| G8B-P1-002 | P1 | [候选 Run 32664464732](https://github.com/eiven-xxw/jshPOS/actions/runs/32664464732) 的正式 API 旅程在登录后首个受保护请求失败 | 旅程客户端仅在登录正文传入 `clientId`，未像正式 Vue 客户端一样在后续请求携带 `clientid` Header，触发令牌客户端身份校验失败 | 所有正式 API 请求固定携带与令牌一致的 `clientid`；治理门禁静态核验该协议 | 正式 MySQL/Redis API 旅程全量重跑，不允许单 Job 重跑掩盖失败 | FIXED_PENDING_FULL_CI |
| G8B-P1-003 | P1 | [候选 Run 32665243610](https://github.com/eiven-xxw/jshPOS/actions/runs/32665243610) 在创建套餐权益版本时失败 | 旅程客户端使用 ISO `T` 分隔符，而正式服务端全局 `LocalDateTime` 契约为 `yyyy-MM-dd HH:mm:ss`，请求被正确拒绝 | 统一用正式日期时间契约发送 UTC 无时区值；治理门禁固定校验格式 | 正式 MySQL/Redis API 旅程全量重跑，不允许单 Job 重跑掩盖失败 | FIXED_PENDING_FULL_CI |

当前未引入新业务能力、数据库迁移或依赖。完整 CI 通过前缺陷总账保持候选状态；若发现新 P0/P1，必须登记、先补回归测试并重新完整运行，不得删除失败证据。
