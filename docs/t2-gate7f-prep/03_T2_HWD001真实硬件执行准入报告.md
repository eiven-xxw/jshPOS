# T2-HWD-001 真实硬件执行准入报告

## 1. 评审结论

`NOT_ACHIEVED / BLOCKED / NO-GO`。商米 `T3 PRO MAX / L15B1` 仅作为主认证文档候选，
iMin `D4 Pro` 仅作为兼容文档候选；精确 SKU、Android/固件、CPU/内存、SDK 授权、样机
资产和联系人均未形成受控证据。真实设备命令为 0。

## 2. 每机型完整资料包

每台候选必须独立提供：厂商和精确 SKU、Android 版本、固件 Build、CPU/ABI、内存与存储、
安全补丁级别、设备能力、SDK 名称/版本/许可证/SHA-256、获取和更新方式、样机资产 ID、
APK 安装/升级/静默安装/重启/回退授权、保修/RMA/EOL、技术联系人，以及来源、授权签署、
适用范围、保管、到期、删除和吊销规则。

| 角色 | 候选 | 完整包 | 当前结论 |
|---|---|---:|---|
| 主认证 | 商米 T3 PRO MAX / L15B1 | 0/1 | MISSING_CONTROLLED_BUNDLE |
| 兼容 | iMin D4 Pro | 0/1 | MISSING_CONTROLLED_BUNDLE |

型号系列名称不能替代精确 SKU；公开规格页不能替代 SDK 授权、固件和样机资产证据。

## 3. 获批执行矩阵

执行准入后至少覆盖冷启动、登录、可信终端、数据包、扫码输入、100/1000 单持续交易、
SQLite 故障、断网同步、待同步/UNKNOWN 下升级阻止、APK 签名/摘要、安装、灰度、迁移失败、
重启、回退/前向修复、8/24 小时长稳、温度/内存/存储和诊断日志脱敏。

## 4. 资产、安全和下一决定

样机必须绑定资产、场地、操作者、时间窗、恢复点和停止开关；序列号、设备证书、厂商账号
和 SDK 原包进入受控系统。任何疑似生产设备、未授权命令或摘要漂移立即停止。两个候选完整
资料达到 `VERIFIED_DOCUMENT` 后，仅提交独立执行准入评审，未经确认不得安装或下发命令。
