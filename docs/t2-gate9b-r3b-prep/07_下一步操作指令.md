# G9A-R3B 正式页面整改操作指令

```text
我确认《G9A-R3B 启动评审报告》，接受准备阶段 CONDITIONAL PASS。

按 CONDITIONAL GO 启动 G9A-R3B Owner 运营页面与高风险写操作正式整改。

以 G9A-R3B 准备阶段最终封存提交
[填写 G9A-R3B Prep 最终封存提交]
作为起点创建独立分支：
t2/gate9b-sprint27d-g9a-r3b-runtime

本批只允许关闭 VUE-05 至 VUE-15 对应的既有 G9A-UI-P1-001 缺口，
复用已接受 Requirement 和 ADR-071，不新增业务能力或 Requirement ID。

严格按 R3B-R0 → R1 → R2 → R3 → R4 → R5 → R6 → R7 → R8 →
R9 → R10 → R11 串行。先补可复现失败测试，再完成最小页面修复；前一步
未独立 VERIFIED，后一步不得铺开。

重点关闭：VUE-08 四个最小权限缺口；VUE-08/13/14 原幂等键恢复；
11 页加载、空态、安全错误、单航班、危险操作确认和挂载交互测试。
VUE-10 保留现有 Advanced → Supply → Replenishment 两级装配链，不新增重复路由。

页面只能调用既有正式 API；服务端可信 tenant 和门店范围继续最终授权。
禁止修改 Owner 算法、Controller/API、资金库存成本事实、依赖和已发布迁移，
禁止跨 Owner Mapper、前端重算、Provider SDK/HTTP 或外部执行。

所有外部 BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED 和零执行边界保持不变。
完成后提交《G9A-R3B Owner运营页面整改独立周门禁报告》等待我确认，
不得自动进入 G9A-R3C、G9A-R4、完整 Alpha 或生产发布。
```
