# T2-MTN-001 运行时影响分析与设计准入

## 准入结论

`CONDITIONAL GO / IN_PROGRESS`。前置 `T2-SEC-002` 已由项目发起人接受；五项发现、Owner、兼容、回退与验收条件已冻结。

## 范围与非目标

唯一范围为 `G8C-MTN-P1-001..005`。不新增业务能力、外部集成、Controller、页面旅程、表或迁移，不改变资金、库存、租户、订阅或其他业务事实。

## Owner 与事务影响

| 发现 | Owner/边界 | 运行时影响 | 保持不变量 |
|---|---|---|---|
| P1-001 | Service 内层端口、SaaS 适配器 | 依赖方向收敛 | 权益拒绝码与服务流程不变 |
| P1-002 | 契约治理 | 历史草案不再冒充当前契约 | 当前 API 路径、请求响应不变 |
| P1-003 | CI/迁移治理 | 合法 callback 正确分类 | 历史迁移与非法命名仍阻断 |
| P1-004 | Foundation 持久化适配器 | SQL 从注解迁入 XML | 行锁、可信租户与返回实体不变 |
| P1-005 | Flutter Checkout/Presentation | 源文件按职责拆分 | SQLite 单事务、幂等、错误码和 UI 行为不变 |

## 权限、安全与数据

- `tenant_id` 仍只来自可信上下文；不得通过端口化或 SQL 迁移接受客户端租户。
- 不处理真实 PII、Secret、支付数据或设备凭据。
- 不修改已发布迁移；无需数据库回退，代码可按发现独立回退。
- 不新增依赖；SBOM 和许可证内容只允许因现有构建重生成而变化。

## 量化准入

- 五项发现各有机器可执行的静态或行为回归；开放 P0/P1 为零。
- OpenAPI 当前集合 `(method,path)` 与 `operationId` 冲突为零。
- Foundation 目标 Mapper 的注解 SQL 和 `SELECT *` 为零。
- 两个 Flutter 目标主文件均完成职责拆分，且正式 API、SQLite Schema 与固定向量摘要不变。
- 治理、Server、Web、Flutter 双平台、Android/Kotlin、MySQL、SQLite、安全、依赖、SBOM、许可证、覆盖率和既有 Gate 全量回归全部通过。

## 证据边界

最高只可形成 `INTERNAL_MAINTAINABILITY_HARDENING_CANDIDATE`；不得解除任何 SANDBOX、REAL_DEVICE、REAL_PERIPHERAL、PILOT、UAT、生产或商业阻断。
