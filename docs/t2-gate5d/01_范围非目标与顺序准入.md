# T2 Gate 5D / Sprint S12 范围、非目标与顺序准入

## 1. 基线与需求状态

- 唯一不可变技术基线：annotated tag `t2-prep-baseline-2026-08-16`。
- Gate 5D 分支起点：Gate 5C 封板提交 `12c916c7a4b956a0bdca09ebc3ee6b4e19f9cf63`。
- `T2-MEM-001/002=ACCEPTED`；`T2-PAY-002=BLOCKED`。
- 严格顺序：`T2-RPT-001 → 独立 VERIFIED → T2-RPT-002`。禁止一次铺开或预建绿色占位。

## 2. T2-RPT-001 准入范围

1. 销售与收银日投影：订单数、取消/退货、gross、discount、surcharge、receivable、退款、现金实收/退回、班次差异和促销快照计数。
2. 库存与成本日投影：在手/可用/预占数量变化、库存流水数量、采购/盘点/调拨数量影响、库存价值变化、销售成本和采购/盘点/调拨成本影响。
3. 来源事件 Inbox、Owner+分区检查点、重复/异摘要/缺口/乱序/晚到处理、全量重建和增量追平。
4. 按可信租户、组织/门店数据范围查询；按日期、门店、终端、收银员、仓库、SKU 聚合。
5. CSV 安全导出状态机：申请、独立审批、生成、短期单次下载、过期清理、失败与审计。
6. 差异记录与受审计修复任务；只记录差异，不回写任何业务 Owner。

## 3. T2-RPT-002 后续准入范围

仅在 RPT-001 独立验证后允许：Provider 无关支付/退款事实、内部合成账单导入、逐日对账投影、差异分类、处理人和审计链。真实 Provider 账单下载、签名、网络、终端、密钥和资金不准入。

## 4. 明确非目标

- BI 自助分析、任意 SQL、跨租户经营大盘、实时流式大数据平台、AI 经营建议。
- 税务申报、总账、发票、应付、复杂数据仓库、渠道生产账单和财务结算。
- 报表直接读取或更新其他 Owner 私有表；以报表、缓存、导出文件代替业务事实。
- 导出会员身份密文、HMAC/令牌摘要、支付敏感字段或不必要 PII。

## 5. Owner 与持久化策略

| 对象 | Owner | 策略 | 理由 |
|---|---|---|---|
| 来源事件 Inbox | Reporting | `XML_ONLY` | 幂等事实、异摘要冲突、只追加与重放 |
| 消费检查点/缺口 | Reporting | `XML_ONLY` | 显式锁、单调条件与修复状态 |
| 销售收银日投影 | Reporting | `READ_PROJECTION` | 可丢弃、可重建，只允许受控 upsert/query |
| 库存成本日投影 | Reporting | `READ_PROJECTION` | 可丢弃、可重建，精确十进制汇总 |
| 差异与修复任务 | Reporting | `XML_ONLY` | 审计状态机，禁止通用删除 |
| 导出请求/制品元数据 | Reporting | `XML_ONLY` | 审批、状态条件、过期与单次下载 |

## 6. RPT-001 独立验证与 RPT-002 准入结论

`T2-RPT-001` 候选 `5884b9503cd1a09fcb70f4e7627457d4a83702e6` 已在 GitHub Actions Run `32034358131` 的十个 Job 全绿，MySQL 8.4 实迁移、百万行容量、服务端/Web/Flutter 双平台、安全、SBOM、许可证和证据 Artifact `9290294945` 均通过，形成独立 `VERIFIED` 证据。

据此按批准顺序准入 `T2-RPT-002`：仅实现 Provider 无关支付/退款事实、内部合成账单、可重建匹配投影、差异处理人与只追加审计链。外部 Provider 账单下载、SDK/HTTP、回调、终端、密钥、真实资金及 SANDBOX 证据仍由 `T2-PAY-002=BLOCKED` 明确阻断。

RPT-002 的数据主权、状态与匹配优先级、权限审计、API/事件、V34/V35 前向迁移、容量、重建、兼容、回退和固定合成向量已由本目录 02—05 与契约目录冻结，可由 `DRAFT` 经 `READY` 进入 `IN_PROGRESS`；不得据内部合成账单宣称渠道对账通过。
