# 范围与当前契约冻结

## 决策边界

- 起点：`206b07f93014a468102380577a987ac85af1ceff`；
- 分支：`t2/gate10a-r2-r2-r2-r2-rpt-inventory-prep`；
- 复用变更：`CR-T2G10A-014`；
- 当前阶段：`PREP_ONLY`，运行时未准入；
- `G10A-SQL-P2-001=OPEN`，`G10A-RES-P2-001=PREPARED`。

允许冻结契约、添加测试范围红基线、采集 MySQL 8.4.11 的 10k/100k 执行计划，完成
Owner 端口、keyset、流式导出、候选 SQL/索引、影响和回退设计。生产 Java、Mapper/XML、
SQL、API、事件、索引、数据库对象、依赖和迁移变化必须为 0。

## 当前正式契约

| 层次 | 冻结值 |
|---|---|
| Requirement | `T2-RPT-001` |
| HTTP | `GET /api/v1/reports/inventory-cost-daily` |
| operationId | `queryInventoryCostDailyReport` |
| 权限 | `report:operation:read`，可信租户及门店范围最终授权 |
| 响应 | `R<List<InventoryCostDailyView>>`，当前无页界 |
| 应用服务 | `ReportQueryService.inventoryCost` |
| 持久化端口 | `ReportingPersistencePort.queryInventoryCost` |
| Mapper | `ReportingPersistenceMapper.queryInventoryCost` |
| 稳定排序 | `business_date,store_id,warehouse_id,sku_id,currency` |
| 当前导出 | 每门店一次查询后内存物化 |

当前 SQL 摘要为
`316e28301946e57bd138724272875e176f3255a4f14fbb3a7c0a9ab58dcc4595`。该摘要只绑定
标准化 `queryInventoryCost` statement，不绑定整个 XML 文件。

## Owner 与语义停止线

Reporting Owner 只读已接受的 Inventory/Costing 权威事实并维护可重建投影。本次后续即使获准
运行时整改，也不得改变在手、可用、预占、成本、COGS、业务日、舍入或投影版本语义；不得跨
Owner Mapper、反写权威事实或让前端重算。任何此类变化必须停止并另行提交 CR/Requirement。
