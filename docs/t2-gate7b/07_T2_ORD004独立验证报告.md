# T2-ORD-004 独立验证报告

## 结论

`VERIFIED`。候选提交
`c81595821f01651e0f07f87b8678aad5b9c9797b` 的 GitHub Actions
[Run 32499856217](https://github.com/eiven-xxw/jshPOS/actions/runs/32499856217)
七类门禁首次全部通过。该结论只代表交易取消、挂单作废和成交后受控反向
处置路由的内部软件执行证据，不代表真实支付、真实设备、完整 Alpha、试点、
生产或商业验收。

## 已验证行为

- 当前购物篮可受权取消；已持久化且未完成的订单可按原幂等键作废；已成交订单禁止回退状态，只能追加 `RETURN_REFUND_REQUIRED` 或具名补偿路由。
- POS 在同一 SQLite 事务中提交订单有效状态、处置事实、审计、幂等结果和 Outbox；磁盘失败或事务中断整体回滚，重启后复用原命令。
- SQLite V11 的 `local_order_disposition` 与 MySQL V54 的 `ord_order_disposition` 均为只追加事实；更新或删除由数据库触发器失败关闭。
- `ord_order_finality_guard` 以租户和订单唯一键仲裁取消与成交的并发竞争；先到取消阻止后到成交，先到成交阻止后到取消。
- 服务端从可信上下文取得租户、门店和员工，校验冻结订单摘要、状态、门店、终端、业务日、原因、授权引用及请求摘要；同键异内容失败关闭。
- Sync Owner 通过明确应用端口分派 `order.cancelled.v1` 与 `order.reversal-routed.v1`；重复、乱序、ACK 丢失及客户端未知结果不得生成第二条业务命令。
- 现金成交、促销成交、退款、库存、成本、支付和审计等其他 Owner 的既有事实不被处置模块跨表覆盖；资金 `UNKNOWN` 只能查询、观察或对账收敛。
- Flutter 正式页面覆盖当前篮取消、挂单作废和成交单进入原单退货流程；UI 不直接访问 SQLite、Mapper 或拼装领域事实。

## 自动化证据

| 门禁 | 结果 | Artifact ID | Artifact SHA-256 |
| --- | --- | --- | --- |
| governance / RTM / 契约 / 依赖差异 | PASS | `9453051129` | `3308e9cec62c9c8b83c047a3a195016bcfe28659ccaad6e44dc2b79a04001e4d` |
| Web 构建、审计、类型、组件和许可证 | PASS | `9453090081` | `199907fbb38871a6e3ce380dcf86172cefa493fbd47bc883a362d29bf2d9c1f3` |
| MySQL 8.4 空库 V1—V54 迁移、约束与重复执行 | PASS | `9453128927` | `43fc4b210282a0c00f4fe838a24a9e52d21cb7e3e7c9ff936c10c5a78ce13ad6` |
| Flutter Windows 全量、SQLite 与覆盖率 | PASS | `9453144898` | `ca81bd3f1de91bd1b63f8580378017e7d606c50fc63d187e64a9ecb3ad9e87ba` |
| Flutter Ubuntu、Kotlin、Android APK、SBOM 与覆盖率 | PASS | `9453214053` | `b24bcfb746cb80d06d940178f1e28cae20f5705e11f28f409ffd1ac2fb8a163a` |
| 服务端全量、覆盖率、聚合 SBOM 和许可证策略 | PASS | `9453273546` | `d3734255d5f26d0399f673a5eaff401a5843b40dd6a7a5fbc5d72bf94de50e11` |
| HIGH/CRITICAL 漏洞、Secret 与工作流配置 | PASS | `9453286354` | `6eb82045079640efe357c93ab698faf836aa647e44c2c39869942439628b1dd3` |

本地补充证据包括 Flutter 全量 158 项测试、Gate 2 作用域覆盖率 91.43%、
Gate 6D 作用域覆盖率 90.69%、服务端全模块 `clean verify` 以及 Gate 7B
治理/RTM/契约校验。GitHub MySQL 8.4、双平台、Android 构建和安全结果作为
独立执行证据。

## 状态边界

- `T2-ORD-004 = VERIFIED`，尚未经项目发起人接受，不得写为 `ACCEPTED`。
- `T2-EXG-001`、`T2-PAY-004` 与 Gate 7C—7E 继续 `DRAFT`。
- `T2-PAY-002`、`T2-HWD-001`、`T2-PRN-001`、`T2-PAR-001` 继续 `BLOCKED`。
- Provider 网络、真实资金、真实设备/外设命令、伙伴执行、完整 Alpha、生产部署和商业声明均为 0。
