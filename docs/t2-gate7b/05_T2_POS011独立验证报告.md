# T2-POS-011 独立验证报告

## 结论

`VERIFIED`。候选提交
`f19406dbb8c8a31818969766cbb6f548320d9f22` 的 GitHub Actions
[Run 32494347095](https://github.com/eiven-xxw/jshPOS/actions/runs/32494347095)
六个 Job 全部通过。该结论只代表版本化语义收据、软件预览、打印请求、
补打授权与审计的内部软件执行证据，不代表真实打印机、钱箱、外设或商业验收。

## 已验证行为

- 现金成交在同一 SQLite 事务中冻结订单、订单行、成交促销快照、收款、班次现金效果、语义收据、原始打印任务、审计、幂等结果和 Outbox；任一写入失败整体回滚。
- 收据绑定可信租户、门店、终端、业务日、订单、订单版本、模板版本、币种、金额和内容 SHA-256；预览只读取冻结语义，不按新模板或当前业务事实重算。
- 补打必须读取原收据，校验权限、原因、次数上限和内容摘要；重复同内容返回原结果，同幂等键异内容失败关闭。
- 服务端 Order Owner 校验收据身份、范围、模板、订单版本、金额守恒、行汇总及摘要；Sync Owner 只通过明确应用端口分派版本化事件。
- MySQL `ord_receipt_document` 为不可变事实，`ord_print_request` 为只追加请求；数据库触发器拒绝更新和删除。
- 真实打印仍失败关闭，任务执行状态固定为 `BLOCKED_EXTERNAL`，软件适配证据为 `SOFTWARE_PREVIEW/BLOCKED_REAL_PRINTER`，未调用真实设备或外设命令。

## 失败与修复记录

首个实现候选提交 `0159a344f05bf52b41b3430683249505568ac998`
的 [Run 32493603840](https://github.com/eiven-xxw/jshPOS/actions/runs/32493603840)
如实保留失败。该轮 V53 迁移已成功执行并完成 53 个版本，但
`ReleaseMigrationMySqlIT` 的期望版本清单仍止于 V52，导致全迁移测试失败。
修复只同步测试期望到已发布的 V53，没有改写迁移、降低阈值、跳过测试或
重跑失败 Job；新提交重新执行了完整六类 Job。

## 自动化证据

| 门禁 | 结果 | Artifact |
| --- | --- | --- |
| governance | PASS | `9450997935` |
| web-and-supply-chain | PASS | `9451038714` |
| Flutter Ubuntu | PASS | `9451046814` |
| MySQL 8.4 空库完整迁移 | PASS | `9451058981` |
| Flutter Windows | PASS | `9451106909` |
| 服务端全量、覆盖率与聚合 SBOM | PASS | `9451232486` |

本地补充证据包括 Flutter 全量 148 项测试、27 项收据相关测试、
Order 47 项与 Sync 43 项测试、Web 构建/类型检查/37 项测试、治理/契约/RTM
及服务端全模块编译测试；GitHub 双平台、MySQL 和全模块结果作为独立证据。

## 状态与下一门禁

- `T2-POS-011 = VERIFIED`；尚未经项目发起人接受，不得写为 `ACCEPTED`。
- `T2-ORD-004 = DRAFT`；本 `VERIFIED` 封存提交的闭环 CI 全绿后，才允许逐项设计准入。
- `T2-PRN-001` 继续 `BLOCKED`，软件预览和打印任务不得替代真实打印证据。
- Provider 网络、真实资金、真实设备/外设命令、伙伴执行、完整 Alpha、生产部署和商业声明均为 0。
