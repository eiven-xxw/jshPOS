# T2-DMT-001 下一步操作指令

```text
我确认《T2-RPL-001 独立周门禁报告》，接受 T2-RPL-001
CONDITIONAL PASS。

同意将 T2-RPL-001 由 VERIFIED 更新为 ACCEPTED。明确该接受只覆盖版本化库存
上下限、确定性缺货预警、可解释补货建议、人工复核/审批和转采购 DRAFT 的内部
软件执行；百万级结果仅为合成趋势，不代表生产容量、真实供应链、完整 Alpha、
现场试点、生产或商业验收。

按 CONDITIONAL GO 启动 T2 Gate 7C / Sprint S21-D：T2-DMT-001
开业商品、供应商、期初库存和会员资料迁移的独立正式开发。

以 T2-RPL-001 最终封存提交
[填写 T2-RPL-001 最终封存提交]
作为工作分支起点，创建独立分支：
t2/gate7c-sprint21d-dmt001-business-migration

本批仅允许逐项准入 T2-DMT-001。先冻结数据主权、文件/批次身份、来源摘要、
映射版本、预检、审批、Owner 写入端口、状态机、检查点、对账、权限、审计、
幂等、API/事件、MySQL 迁移、Vue 向导、容量、失败恢复、清理和量化验收，
再进入正式实现。

Migration Owner 只负责文件登记、隔离 staging、映射、预检、批次编排、检查点、
差异、签署和清理，不得直接写 Catalog、Procurement、Inventory 或 Member 私有表。
商品、条码、单位和分类品牌必须通过 Catalog 正式导入端口；供应商必须通过
Procurement 正式端口；期初库存必须通过 Inventory Owner 追加不可变 OPENING_IN/
OPENING_ADJUSTMENT 流水；会员必须通过 Member 正式端口并遵守身份密文、令牌摘要、
最小化展示和删除规则。禁止用数据库脚本绕过正式 API/应用端口。

每个导入文件必须绑定可信 tenant_id、资料类型、Schema/映射版本、原文件 SHA-256、
字符集、行数、来源系统、受控保管引用和上传人；tenant_id 不得来自文件内容。
CSV/Excel 公式、路径穿越、压缩炸弹、超限文件、重复列、未知字段、非法编码、
恶意文本、摘要变化和跨租户替换必须失败关闭。原始文件不得进入普通日志、CI、
公共制品或 Git 仓库。

批次状态至少覆盖 UPLOADED、PREFLIGHTING、PREFLIGHT_FAILED、READY、APPROVED、
IMPORTING、IMPORTED、RECONCILING、RECONCILED、ACTIVATION_PENDING、ACTIVATED、
FAILED、COMPENSATION_REQUIRED 和 CLEANED。预检必须完整输出字段级/行级错误，
任一阻断错误不得形成部分可见业务事实；同幂等键异内容拒绝，重复提交、服务重启、
乱序回调、检查点恢复和部分 Owner 失败必须最终收敛。

跨 Owner 导入采用可恢复 Saga，不承诺跨模块数据库大事务。每个 Owner 只写自己的
事实并返回稳定结果摘要；批次必须保存逐 Owner 检查点、成功/失败数、结果摘要和
对账差异。已经被后续业务引用的事实禁止物理删除或覆盖回写；失败通过未激活版本、
补偿命令或新调整事实处理。期初库存一旦激活只能通过新库存流水修正。

激活前必须完成双人审批和逐项对账：商品/条码/单位唯一性、供应商唯一性、期初库存
逐仓逐 SKU 守恒、会员身份冲突与最小必要字段。只有所有必需 Owner 达到 RECONCILED
才可原子切换可见版本或激活里程碑；任何 P0/P1 差异、摘要漂移或审计缺失必须
NO-GO。激活不得自动开店、自动发布价格、自动生成采购承诺或产生资金效果。

本 Sprint 只使用两个虚构租户、多门店、多仓库、三业态合成数据和明确获授权的
脱敏样本。覆盖空文件、重复文件、100k/1m 合成行、字段映射冲突、重复条码、单位
冲突、供应商冲突、负/超精度库存、会员重复身份、同键异内容、断点续传、部分 Owner
失败、重启恢复、跨租户替换、对账不平、激活中断、补偿、到期删除和前向修复。
合成容量结果不得写成生产 SLA。

CI 继续执行治理、服务端、Web、MySQL、Flutter Linux/Windows、SQLite、
Android/Kotlin、迁移故障、租户权限、文件攻击、Secret/PII、依赖、SBOM、许可证、
覆盖率和既有 Gate 全量回归；不得降低阈值、跳过测试、自动重跑掩盖 Flaky、
修改已发布迁移或建立测试专用导入后门。

T2-ONB-001、T2-LOT-001 与 Gate 7C 后续需求继续 DRAFT；T2-PAY-002、
T2-HWD-001、T2-PRN-001、T2-PAR-001 继续 BLOCKED；T2-JSH-001、T2-LIC-001
继续 DEFERRED。不得进行 Provider 网络、真实资金、真实设备/外设、未经授权真实
PII/商户数据、伙伴现场执行、完整 Alpha、生产部署或商业可用声明。

完成后提交《T2-DMT-001 独立周门禁报告》等待我确认，不得自动准入
T2-ONB-001、T2-LOT-001 或 Gate 7D。完成后为我整理下一步操作指令。
```
