# T2-DAT-001 独立验证报告

## 1. 结论

`T2-DAT-001` 完成独立验证，结论为 `PASS / VERIFIED`。验证提交为
`2f1ad5ecf80ffde85e3ea0d6f7de3013c3145aaa`，GitHub Actions run 为
[`32440088990`](https://github.com/eiven-xxw/jshPOS/actions/runs/32440088990)，八个 Job 全部成功。

本结论只覆盖内部合成数据、软件生成密钥、GitHub 干净执行器和 MySQL 8.4 / SQLite 正式迁移代码；不覆盖生产 PITR、生产 KMS、真实对象存储、真实商户数据或商业灾备 SLA。

## 2. 验证结果

| 门禁 | 结果 | 核验内容 |
| --- | --- | --- |
| governance | PASS | 串行状态、外部零执行、CORE/API/DAT 机器审计 |
| server | PASS | 47 项模块化单体全量回归、覆盖率、正式聚合 SBOM |
| mysql | PASS | MySQL 8.4 空环境执行 V1—V51、159 张正式表、中文说明、主键、租户索引和精度 |
| pos-linux | PASS | Dart 格式、Analyze、SQLite v1—v7、迁移中断恢复、未来版本失败关闭、Android/Kotlin/APK |
| pos-windows | PASS | Windows Flutter、SQLite 和固定 Gate 6G 用例复现 |
| admin-web | PASS | 类型检查、ESLint、组件测试和生产构建 |
| security | PASS | Secret、依赖、许可证、SBOM 与漏洞门禁 |
| evidence | PASS | 同一 run 制品、摘要、状态和证据边界汇总 |

## 3. 数据不变量

- 51 个 Flyway 版本连续、全局唯一；既有 V1—V42 未修改，只追加 V43—V51。
- 159 张正式表均具备主键和中文表说明；含 `tenant_id` 的业务表具备租户索引或唯一约束。
- `FLOAT/DOUBLE` 正式列为 0；金额、成本与数量继续使用已冻结的整数或 DECIMAL 精度。
- 五张备份控制面表继续作为显式多租户范围例外，以租户集合摘要和父备份范围授权，不构成业务表租户缺口。
- 合成种子固定为两个虚构租户、四门店、四虚构终端及三业态，写入路径为正式 API 顺序，不包含 Secret、真实 PII 或真实设备资料。

## 4. 失败与修复记录

首次 run `32439772017` 因两份新增 Dart 文件未实际写回固定 Dart 3.13.0 的格式化结果而失败。失败日志保留；修复仅写回格式结果，并重新执行 Analyze、SQLite 故障测试和全部流水线，没有跳过测试、降低阈值或自动重跑失败 Job。

## 5. 制品

- Evidence index：Artifact `9432253292`，保留至 2026-11-19。
- Server / SBOM：Artifact `9432229137`。
- MySQL：Artifact `9432120613`。
- POS Linux / APK：Artifact `9432194698`。
- POS Windows：Artifact `9432155569`。
- Web：Artifact `9432109169`。
- Security：Artifact `9432242551`。
- Governance：Artifact `9432087104`。

因此只允许 `T2-INT-001` 进入 `IN_PROGRESS`；`T2-E2E-003` 继续保持 `DRAFT`。
