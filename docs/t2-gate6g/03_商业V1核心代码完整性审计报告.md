# 商业 V1 核心代码完整性审计报告

## 1. 结论

`T2-CORE-001` 独立验证结论为 `VERIFIED`。审计覆盖 55 项已接受 T2 需求和 15 个正式 Owner 模块，未发现未分类生产占位、跨 Owner Mapper、缺失组合根依赖或开放的 P0/P1 缺陷。证据等级为 `STATIC_AND_SOFTWARE_EXECUTION`，不提升任何外部证据等级。

## 2. 覆盖与装配结果

机器审计命令：

```text
python scripts/audit_t2_gate6g_core.py --output artifacts/t2/gate6g/core/core-audit.json
```

审计结果：

| 项目 | 结果 |
|---|---:|
| 已接受 T2 Requirement | 55 |
| 正式 Owner 模块 | 15 |
| REST Controller | 30 |
| Spring 应用组件 | 99 |
| Mapper/XML | 17 |
| 已发布 Flyway 迁移 | 42 |
| Java 测试类 | 167 |
| 缺失覆盖链 | 0 |
| 未分类生产临时实现 | 0 |
| 跨 Owner Mapper | 0 |
| P0/P1 | 0/0 |

`core-audit.json` 为每项已接受需求生成 `Requirement → 页面/界面 → API → Application Service → Domain → Repository/Mapper → Migration → Test` 机器覆盖链；不适用页面的纯后端需求显式标记 `N/A_BY_REQUIREMENT_SCOPE`，不以空值伪装覆盖。

## 3. 已修复问题

审计发现订单 Owner 与促销 Owner 曾由 Mapper 直接读取 `jsh_config_*` 私表。该实现违反 Owner 数据主权，且旧交班阈值 SQL未限定配置模板，存在读到错误配置值的风险。本次已完成：

1. Foundation Owner 新增 `PublishedConfigReadPort` 与可信实现，租户只来自认证上下文，门店覆盖租户的优先级固定；
2. Order Owner 通过 `ShiftDifferencePolicy` 读取 `SHIFT_CASH_DIFFERENCE`，缺少策略时按零阈值失败偏安全，畸形/负值拒绝；
3. Promotion Mapper 删除对 `jsh_config_*` 的 SQL，适配器改经 Foundation 只读端口读取 `PROMOTION_MANUAL_AUTHORITY`；
4. 增加可信租户、门店覆盖、未发布版本、策略解析与失败关闭单元测试；
5. Mapper 门禁改为明确禁止 `jsh_config_*` 出现在促销 XML。

## 4. 允许保留的显式边界

- `PosLocalDatabase.inMemory`：只作为测试/合成执行的显式构造器；生产组合根使用路径数据库，不会自动选择内存库。
- `pos_device_adapter_platform_interface` 的未实现异常：真实硬件仍为 `BLOCKED` 时的插件平台失败关闭；不得作为真实设备成功证据。
- Flutter `Locked*` 默认服务：未注入可信终端、业务组合根时明确返回不可用错误，避免在真实外部资料缺失时伪造登录、销售或退款成功。

## 5. 独立执行证据

- Gate 6G 治理与外部零执行检查：通过；
- CORE 机器审计：`PASS`；
- `ruoyi-admin -am clean verify`：47 个 Reactor 项目全部成功，全部既有 JaCoCo 门槛通过；
- Foundation 新增测试 4 项、Order 新增策略测试 3 项通过；促销 Mapper 边界回归通过；
- 已发布迁移未修改，Provider 网络调用、真实资金、真实设备命令、伙伴联系、现场试点、完整 Alpha 和生产部署均为 0。

## 6. 证据边界与后继条件

本结论只允许 `T2-API-001` 进入 `IN_PROGRESS`。它不接受 Gate 6G、不接受任何外部 P0、不更新 V1 汇总/UAT/REL，也不允许自动进入 DAT/INT/E2E。
