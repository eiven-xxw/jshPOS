# 准入范围与先红后绿记录

## 准入

项目发起人于 2026-08-27 确认 `CR-T2G10A-024 CONDITIONAL GO`，只允许通过唯一前向迁移
`V202608260089` 为 `rpt_inventory_cost_daily` 新增：

```text
idx_rpt_inventory_keyset(
  tenant_id, projection_version, business_date, store_id,
  warehouse_id, sku_id, currency
)
```

## 红基线

先增加 `ReportingInventoryKeysetIndexMigrationPolicyTest`，在 V89 尚不存在时执行：

```text
./server/mvnw -f server/pom.xml -pl ruoyi-modules/jshpos-reporting \
  -Dtest=ReportingInventoryKeysetIndexMigrationPolicyTest \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

预期且必须失败于：`approved V89 RPT-INVENTORY keyset index migration` 为 `null`。
该失败只证明 V89 尚未实施，不以跳过测试、放宽断言或建立绿色占位解决。

2026-08-27 实际执行结果：`Tests run: 1, Failures: 1`，失败位置为
`ReportingInventoryKeysetIndexMigrationPolicyTest.java:24`，断言内容与上述预期完全一致；
因此红基线有效。此前一次命令因本机 `JAVA_HOME` 缺失未进入测试，不作为红基线证据。

## 绿基线要求

V89 加入后，同一测试必须通过，并在 MySQL 8.4.11 继续验证：空库到 V89、V88 到 V89、
重复执行、Flyway validate、10k/100k 无全扫/无 filesort、查询数预算、0 重复/缺失/越权及
十二项数量/成本守恒。
