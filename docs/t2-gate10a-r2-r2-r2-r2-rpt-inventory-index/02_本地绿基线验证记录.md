# 本地绿基线验证记录

## 实现提交

- V89 实现提交：`98b157ce73207163e862c6ef12aa40af53f5251f`
- 唯一生产数据库变化：新增
  `V202608260089__reporting_inventory_keyset_index.sql`
- 生产 Java、Mapper/XML、OpenAPI、事件与既有 V1—V88 迁移：无变化

## 已通过检查

2026-08-27 在 Windows 干净 Maven 进程中完成：

1. `ReportingInventoryKeysetIndexMigrationPolicyTest`：`1` 项测试通过，证明 V89
   文件、表、索引名、七列顺序、在线 DDL 约束以及禁止删除索引规则满足批准内容；
2. `jshpos-release` 及其 `38` 个 reactor 模块测试源码编译通过，证明 MySQL 8.4.11
   专项夹具可编译；
3. RTM、JSON 契约、专项范围检查、Python 编译、`git diff --check` 均通过；
4. `actionlint 1.7.12` 对专项 GitHub Actions 工作流校验通过；
5. 相对批准起点 `30edba224d9acf35f64d81c042b5153e08e2eb66`，迁移差异严格为
   单一新增 V89。

## 远端验证边界

本机未安装 Docker，因此不生成本地 MySQL 证据。空库到 V89、V88 升级、重复执行、
Flyway validate、MySQL 8.4.11 的 10k/100k 执行计划、查询数、租户隔离、逐键完整性和
十二项守恒，统一交由 GitHub 干净执行器验证。远端全量门禁通过前，结论仍为
`IN_PROGRESS`，`G10A-SQL-P2-001` 保持 `OPEN`。
