# 范围与当前契约冻结

## 决策边界

- 起点：`8ae78147028dfab37c8ee615f212b4212f7673d6`；
- 分支：`t2/gate10a-r2-r2-r2-r3-rpt-pay-rec-prep`；
- 复用：`T2-RPT-002`、`T2-API-001`、`CR-T2G10A-015`、`ADR-074`；
- 当前阶段：`PREP_ONLY`，运行时、索引和迁移均未准入；
- `G10A-SQL-P2-001=OPEN`，`G10A-RES-P2-001=PREPARED`。

允许冻结契约、增加测试范围红基线、采集 MySQL 8.4.11 的 10k/100k 当前执行计划，完成 Owner
批量端口、版本化 keyset、受控流式导出、差异守恒、候选 SQL/索引、影响和回退设计。生产 Java、
Mapper/XML、SQL、API、事件、索引、数据库对象、依赖和迁移变化必须为 0。

## 当前正式契约

| 层次 | 冻结值 |
|---|---|
| Requirement | `T2-RPT-002` |
| HTTP | `GET /api/v1/reports/payment-reconciliation` |
| operationId | `queryPaymentReconciliation` |
| 权限 | `report:payment-reconciliation:read`，服务端可信租户和门店授权 |
| 响应 | `R<List<ReconciliationView>>`，当前无页界 |
| 应用服务 | `PaymentReconciliationService.query` |
| 持久化端口 | `PaymentReconciliationPersistencePort.query` |
| Mapper | `PaymentReconciliationMapper.query` |
| 稳定排序 | `business_date,reconciliation_id` |
| 可选筛选 | `difference_type,handling_state` |
| 当前导出 | 每门店一次查询，全部结果内存物化 |

当前 statement 标准化 SHA-256 为
`7562de6d4e0698f16f94a30bc27db9a7c75df8cffe319140375576360741709d`。v1、错误码、权限、
Payment/Refund 状态、差异类型与处理状态均冻结；准备阶段不得“顺手”修改。

## Owner 与外部停止线

Reporting Owner 独占可重建对账投影、差异处理状态和报表审计，只能消费 Payment/Refund 的版本化
事实或明确只读端口；不得查询 Payment 私有 Mapper。Payment/Refund 继续独占资金状态，`UNKNOWN`
只能观察原命令、查询、回调或账单收敛，报表不得重发扣款/退款命令。

本阶段仍只覆盖 Provider 无关内部事实与内部合成账单。拉卡拉或其他 Provider SDK/HTTP、真实回调、
账单下载、沙箱/生产密钥、真实资金和外部网络调用全部为 0。
