# G9A-R3A 失败种子与依赖审查

## 1. 测试先行记录

| Seed | 首次失败 | 修复后 |
|---|---|---|
| R3A-VUE-MOUNT-01 | 四个挂载用例 4/4 失败 | 扩展为 5 项，5/5 通过 |
| R3A-FLT-ROUTE-01 | `JshposApp` 无 `tenderController`，用例编译失败 | 会话入口、可信校验和失败关闭 7/7 通过 |
| R3A-VUE-WORKER-01 | Vitest worker 并发改写自动导入声明，Windows 文件锁冲突 | 测试环境关闭声明生成，默认并发全量测试通过 |
| R3A-FLT-LAYOUT-01 | 全量回归中旧用例未滚动，按钮点击未生效 | 增加真实滚动步骤，原资金失败断言保留，全量通过 |

没有删除失败用例、降低断言或使用自动重跑。完整机器记录见
`contracts/t2/gate9b-r3a/failure-seeds-v1.json`。

## 2. Vue 测试依赖

首次 `@vue/test-utils 2.4.6 + happy-dom 20.8.3` 组合出现 3 个 High；升级直接依赖后
仍有 `glob 10.4.5` 的 1 个 High。最终锁定：

- `@vue/test-utils 2.4.11`；
- `happy-dom 20.11.6`；
- `glob 10.5.0` 精确安全覆盖。

最终 `pnpm audit --audit-level high` 为 0 个已知漏洞。三项只进入开发测试依赖树，
生产构建、许可证清单和 SBOM 仍由 CI 校验。

## 3. 本地复跑摘要

- Web：26 个文件、76 项测试通过；ESLint、Vue TypeScript、生产构建和 High 审计通过；
- Flutter：分析 0 问题；191 项通过，1 项既有专用性能用例按原策略跳过；
- Flutter 覆盖率：Gate 2/Gate 6D/Gate 6E 分别为 0.9188/0.9101/0.9070，均高于 0.90；
- Server：49 个 Maven reactor 模块全量回归和既有覆盖率校验通过；
- 治理：目录、RTM、契约和 `git diff --check` 通过；
- 数据：无 Flyway/SQLite 迁移变化；服务端运行时无变化。
