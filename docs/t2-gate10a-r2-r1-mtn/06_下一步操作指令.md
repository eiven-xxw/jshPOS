# Gate 10A-R2-R1 完成后的下一步操作指令

以下指令仅在本批完整 GitHub CI 全绿并经项目发起人确认后使用：

> 我确认《G10A-MTN-P2-001 独立周门禁报告》，接受 G10A-MTN-P2-001 CONDITIONAL PASS。
>
> 同意将 G10A-MTN-P2-001 确认为 CLOSED_IN_GATE10A_R2_R1。明确该接受只覆盖内部 Server
> 行为金标、复杂度预算和 Owner 内最小职责拆分，不代表支付沙箱、真实设备/外设、完整 Alpha、
> 生产、商业验收或商业 SLA。
>
> 按 CONDITIONAL GO 启动 Gate 10A-R2-R2：G10A-SQL-P2-001 SQL 性能整改独立准备阶段。
> 以 G10A-MTN-P2-001 最终治理提交作为起点创建独立准备分支。本阶段只允许冻结 MySQL 8.4
> 数据分布、关键查询清单、参数、EXPLAIN ANALYZE、查询次数、分页、索引、N+1、超时、权限与
> 租户边界，补齐可复现失败回归、影响分析、停止线和串行修复计划；不得修改运行时 SQL、Mapper、
> 索引、数据库对象或已发布迁移。
>
> 任何新索引、数据库对象、查询语义、资金/库存/租户/同步语义变化必须另行 CR；
> G10A-RES-P2-001 继续 PREPARED，不得提前整改。完成后提交《G10A-SQL-P2-001 启动评审报告》
> 等待确认，不得自动进入 SQL 运行时、资源整改、R3、完整 Alpha 或生产发布。
