# 复杂度预算与行为金标执行结果

## 1. 复杂度预算

| 目标类 | 基线行数 | 当前行数 | 减少 | 上限 |
|---|---:|---:|---:|---:|
| `ProcurementService` | 760 | 600 | 160 | 600 |
| `LotInventoryService` | 700 | 590 | 110 | 600 |
| `TransferService` | 658 | 539 | 119 | 600 |
| `ReplenishmentService` | 607 | 502 | 105 | 600 |
| `ReturnOrchestrationService` | 581 | 549 | 32 | 560 |

五类合计由 3306 行降至 2780 行，共减少 526 行。其余 14 个基线大类均未增长；本批新增生产类最大 141 行，低于 400 行预算。22 个 Owner Maven 依赖图无环。

## 2. 最小职责拆分

- Procurement：提取 `ProcurementCommandPolicy` 与 `ProcurementLotCoordinator`；
- Inventory：提取 `LotInventoryCommandPolicy`；
- Transfer：提取 `TransferCommandPolicy`；
- Replenishment：提取 `ReplenishmentCommandPolicy`；
- Returns：提取 `ReturnCommandPolicy`。

新类均位于原 Owner 应用服务边界内，承担输入校验、稳定摘要、关联标识或受控 Owner 端口协调；未将领域逻辑迁入 Controller、Mapper 或通用工具类。

## 3. 行为金标

- 五个公开应用服务的方法集合与 `@Transactional` 边界：一致；
- 四个 Owner 应用服务错误码多重集合 SHA-256：一致；
- API、事件 Schema、SQL/XML、POM、配置、数据库迁移：无变化；
- 幂等、审计、Outbox 与数据守恒定向测试：通过；
- Finding 状态：`VERIFIED_AWAITING_SPONSOR_CONFIRMATION`，未关闭。
