# 下一步操作指令

```text
我确认《G9A-R2 启动评审报告》，接受准备阶段 CONDITIONAL PASS，
并接受 ADR-070 对商业默认装配与非 V1 平台能力隔离的建议。

按 CONDITIONAL GO 启动 G9A-R2 生产装配与演示面收敛正式整改。

以 G9A-R2 准备阶段最终封存提交
[填写最终封存提交]
作为工作分支起点，创建独立分支：
t2/gate9b-sprint27b-g9a-r2-runtime

本批只允许关闭 G9A-ASM-P1-001，复用 T2-CORE-001、T2-SEC-002、
T2-RDY-001，不新增 Requirement ID 或业务能力。先将 ADR-070 更新为
Accepted，再按“失败回归 → Maven/JAR → Web/路由 → 四方言初始化 →
SBOM/配置 → 启动与完整 CI”的顺序串行整改。

商业默认构建必须移除 ruoyi-demo；ruoyi-generator、ruoyi-workflow、
ruoyi-job 仅可保留在默认 reactor 和商业制品之外，相关 Web、菜单、路由、
模拟账单执行器和 SBOM 均不得进入商业候选。禁止仅隐藏菜单。

已发布迁移不得修改；既有环境只能使用带预检、摘要、审计和失败关闭的
前向清理。正式300项API、22个Owner、权限、租户、资金、库存及三业态旅程
必须保持不变。CI全绿后提交《G9A-R2生产装配与演示面收敛独立周门禁报告》
等待我确认，不得自动进入G9A-R3、完整Alpha或生产发布。

所有外部BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED及零执行边界保持不变。
完成后为我整理下一步操作指令。
```
