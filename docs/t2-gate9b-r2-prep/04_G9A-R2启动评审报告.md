# G9A-R2 启动评审报告

## 评审结论

**PREP CONDITIONAL PASS / CONDITIONAL GO RECOMMENDED**。

准备阶段已完成装配事实、影响面、保留边界、实施顺序、测试矩阵和回退设计；
`G9A-ASM-P1-001` 仍为 `OPEN`，必须等待项目发起人确认后才能进入正式整改。

## 基线与范围守恒

- 唯一基线：`53e540fd14559e7ae0f907b244b0dbac37167cfe`；
- 工作分支：`t2/gate9b-sprint27b-g9a-r2-prep`；
- 复用需求：`T2-CORE-001/T2-SEC-002/T2-RDY-001`；
- 本阶段运行时、POM、Vue、初始化 SQL、已发布迁移和依赖锁变更：0；
- 新业务能力和 Requirement ID：0；
- Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、生产部署：0。

## 关键证据

1. 当前 JAR 同时包含 demo/job/generator/workflow；
2. `ruoyi-demo` 有 19 个 Controller，且 Springdoc 主动暴露演示分组；
3. 2 个演示页面和 4 个 API 文件被全量 Vite glob 发现，生产 dist 有演示信号；
4. 四方言初始化源均有 13 条演示菜单、24 条角色绑定和 2 张测试表；
5. 435 组件的当前聚合 SBOM 包含四个非 V1 模块；
6. 22 个已接受 Owner 对 generator/workflow/job 无代码依赖，具备隔离前提。

## Go 条件

- 项目发起人接受 ADR-070 的推荐保留边界；
- 正式整改仅关闭 `G9A-ASM-P1-001`，不新增业务能力；
- 商业默认构建、JAR、Web、SQL、路由和 SBOM 必须同时归零；
- 正式 300 API、22 Owner、三业态内部旅程和全部质量门禁不得回退；
- 数据库只允许安全前向清理，摘要漂移或客户自定义必须失败关闭。

## 准备阶段 CI 证据

- 初始候选提交：`38aaa5f64bc1d3d45443b2bebf79cf06c92c71e9`；
- GitHub Actions：[Run 32733150548](https://github.com/eiven-xxw/jshPOS/actions/runs/32733150548)，
  结论 `success`；
- `governance-ubuntu`、`governance-windows`、`scope-integrity`、`assembly-baseline`、
  `evidence` 共 5 个 Job 全绿；
- Evidence Artifact：`9522619610`，GitHub 摘要
  `sha256:294cfd03372ff04a2e6082bb1653f19ee34836e8a8fd71e47b70de61c277d08a`；
- 该绿色结论只证明准备审计和缺陷复现可信，不代表 `G9A-ASM-P1-001` 已关闭。

## No-Go 条件

- 只隐藏菜单或关闭前端按钮但保留服务端路由；
- SBOM、JAR 或初始化源仍含被排除模块；
- 任何正式 API、Owner、权限、资金、库存或租户行为被删除或放宽；
- 修改已发布迁移、降低门禁或用重跑掩盖 Flaky；
- 外部状态、证据等级或零执行边界发生变化。

## 状态保持

PAY/HWD/PRN/PAR 继续 `BLOCKED`，UAT/REL 继续 `DRAFT`，LIC/JSH 继续
`DEFERRED`。本报告不代表完整 Alpha、生产或商业验收。
