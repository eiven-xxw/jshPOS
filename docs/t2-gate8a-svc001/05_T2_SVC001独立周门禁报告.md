# T2-SVC-001 独立周门禁报告

## 1. 候选结论

- Requirement：`T2-SVC-001`
- 状态：`VERIFIED`（项目发起人确认前不得 `ACCEPTED`）
- 证据：`INTERNAL_SYNTHETIC_SOFTWARE_ONLY`
- 起点：`ae0c145b82306dd08f9a0999ba3f030d54e8e3e9`
- 分支：`t2/gate8a-sprint24c-svc001-runtime`
- 候选提交：`2f2d190e4d0c00f614c0af032ddfb27546df4342`
- GitHub Actions：[Run 32656004548](https://github.com/eiven-xxw/jshPOS/actions/runs/32656004548)，10 个 Job 全部成功
- 证据 Artifact：`t2-gate8a-svc001-evidence-index`，ID `9497612504`，41,892 bytes，保留 90 天
- 证据校验：ZIP SHA-256 `a64c96bac0cc60432f7e753d96c5efc988b7430f769a2bf40a9ad71f5db7f3a2`；文件 SHA-256 `f496b1d720cb4cfc031139c42995c301d1127be938411526b5f3c5be69f043e9`
- P0/P1：`0/0`
- 建议结论：`CONDITIONAL PASS`，等待项目发起人确认

## 2. 实现范围

已建立独立 `jshpos-service` Owner，实现版本化服务目录、实施项目与冻结检查项、工单状态机、
认领租约与责任流转、解决与独立复核、关闭重开、内部时间目标、附件元数据、租户命名空间对象存储、
按次短期下载、受控清理、稳定幂等、乐观锁、审计与 Outbox。完成 MySQL V85/V86、
MyBatis-Plus/XML 双边界、正式 REST/OpenAPI/事件、Vue 运营旅程和 28 项固定故障向量。

Service 只通过正式端口读取 SaaS/Subscription 授权，不得激活/停用租户、切换套餐、修改订阅、
执行支付、安装设备、调用外设或批准生产变更。附件正文不进入数据库、Git、日志或普通证据。

## 3. 本地门禁结果

| 门禁 | 当前结果 |
|---|---|
| 治理、RTM、契约 | 143 项 RTM、106 个契约、ADR-059、持久化登记、状态保持与外部零执行通过 |
| Server | 54 个 Reactor 模块全量 `clean verify` 通过；249 个测试套件共 831 项测试，失败/错误/跳过均为 0 |
| Service 覆盖率 | 行覆盖 96.15%，分支覆盖 85.14%，达到行≥90%、分支≥85% |
| Web | 生产构建、ESLint、typecheck 和 25 个文件共 71 项 Vitest 测试通过 |
| SBOM/许可证 | Server 聚合 SBOM 435 个组件；14 个受限许可证组件均有既有复核，无未批准 GPL/AGPL |
| Web 依赖 | `pnpm audit --audit-level high` 无已知漏洞 |
| Flutter/Android/MySQL/正式运行栈/安全 | 本机缺少 Flutter、Docker；必须由 GitHub Ubuntu/Windows 干净执行器补足，未创建绿色占位 |

## 4. GitHub 完整门禁与制品

候选提交对应的 GitHub Actions Run 于 2026-08-24（北京时间）完成，治理 Ubuntu/Windows、
Server、MySQL、Web、Flutter Ubuntu/Windows、正式运行栈、Security 和 Evidence 共 10 个 Job
一次全绿。最终证据索引状态为 `PASS`、Requirement 状态为 `VERIFIED`，覆盖 9 类证据生产者和
765 个文件；索引内规范化摘要为
`32f0de6fa080d34a07c5a3cb2a88f2d8a471a11a86b386359755578b5e7651d0`。

运行栈由该候选的正式 Server JAR 与 Web dist 启动，并确认 MySQL 中 10 张 `svc_*` 表和
Flyway 最高版本 `202608240086`。Security 对 Server/Flutter SBOM 的 HIGH/CRITICAL 漏洞、
仓库 Secret、Workflow 配置和许可证策略均已通过；没有降低阈值、重跑失败 Job 或创建绿色占位。

## 5. 迁移、恢复与安全

- V85 只新增 10 张 `svc_*` 表，V86 只新增菜单权限；已发布迁移未修改。
- 目录项、状态/责任历史、命令、审计和 Outbox 只追加；头表使用乐观锁和具名条件更新。
- 附件正文只经对象存储端口写入；数据库只存不透明键、摘要、类型、大小、保留和清理状态。
- 数据库事务回滚会尝试清理已写对象；对象存储不可用时失败关闭，不落本地或数据库。
- 迁移失败只能新增更高版本安全前向修复；不得覆盖历史或通过修改 V85/V86 修复。

## 6. 证据边界与待确认结论

真实通知、第三方客服、呼叫中心、在线聊天、远程控制、AI 派单、Provider 网络、真实资金、
设备/外设、伙伴现场、完整 Alpha 和生产执行均为 0。`T2-PAY-002/HWD-001/PRN-001/PAR-001`
仍为 `BLOCKED`，`T2-UAT-001/REL-001` 仍为 `DRAFT`，`T2-LIC-001/JSH-001` 仍为 `DEFERRED`。

当前已形成 `VERIFIED / CONDITIONAL PASS` 建议。项目发起人确认前不得更新为 `ACCEPTED`，
不得自动进入后续需求、完整 Alpha 或生产发布。
