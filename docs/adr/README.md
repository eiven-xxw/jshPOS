# 架构决策记录（ADR）

ADR 一经接受不得删除或静默改写。需要改变决策时，新建 ADR 并把旧记录标记为 `Superseded by ADR-xxx`。

状态：`Proposed`、`Accepted`、`Deprecated`、`Superseded`。

T0 必须记录 ADR-001 至 ADR-012；仓库/CI 切换、供应链安全修复、许可证准入和私有仓库依赖评审分别记录为 ADR-013、ADR-014、ADR-015、ADR-016。每项都应有上下文、决策、后果和验证方式。
