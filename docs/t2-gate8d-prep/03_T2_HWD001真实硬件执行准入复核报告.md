# T2-HWD-001 真实硬件执行准入复核报告

## 1. 独立结论

`NOT_ACHIEVED / BLOCKED / NO_GO`。商米 `T3 PRO MAX / L15B1` 仍只是主认证文档候选，
iMin `D4 Pro` 仍只是兼容文档候选。两台候选的完整受控包为 `0/2`；真实安装、重启、升级、
固件或诊断命令均为 0。

## 2. 每机型必需包

| 角色 | 候选线索 | 缺失的完整证据 | 当前 |
|---|---|---|---|
| 主认证 | 商米 T3 PRO MAX / L15B1 | 精确 SKU、Android/固件 Build、CPU/ABI、内存/存储、安全补丁、SDK 版本/许可/摘要、样机资产、安装升级授权、RMA/EOL、联系人 | MISSING_CONTROLLED_BUNDLE |
| 兼容 | iMin D4 Pro | 同上，并需独立兼容范围和停止规则 | MISSING_CONTROLLED_BUNDLE |

公开规格页、系列名称或模拟器不能代替精确 SKU、固件、SDK 授权和样机资产。每包还必须有
来源、签署、适用范围、SHA-256、保管、到期、删除和吊销规则。

## 3. 获批后的最小执行矩阵

覆盖 APK 签名/摘要、安装/升级/回退、冷启动、可信终端、数据包、键盘/扫码输入、SQLite、
断网同步、待同步/资金 UNKNOWN 下升级阻止、迁移失败、安全前向修复、8/24 小时长稳、CPU/
内存/温度/存储和脱敏诊断。操作必须绑定样机资产、场地、操作者、时间窗、恢复点和停止开关。

## 4. Go/No-Go

两个候选完整包均达到 `VERIFIED_DOCUMENT` 后，才提交新的独立执行准入评审。当前不得改变
`T2-HWD-001=BLOCKED`，不得安装、重启或向真实设备下发命令。
