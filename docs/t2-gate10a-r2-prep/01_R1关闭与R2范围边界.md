# R1 关闭与 R2 范围边界

## R1 关闭

项目发起人已确认 Gate 10A-R1 `CONDITIONAL PASS`，以下 Finding 已固化为
`CLOSED_IN_GATE10A_R1`：

- `G10A-CI-P2-001`；
- `G10A-DEP-P2-001`；
- `G10A-SUP-P2-001`。

该关闭只覆盖内部 CI、四栈依赖兼容快照和供应链治理，不提升支付、设备、Alpha 或生产证据。

## R2 准备范围

只复核 `G10A-MTN-P2-001`、`G10A-SQL-P2-001`、`G10A-RES-P2-001`。允许产出 ADR、
红基线、影响分析、测试矩阵、阈值、串行计划和治理证据；运行时、SQL/XML、依赖、配置、
数据库与迁移变更均为 0。

三项 Finding 当前统一为 `PREPARED_AWAITING_SPONSOR_CONFIRMATION`。准备完成不等于整改完成。
