# Gate 10A-R2 Server、数据库与可维护性整改准备

本目录只固化 `G10A-MTN-P2-001`、`G10A-SQL-P2-001`、`G10A-RES-P2-001` 的
当前事实、失败红基线、ADR、影响分析、测试矩阵和串行整改计划。准备阶段不修改运行时、
SQL、依赖、配置、数据库或迁移，三项 Finding 不在本目录中关闭。

文档按 `01` 至 `09` 阅读；机器权威位于 `contracts/t2/gate10a-r2-prep/`。
