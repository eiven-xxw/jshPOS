# T2-MEM-003 Promotion Owner 独立验证记录

## 结论

Promotion Owner 阶段记录为 `VERIFIED_LOCAL`，允许按批准的串行计划进入 Order/Refund。
该结论不是整体 T2-MEM-003 VERIFIED，也不代表干净 MySQL、跨端成交、完整 CI 或外部证据通过。

## 已完成

- 普通促销与会员价候选统一进入确定性组合引擎，默认 `BEST_PRICE`，同额固定选择普通促销；
- 只有权益版本与促销能力配置双向允许时才执行叠加，单边允许仍按 `BEST_PRICE`；
- 叠加封顶会同步裁剪会员价调整明细，保证调整、逐行和订单头优惠金额完全守恒；
- 能力默认关闭，能力/规则包/权益快照/会员价版本任一无效、过期、摘要漂移或跨范围均失败关闭；
- 报价、逐行结果、调整、无 PII 权益绑定、命令、审计和 Outbox 在同一事务写入；
- 正式 REST、应用服务、Owner 查询端口、XML Mapper、V78 前向迁移及中文注释已完成。

## 验证结果

- `jshpos-promotion`：66/66；失败 0、错误 0；
- 首轮覆盖率门禁如实发现 84% 分支覆盖不足，未降低 85% 阈值，补齐失败关闭边界后门禁通过；
- `gross - discount = payable`、逐行合计、调整分摊和封顶叠加守恒通过；
- 幂等同键异内容、可信租户/门店、无 PII 绑定与只追加迁移静态策略通过；
- 干净 MySQL 8.4 迁移留待本 Gate 完整 CI，不创建绿色占位。

## 证据边界

仅为内部软件本地证据；订单退款原快照、数据包、SQLite、POS、Web 和跨端 E2E 尚未完成。
Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产与商业验收均为 0。
