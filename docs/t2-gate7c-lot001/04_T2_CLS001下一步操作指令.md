# T2-CLS-001 下一步操作指令

```text
我确认《T2-LOT-001 独立周门禁报告》，接受 T2-LOT-001
CONDITIONAL PASS。

同意将 T2-LOT-001 由 VERIFIED 更新为 ACCEPTED。明确该接受只覆盖
COMMUNITY_SUPERMARKET 条件模板下的基础批次/效期、FEFO、原单退货归批、
盘点/调拨来源保留、过期禁售、临期预警、签名包和 SQLite 原子成交的
内部软件执行；不代表真实电子秤、厂商扫码 SDK、REAL_DEVICE、完整 Alpha、
生产、商业验收，也不准入批次成本或复杂 WMS。

按 CONDITIONAL GO 启动 T2 Gate 7D / Sprint S22-A 第一批：
T2-CLS-001 门店业务日日结、差异、审批和只追加签署事实的独立正式开发。

以 T2-LOT-001 最终封存提交
[填写 T2-LOT-001 最终封存提交]
作为工作分支起点，创建独立分支：
t2/gate7d-sprint22a-cls001-daily-close

本批仅允许逐项准入 T2-CLS-001。先冻结数据主权、业务日/时区、来源检查点、
状态机、冻结点、金额口径、差异类型、职责分离、权限、审计、幂等、API/事件、
MySQL 前向迁移、Vue 日结工作台、容量、晚到事实、重建、兼容、回退和量化验收，
再进入正式实现。

Operations Owner 独占日结头、业务日快照引用、来源检查点、差异、预检结果、
审批/签署、状态历史、审计和 Outbox。只能通过 Foundation、Shift、Order、Promotion、
Payment、Refund、Sync、Inventory、Costing 和 Reporting 的版本化事件或明确只读端口取得
权威事实。禁止跨 Owner Mapper、任意 SQL、直接更新任何 Owner 事实，禁止用报表值
覆盖订单、班次、支付、退款、库存或成本。

日结身份必须绑定可信 tenant_id、store_id、business_date、IANA 时区、业务日起点、
close_version、各 Owner 检查点、内容 SHA-256、发起人、审批人和签署时间。客户端提交的
tenant_id、门店、金额、差异或完成状态均不得作为权威事实。

状态至少覆盖 DRAFT、PREFLIGHTING、PREFLIGHT_FAILED、READY、APPROVED、
CLOSING、CLOSED、FAILED、CORRECTION_REQUIRED 和 COMPENSATION_REQUIRED。开始人与审批/签署人
必须分离；同幂等键同内容返回原结果，同键异内容拒绝。CLOSED 事实只追加，
禁止删除或覆盖重开；晚到销售、退款、现金或同步事实必须生成显式差异和新的
更正/补结版本，不得改写已签署日结。

预检至少要求：所有必需班次已关闭，现金班次差异已确认，订单/取消/退货退款/
促销金额按已接受口径守恒，同步 Inbox/Outbox、死信、数据包和报表投影的检查点可解释。
存在未关班次、UNKNOWN 支付/退款、未处理资金差异、不可解释的同步缺口、摘要漂移、
权限或审计缺失时必须失败关闭。外部 Provider 未解阻时只能显示 BLOCKED/UNAVAILABLE，
不得伪造渠道对账通过。

金额使用最小货币单位整数并绑定币种；数量使用 DECIMAL/BigDecimal，禁止浮点数。
日结快照至少覆盖 gross、discount、surcharge、receivable、取消/退款、现金实收、
班次差异和 Provider 无关支付/退款内部事实，逐日、逐门店与权威事实守恒。

使用两个虚构租户、多组织、多门店、多终端和三业态合成数据；覆盖正常日结、
未关班次、现金差异、重复日结、同键异内容、并发审批、跨夜/时区/夏令时、业务日切换、
同步积压/死信、UNKNOWN、Owner 延迟、投影缺口、晚到销售/退款、更正版本、全量重建、
跨租户/门店越权、摘要篡改、百万级合成事实、迁移失败和安全前向修复。合成结果
不得写成生产 SLA。

CI 继续执行治理、Server、Web、MySQL、Flutter Linux/Windows、SQLite、Android/Kotlin、
日结故障、投影重建、租户权限、Secret/PII、依赖、SBOM、许可证、覆盖率和既有
Gate 全量回归；不得降低阈值、跳过测试、自动重跑掩盖 Flaky、修改已发布迁移
或创建测试后门。

T2-EXC-001 及 Gate 7D 后续需求继续 DRAFT；只有 T2-CLS-001 独立 VERIFIED 且经我
再次确认后，才可考虑准入 T2-EXC-001。T2-PAY-002、T2-HWD-001、T2-PRN-001、
T2-PAR-001 继续 BLOCKED；T2-JSH-001、T2-LIC-001 继续 DEFERRED。不得进行 Provider 网络、
真实资金、真实设备/外设、未经授权真实 PII/商户数据、伙伴现场执行、完整 Alpha、
生产部署或商业可用声明。

完成后提交《T2-CLS-001 独立周门禁报告》等待我确认，不得自动准入
T2-EXC-001、Gate 7D 后续需求或 Gate 7E。完成后为我整理下一步操作指令。
```
