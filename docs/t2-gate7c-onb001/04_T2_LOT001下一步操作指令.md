# T2-LOT-001 下一步操作指令

```text
我确认《T2-ONB-001 独立周门禁报告》，接受 T2-ONB-001
CONDITIONAL PASS。

同意将 T2-ONB-001 由 VERIFIED 更新为 ACCEPTED。明确该接受只覆盖门店开通
白名单快照、版本化行业模板绑定、独立审批、内部 Owner 检查、外部 P0 失败关闭和
Vue 向导的内部软件执行；不代表生产开店、支付沙箱、真实设备/打印、完整 Alpha、
现场试点、生产或商业验收。

按 CONDITIONAL GO 启动 T2 Gate 7C / Sprint S21-F：T2-LOT-001
社区超市可选批次、生产日期、保质期、到期日、FEFO 和临期预警的独立准备与开发。

以 T2-ONB-001 最终封存提交
[填写 T2-ONB-001 最终封存提交]
作为工作分支起点，创建独立分支：
t2/gate7c-sprint21f-lot001-lot-expiry

本批仅允许逐项准入 T2-LOT-001，并明确它只适用于 COMMUNITY_SUPERMARKET
行业模板；便利店和零食折扣店默认关闭。先冻结数据主权、能力开关、批次身份、
生产/入库/到期日期、保质期规则、收货拆批、销售扣批、FEFO、退货归批、盘点、调拨、
过期禁售、临期阈值、权限审计、幂等、API/事件、MySQL/SQLite迁移、数据包、
Flutter/Vue页面、容量、兼容、回退和量化验收，再进入正式实现。

Catalog Owner 只负责商品的批次/效期管理属性和版本化模板；Inventory Owner 独占
批次数量事实、批次流水和可重建投影。采购收货、销售、退货、盘点和调拨必须通过
既有 Owner 正式端口追加库存事实，禁止跨 Owner Mapper、直接修改库存余额或覆盖
历史流水。Costing 继续沿用已接受的仓级移动加权成本，不得引入批次成本。

批次标识必须绑定可信 tenant_id、仓库、SKU、来源单据与行、供应商批号或内部批号、
生产日期、到期日、单位和冻结换算。数量使用 DECIMAL/BigDecimal，禁止浮点数；
日期计算使用门店业务时区和冻结规则，不得由客户端自行决定过期状态。

商业 V1 只允许基础批次与效期：采购收货建批、销售按 FEFO 选择、原单退货优先归原批、
盘点/调拨保留批次来源、过期禁售和版本化临期预警。禁止批次成本、库位、波次、越库、
加工、序列号、冷链、质检、召回编排、自动报损和复杂 WMS。

离线 POS 必须安装带版本、摘要和适用门店的批次数据包；旧包、损坏包、跨租户包、
未知批次、批次余额不足、过期批次、日期歧义和摘要漂移必须失败关闭。离线成交必须
冻结所选批次和规则版本并与订单/库存 Outbox 原子提交；重启后复用原幂等键，禁止
按新批次重新生成业务命令。

使用两个虚构租户、多门店、多仓库和三业态合成数据，并证明非社区超市模板不会启用
批次路径。覆盖同日多批、跨日/跨月/闰年、无生产日期、有到期日、FEFO、过期禁售、
临期阈值、部分销售、原单退货归批、采购退货、盘点、调拨、重复/乱序/晚到、余额不足、
同键异内容、旧包重放、跨租户/仓库/批次污染、离线重启、迁移失败和安全前向修复。

CI 继续执行治理、服务端、Web、Flutter Linux/Windows、MySQL、SQLite、Android/Kotlin、
批次故障、租户权限、Secret/PII、依赖、SBOM、许可证、覆盖率和既有 Gate 全量回归；
不得降低阈值、跳过测试、自动重跑掩盖 Flaky、修改已发布迁移或创建测试后门。

T2-PAY-002、T2-HWD-001、T2-PRN-001、T2-PAR-001 继续 BLOCKED；
T2-JSH-001、T2-LIC-001 继续 DEFERRED；Gate 7D 需求继续 DRAFT。不得进行 Provider
网络、真实资金、真实设备/外设、未经授权真实 PII/商户数据、伙伴现场执行、完整 Alpha、
生产部署或商业可用声明。

完成后提交《T2-LOT-001 独立周门禁报告》等待我确认，不得自动进入 Gate 7D。
完成后为我整理下一步操作指令。
```
