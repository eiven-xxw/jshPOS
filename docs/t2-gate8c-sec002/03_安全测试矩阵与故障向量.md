# T2-SEC-002 安全测试矩阵与故障向量

| 域 | 自动化 | 关键断言 |
|---|---|---|
| 生产启动 | `ProductionSecurityConfigurationTest` | 完整配置可启动；缺 Secret、root、扩大端点、启用集成缺令牌均失败 |
| 配置静态契约 | `ProductionConfigurationContractTest` | 无已知默认凭据；受控引用、默认关闭、端点白名单存在 |
| 附件协议边界 | `ServiceOperationsControllerAttachmentTest` | 超限在打开流前拒绝；禁止 `getBytes()` |
| 附件资源边界 | `RuoYiServiceAttachmentStorageAdapterTest` | 10 MiB、超限、长度不符、SHA-256、24 并发、零残留 |
| 应用事务 | `ServiceApplicationServiceTest` | 幂等、对象键、事务回滚清理、元数据一致 |
| 正式 API 回归 | `CommercialSaasOperationsFormalApiE2ETest` | 正式服务旅程继续通过流式端口 |

固定向量以 `contracts/t2/gate8c-sec002/fixed-vectors.json` 为机器可读权威。任一失败不得自动重跑掩盖，必须固定输入、修复并从新提交完整复跑。
