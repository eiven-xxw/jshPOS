# 第一批正式修复启动指令

项目发起人确认本次审计后，可直接给出以下指令：

```text
我确认《商业V1内部产品完整性审计报告》和《T2 Gate 9A-Prep 启动评审报告》，
接受 T2-CMP-001 STATIC_GOVERNANCE_AND_REPOSITORY_AUDIT CONDITIONAL PASS。

按 CONDITIONAL GO 启动 T2 Gate 9B / Sprint S27-A：G9A-R1 正式 API 与 OpenAPI
一致性修复。以 Gate 9A-Prep 最终封存提交作为起点创建独立分支。

本批只允许修复 G9A-API-P1-001，并复用已接受的 T2-API-001，不新增业务能力或
Requirement ID。先冻结 300 项 Controller 操作、当前 OpenAPI、Vue/Flutter 客户端调用、
operationId、错误码、权限、可信 tenant_id、幂等、版本和兼容矩阵，再逐 Owner 串行处理
64 项 Controller 无当前 OpenAPI与21项OpenAPI无精确Controller的差异。

每项差异必须分类为：补齐当前契约、修正实现、显式历史草案/替代契约或经审批删除的孤立能力；
禁止为归零而静默删除 Controller/契约、放宽权限、改变资金库存租户事实或把历史草案标为当前。
先补契约回归，再修改实现；已发布迁移不得修改。

退出标准：Controller/OpenAPI双向差异为0，operationId唯一，客户端页面调用均有正式服务端
实现，权限/租户/错误码/幂等/兼容测试和完整CI全绿。完成后提交《G9A-R1正式API契约修复
独立周门禁报告》等待我确认，不得自动进入G9A-R2、完整Alpha或生产发布。

所有外部BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED及零执行边界保持不变。
完成后为我整理下一步操作指令。
```
