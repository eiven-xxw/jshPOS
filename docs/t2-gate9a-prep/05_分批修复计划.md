# 分批修复计划

机器权威：`contracts/t2/gate9a-prep/repair-batches-v1.json`。所有批次严格串行，每批均需独立
设计准入、回归、`VERIFIED` 和项目发起人确认。

## G9A-R1：正式 API 与 OpenAPI 一致性

- 关联：`T2-API-001`、`G9A-API-P1-001`；
- 按业务 Owner 确认 64+21 项差异是契约遗漏、路径变量命名差异、历史草案还是实现错误；
- 先补回归和权威标记，再修改当前 OpenAPI 或实现；
- 退出：Controller/OpenAPI/客户端契约双向差异为 0，权限、租户、错误码和兼容回归全绿。

## G9A-R2：生产装配与演示面收敛

- 关联：`T2-CORE-001/T2-SEC-002/T2-RDY-001`、`G9A-ASM-P1-001`；
- 从商业构建和初始化数据移除 `ruoyi-demo`、演示页面及菜单；
- 对 generator/workflow 等非 V1 平台能力单独确认是否保留、默认关闭或拆分 profile；
- 退出：JAR/Web/SQL/SBOM/路由均无演示面且完整回归通过。

## G9A-R3：页面状态、权限与失败恢复

- 关联：既有 ADM/POS/UX 需求、`G9A-UI-P1-001`；
- 逐页核对权限、空态、错误、加载、重复点击、恢复、离线和业务日切换；
- 先补组件/Widget 失败用例，再修实现；全局拦截器可复用，但必须证明页面仍可恢复且不会重复命令；
- 退出：26 页逐项 `PASS`，不新增领域算法。

## G9A-R4：当前 22 Owner 三业态正式栈汇总回归

- 关联：`T2-E2E-004/T2-E2E-005/T2-INT-001`、`G9A-E2E-P1-001`；
- 串联 SAA→SUB→SVC 和三业态业务闭环；
- 使用正式 MySQL/Redis/JAR/HTTP、Flutter/SQLite、正式 API 和虚构数据；
- 退出：禁止数据库后门，固定故障 seed、守恒、备份恢复、升级回退、P0/P1=0，全 CI 可重复。

四批完成后仍不能解除 SANDBOX、REAL_DEVICE、REAL_PERIPHERAL、PILOT、UAT、许可证或生产阻断。
