# T2-ADM-001 独立验证报告

## 1. 结论

`T2-ADM-001` 达到 Gate 6D 内部证据上限，状态由 `IN_PROGRESS` 更新为 `VERIFIED`。该结论覆盖 Vue 正式运营界面、正式 API 边界和内部软件验证，不代表 `ACCEPTED`、真实商户数据、现场试点、完整 Alpha 或商业可用。

## 2. 能力与边界

- 首页已由框架演示页替换为经营工作台，只汇总当前可信会话可见的组织、门店、商品、模板和审计；部分 API 失败显示未知，不伪造成零。
- Foundation 页面覆盖组织、门店、业务日、行业模板和员工数据范围；账号与角色继续复用 RuoYi 正式能力。
- Catalog 页面覆盖分类、品牌、条码、多单位、价格版本、导入预检、逐行错误、原子发布和安全回退。
- 页面只调用正式 API；不接收或提交 `tenant_id`，不跨模块查询私表，不复制状态机，不计算金额、库存、成本或促销事实。
- 多单位表单强制一个主单位、正安全整数换算和商品内条码/单位唯一；服务端仍是最终数据主权与权限校验者。

## 3. 证据

| 证据 | 结果 |
|---|---|
| 实现候选 | `ea7c23a1dab26802f29d56acacde9c1c8f17d5c7` |
| GitHub Actions | Run `32361270390`，八个 Job 单次全部成功 |
| Web 测试 | 6 个测试文件、20 项测试通过 |
| TypeScript / ESLint / 生产构建 | 全部通过 |
| MySQL / Server | 全量门禁通过 |
| Flutter Linux / Windows / Android / SQLite | 既有全量回归通过 |
| 安全 / Secret / SBOM / 许可证 | 通过 |
| 证据索引 Artifact | `t2-gate6d-evidence-index`，ID `9403817648` |

## 4. 外部证据边界

真实 PII、Provider 网络、真实设备命令、现场试点和完整 Alpha UAT 均为 0。`T2-PAY-002`、`T2-HWD-001`、`T2-PAR-001` 继续 `BLOCKED`；本结论不得改变其状态或证据等级。

## 5. 串行准入决定

ADM-001 的独立验证条件已经满足，允许 `T2-E2E-001` 由 `DRAFT` 更新为 `IN_PROGRESS`。E2E 只能使用虚构租户、虚构终端、现金和已接受 Owner 的正式端口/契约，不得新增领域算法或形成外部验收结论。
