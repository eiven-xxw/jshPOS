# T2-POS-008 独立验证报告

## 1. 结论

`T2-POS-008` 达到 Gate 6D 内部证据上限，状态由 `IN_PROGRESS` 更新为 `VERIFIED`。该结论覆盖正式 Flutter 收银页面的软件与合成 UI 验证，不代表 `ACCEPTED`、真实外设、真实支付、现场试点或商业可用。

## 2. 能力与边界

- 正式应用端口覆盖扫码、搜索、加购、改量、重新报价、人工优惠、挂取单、现金成交、打印任务预览与同步状态。
- 页面只调用 `PosSaleController`，控制器只调用会话权限与 `PosSaleApplicationService`；presentation 无 SQLite、MethodChannel、租户参数或 Owner 私表访问。
- 现金成交使用 `saleRef + quoteFingerprint` 稳定幂等键；失败重试不创建第二条业务命令。
- 默认未配置组合根时失败关闭；打印只形成任务预览和 `FAKE_DEVICE_ADAPTER` 证据。
- 大触控、扫码枪键盘输入、F2/F4/F9、断网/积压、错误恢复和业务日提示已纳入页面。

## 3. 证据

| 证据 | 结果 |
|---|---|
| 实现候选 | `4675c7e5e32645044482e0b0b507e79ca80ab47a` |
| GitHub Actions | Run `32359246598`，八个 Job 单次全部成功 |
| Flutter 全量测试 | 102 项通过 |
| Controller / Widget | Controller 7 项、Widget 3 项通过 |
| Gate 6D 目标代码覆盖率 | 296/304，97.37%，门槛 90% |
| Flutter Linux / Windows | 双平台通过 |
| Android / Kotlin / SQLite | 编译、APK 与既有迁移全量回归通过 |
| MySQL / Server / Web | 全量门禁通过 |
| 安全 / Secret / SBOM / 许可证 | 通过 |
| 证据索引 Artifact | `t2-gate6d-evidence-index`，ID `9403082517` |

## 4. 外部证据边界

Provider 网络、真实设备命令、真实打印、真实资金、现场试点和完整 Alpha UAT 均为 0。`T2-PAY-002`、`T2-HWD-001`、`T2-PAR-001` 继续 `BLOCKED`；本结论不得改变其证据等级。

## 5. 串行准入决定

POS-008 的独立验证条件已经满足，允许 `T2-ADM-001` 由 `DRAFT` 更新为 `IN_PROGRESS`。`T2-E2E-001` 继续保持 `DRAFT`，在 ADM-001 独立 VERIFIED 前不得建立正式 E2E 运行时。
