# T2-POS-007 独立验证报告

## 1. 结论

`T2-POS-007` 达到 Gate 6D 内部证据上限，状态由 `IN_PROGRESS` 更新为 `VERIFIED`。该结论只覆盖 Flutter POS 应用壳、可信终端失败关闭、员工登录、权限、门店/终端/班次/业务日上下文和安全退出的软件验证，不代表 `ACCEPTED`、`REAL_DEVICE`、现场试点或商业验收。

## 2. 实现边界

- 页面只调用 `PosSessionService`，未直接访问 SQLite、MethodChannel 或构造领域事实。
- `tenant_id`、门店、终端、业务日和批准能力只来自 `PosSessionRepository.verifyTerminal` 返回的可信上下文。
- 默认生产仓储在可信通信尚未接入时失败关闭；不存在绿色占位或本地自报租户解锁路径。
- 重复登录与退出使用单航班；异常消息不回显底层响应、口令或设备凭据。
- 终端停用/过期、能力不匹配、会话过期、班次与业务日冲突、跨租户刷新和权限不足均失败关闭。

## 3. 证据

| 证据 | 结果 |
|---|---|
| 实现候选 | `e234d85f2f2c2ad83d702c9aaab8d2271f3ab57c` |
| GitHub Actions | Run `32357245349`，八个 Job 单次全部成功 |
| Flutter 会话测试 | 19 项通过 |
| Gate 6D 目标代码覆盖率 | 161/162，99.38%，门槛 90% |
| Web / MySQL / Server | 全量门禁通过 |
| Flutter Linux / Windows | 双平台通过 |
| Android / SQLite / 既有 Gate | 随 POS 与服务端全量回归通过 |
| 安全 / Secret / SBOM / 许可证 | 通过 |
| 证据索引 Artifact | `t2-gate6d-evidence-index`，ID `9402368900` |

失败 Run `32355937056`、`32356409883`、`32356617979` 及其红灯原因继续保留。修复未降低格式、Lint、测试、覆盖率或安全阈值，也未重跑单个失败 Job 掩盖问题。

## 4. 外部证据边界

- `T2-HWD-001` 仍为 `BLOCKED`，虚构终端和软件设备快照不构成实机认证。
- Provider 网络调用、真实设备命令、现场试点、完整 Alpha UAT 均为 0。
- `T2-POS-007` 等待 Gate 6D 总体验收前保持 `VERIFIED`，不得提前改为 `ACCEPTED`。

## 5. 串行准入决定

POS-007 的独立验证条件已经满足，允许 `T2-POS-008` 由 `DRAFT` 更新为 `IN_PROGRESS`。`T2-ADM-001` 与 `T2-E2E-001` 继续保持 `DRAFT`，不得预建其正式运行时。
