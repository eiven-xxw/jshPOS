# T2-E2E-001 独立验证报告

## 1. 结论

`T2-E2E-001` 达到 Gate 6D 内部证据上限，状态由 `IN_PROGRESS` 更新为 `VERIFIED`。该结论只证明正式 Owner 单测、跨端组件测试和固定合成现金旅程可以在同一提交上组合收敛，不代表 `ACCEPTED`、支付沙箱、真实设备、现场试点、完整 Alpha、生产或商业可用。

## 2. 闭环与守恒

- 两个虚构租户各覆盖便利店、零食折扣店和社区超市，共六门店、六虚构终端和六条固定现金旅程。
- 每条旅程覆盖初始化、商品价格包、班次、购物篮、促销/人工优惠、现金成交、订单/Outbox、同步 ACK、库存、成本、报表和班次关闭。
- 逐旅程核对 `gross - discount + surcharge = receivable`、找零、订单状态、原 Outbox 身份、库存数量、冻结销售成本、报表逐字段及班次现金守恒。
- 固定攻击矩阵覆盖重复请求、同键异内容、跨租户替换、摘要篡改、ACK 丢失和重放；Provider 网络、真实设备命令、现场试点和完整 Alpha 执行数均为 0。

## 3. 机器证据

| 证据 | 结果 |
|---|---|
| 实现及修复候选 | `f5a450ded88dcbdb2266d77391fafe5477bb0b2a` |
| GitHub Actions | Run `32366111390`，九个 Job 全部成功 |
| Server Owner | 12 个冻结测试套件及方法成功 |
| Flutter | Linux、Windows 各 6 个冻结用例成功且未跳过 |
| Web | 3 个冻结组件用例成功且未跳过 |
| MySQL | `ReleaseMigrationMySqlIT` 全迁移验证成功 |
| 合成旅程 | 6/6 PASS，攻击向量 6/6 按预期拒绝或返回原结果 |
| E2E Artifact | `t2-gate6d-internal-e2e`，ID `9405572867`，SHA-256 `cf36abf7e049174b68e5835ea0bb2b3be97e372d0f728df7db4d12de9f3955d5` |
| 证据索引 | `t2-gate6d-evidence-index`，ID `9405603585`，SHA-256 `0b55e9238b297b3ab2a535f0845cf91eb2fb56d0d529e63061c4155a28041126` |

## 4. 失败证据保留

- Run `32363250672` 因 Web JUnit 文件命名和限定测试名解析缺陷失败；六个前置 Job 与安全成功，E2E Artifact 未生成。
- Run `32364716110` 因 Flutter 扩展事件数组未按协议解析失败；六个前置 Job 与安全成功，E2E Artifact 未生成。
- 两次修复都保留原失败 Run，未删除冻结用例、未允许失败或跳过、未自动重跑失败 Job、未降低覆盖率或安全阈值。最终解析器分别核验两平台的成功完成事件，比原合并启动名规则更严格。

## 5. 状态边界

`T2-POS-007`、`T2-POS-008`、`T2-ADM-001`、`T2-E2E-001` 均保持 `VERIFIED`，等待项目发起人对 Gate 6D 总体验收；不得提前改为 `ACCEPTED`。外部三项 P0、完整 Alpha 和发布状态均未改变。
