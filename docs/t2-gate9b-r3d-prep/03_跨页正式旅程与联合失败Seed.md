# 跨页正式旅程与联合失败 Seed

## 1. 冻结旅程

### R3D-JNY-WEB-01：后台主数据到经营报表

`VUE-01/02/03/04 → VUE-05/06/07/08/10/11/12/13/14/15 → VUE-16`

验证可信租户、组织/门店、权限、业务日、危险操作和原操作恢复在页面切换后不漂移。

### R3D-JNY-WEB-02：商业运营与发布治理

`VUE-17 → VUE-19 → VUE-18 → VUE-20 → VUE-09`

验证平台角色、商户租户、套餐权益、订阅访问模式、附件/凭据边界和发布失败关闭。

### R3D-JNY-POS-01：可信 POS 会话与辅助交易

`FLT-04 → FLT-03 → FLT-06 → FLT-05 → FLT-02 → FLT-01`

验证租户、门店、终端、员工、班次、业务日、原订单和原幂等键连续；电子支付和真实钱箱
继续 `BLOCKED_EXTERNAL`。

## 2. 联合失败 seed

| Seed | 严重级别 | 缺口 | 正式 R3D 退出要求 |
|---|---|---|---|
| R3D-JNT-P1-001 | P1 | 缺少同一 Router/可信上下文串联 VUE-01..16 的联合挂载旅程 | 跨页切换中权限、范围、状态和恢复连续，负向测试可复现 |
| R3D-JNT-P1-002 | P1 | 缺少串联 SaaS、订阅、服务、终端和发布的联合旅程 | 服务端权限与外部失败关闭边界逐跳保持 |
| R3D-JNT-P1-003 | P1 | 缺少同一可信会话遍历六个 POS 页面的 Widget 旅程 | 班次、业务日、原命令和离线恢复逐页一致 |

准备阶段仅固定 seed，不创建联合测试或修改运行时。当前 `P0=0`、`P1=3`，所以
`G9A-UI-P1-001` 继续 `OPEN`。
