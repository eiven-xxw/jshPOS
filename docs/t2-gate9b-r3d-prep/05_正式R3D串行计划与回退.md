# 正式 R3D 串行计划与回退

本文件只是待批计划，不构成运行时授权。

| 顺序 | 工作 | 退出条件 |
|---|---|---|
| R3D-R0 | 冻结三份来源摘要、26页清单、联合测试底座 | 历史证据不可变，先补失败回归 |
| R3D-R1 | 关闭 R3D-JNT-P1-001 | VUE-01..16 在同一 Router/可信上下文中联合通过 |
| R3D-R2 | 关闭 R3D-JNT-P1-002 | VUE-17/19/18/20/09 权益、权限和失败关闭联合通过 |
| R3D-R3 | 关闭 R3D-JNT-P1-003 | 六个 POS 页面在同一可信会话中联合通过 |
| R3D-R4 | 全部26页联合负向回归、双平台CI和证据封存 | P0/P1=0，形成 Finding 关闭候选 |

正式阶段仍应优先新增测试；只有失败回归证明既有运行时缺陷时，才允许在获批页面范围内做
最小修复。任何 API、Controller、依赖、数据库或迁移需求必须停止并单独评审。

回退为删除未获接受的联合测试/治理提交并返回准备封存提交；不得回退或改写 R3A/R3B/R3C
历史代码、迁移或证据。
