# T2-PAY-002 支付沙箱执行准入评审报告

## 1. 评审结论

`NOT_ACHIEVED / BLOCKED / NO-GO`。拉卡拉仍只是首接受控文档候选；实际受控材料 ID 为 `0/11`，不得进行域名探测、SDK/HTTP 调用、回调接收、账单下载或资金交易。

## 2. 必需材料

| ID | 材料 | 当前状态 | 通过标准 |
|---|---|---|---|
| PAY-AUTH | 授权沙箱和自动化授权 | `MISSING` | 签署主体、环境、期限和允许用例可复核 |
| PAY-MERCHANT | 测试商户 | `MISSING` | 适用产品、权限和非生产属性明确 |
| PAY-TERMINAL | 测试终端 | `MISSING` | 终端绑定、生命周期和责任边界明确 |
| PAY-API | 正式版本化接口 | `MISSING` | 支付、查询、退款、错误码和变更政策锁定 |
| PAY-SIGN | 签名验签 | `MISSING` | 算法、证书、轮换、防重放和时钟规则闭环 |
| PAY-CALLBACK | 回调 | `MISSING` | 域名、验签、幂等、重试、乱序和停止规则闭环 |
| PAY-BILL | 账单 | `MISSING` | 格式、样例、下载窗口、差异和对账口径闭环 |
| PAY-LIMIT | 限流与维护 | `MISSING` | 配额、超时、维护窗口和升级路径明确 |
| PAY-NETWORK | 网络白名单 | `MISSING` | 出口、回调、DNS/TLS 和责任边界明确 |
| PAY-SECRET | Secret 系统引用 | `MISSING` | 只登记引用；轮换、到期、吊销和审计闭环 |
| PAY-CONTACT | 技术/商务联系人 | `MISSING` | 受控目录可验证且职责/升级路径明确 |

## 3. 执行前最小矩阵

资料齐备后仍须另行评审：付款成功/失败/超时/UNKNOWN、原请求查询收敛、重复乱序回调、同键异内容、部分/最终退款、退款 UNKNOWN、账单缺失/重复/差异、签名错误、证书轮换、限流和网络中断。任何 UNKNOWN 都不得通过新建支付命令解决。

## 4. 安全和停止条件

- Secret、商户号、终端号、证书和原始支付报文不得进入 Git、日志、CI 或普通 Artifact。
- 只允许专用沙箱测试商户、测试终端和批准出口；真实资金固定为 0。
- 疑似生产凭据、授权/摘要冲突、超范围产品或材料过期时立即 `NO-GO`。
- 即使整包达到 `VERIFIED_DOCUMENT`，也只能提交更新后的独立执行准入报告；项目发起人确认前网络调用仍为 0。

## 5. RACI 与下一决定

支付负责人负责材料映射和执行矩阵，安全负责人核验 Secret/网络，财务负责人核验账单口径，QA 负责证据，项目发起人最终批准。当前下一动作仅为通过受控渠道补齐 11 项元数据。
