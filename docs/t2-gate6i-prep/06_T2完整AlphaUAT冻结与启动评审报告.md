# T2 完整 Alpha UAT 冻结与启动评审报告

## 1. 结论

计划、矩阵和判定规则已冻结；启动结论仍为 `DRAFT / NO-GO_FULL_ALPHA`。Gate 6H 内部候选是软件输入，不是完整 Alpha 证据。`T2-PAY-002/HWD-001/PRN-001/PAR-001` 未经独立执行准入确认前，不得启动完整 Alpha。

## 2. 候选身份

- Commit：`e2bd699ad565845c363ffe478320eb2b3f9a0015`
- GitHub Run：`32471050486`，12 个作业成功
- Artifact：`9443017031`
- Artifact SHA-256：`9cc79a2248d780c4f818ac811014b67c9f0cca066a1022a058b7685696c07418`
- 证据等级：`INTERNAL_RELEASE_CANDIDATE`，不升级为 `SANDBOX/REAL_DEVICE/PILOT/FULL_ALPHA`

## 3. 环境拓扑冻结

| 区域 | 组成 | 关键约束 |
|---|---|---|
| 控制面 | 独立测试租户、组织、门店、终端注册、审计和可观测性 | 与开发/生产隔离；可信租户和数据范围失败关闭 |
| 服务面 | 固定候选服务端、Web、MySQL、Redis、对象存储和签名制品 | 版本/迁移/SBOM/签名与候选身份一致 |
| POS 面 | 主认证与兼容 Android、每店至少两终端、正式 SQLite | 只使用批准样机；日志诊断脱敏 |
| 支付面 | 授权拉卡拉沙箱、测试商户/终端、批准出口与回调入口 | 生产域名/密钥/资金禁止；UNKNOWN 不二次扣款 |
| 外设面 | 两打印、扫码、秤、钱箱、客显 | 精确型号、协议、权限和恢复矩阵已批准 |
| 伙伴面 | 已授权伙伴门店、脱敏样本和旧系统对账输入 | 目的限制、保留删除与现场责任已签署 |
| 证据面 | 只读证据库、SHA-256、角色签署、缺陷账和数据清除证明 | Secret/PII/受控原文不进入普通 Artifact |

环境当前为 `FROZEN_NOT_PROVISIONED`，不得据此声明环境已就绪。

## 4. 商户、门店和终端矩阵

| 不透明商户 | 业态 | 最小门店 | 每店终端 | 当前 |
|---|---|---:|---:|---|
| UAT-MER-01 | 便利店 | 2 | 2 | `BLOCKED_EXTERNAL_INPUT` |
| UAT-MER-02 | 零食折扣店 | 1 | 2 | `BLOCKED_EXTERNAL_INPUT` |
| UAT-MER-03 | 社区超市 | 1 | 2 | `BLOCKED_EXTERNAL_INPUT` |

真实主体必须由伙伴受控包映射；表中不透明 ID 不能当成伙伴证据。

## 5. 强制测试域

1. 租户、组织、门店、员工权限、数据范围和审计；
2. 商品、条码、多单位、导入、价格版本和签名数据包；
3. 登录、开班、扫码、促销、人工优惠、挂取单、现金结算和关班；
4. 拉卡拉支付/退款成功、失败、超时、UNKNOWN、查询、回调、账单与对账；
5. 主认证/兼容机、两打印、扫码、秤、钱箱、客显和长稳；
6. 断网、重复、乱序、ACK 丢失、游标损坏、重启和积压恢复；
7. 库存、盘点、采购、调拨、成本、退货和逐事实守恒；
8. 促销快照、金额守恒、退款原快照恢复；
9. 会员隐私、同意、等级、积分、撤回和删除限制；
10. 报表投影重建、逐日对账、脱敏导出和 CSV/对象存储攻击；
11. 服务端/Web/Schema/数据包/APK 灰度、待同步保护、回退和前向修复；
12. 备份、恢复、PITR 边界、RPO/RTO、投影和游标核对；
13. 百万级合成/授权脱敏容量、安全、Secret、依赖、SBOM 和可观测性；
14. 伙伴旧系统逐日对账、数据清除和现场退出。

## 6. 故障与一致性矩阵

必须固定并保留重复请求、同键异内容、回调乱序、ACK 丢失、进程终止、磁盘满/写失败、网络抖动、业务日切换、时钟偏移、跨租户替换、摘要/签名错误、设备吊销、缺纸/断连、秤异常、部分 Owner 失败、迁移中断、恢复中断和旧客户端兼容等 seed。失败不能通过自动重跑隐藏；修复须先补回归并重跑完整相关矩阵。

## 7. 缺陷、证据与签署

- P0：资金重复/丢失、跨租户泄漏、库存成本不可重建、数据不可恢复、关键交易不可用或不能安全回退。
- P1：核心旅程无可接受替代、对账不守恒、主设备/支付不稳定、关键权限审计缺失或恢复目标未达。
- 启动：四项外部需求分别取得执行准入确认；环境、数据、角色、停止、清除和回退就绪。
- 接受：P0=0、P1=0，强制用例证据合格，无未解释资金/库存/租户差异，项目发起人、产品、架构、QA、安全、SRE、支付、设备和伙伴责任人签署。
- 证据必须标明 `SANDBOX/REAL_DEVICE/PILOT/FULL_ALPHA` 等级，不得把内部软件执行升级标注。

## 8. 数据保留和销毁

每个数据集必须绑定目的、最小字段、授权、加密、访问人、保留期限和销毁方法。UAT 结束或撤回时停止写入，吊销 Secret/证书，清除终端本地库、对象、缓存、临时文件和日志中的授权测试数据，并保存不含原数据的删除证明与审计。

## 9. 当前 Go/No-Go

支付 `0/11`、硬件打印 `0/10`、伙伴 `0/5` 与 `0/3`，因此完整 Alpha Run 必须保持 `0`。许可证 `0/3` 继续阻断生产/商业发布。下一步只能逐轨补件并分别提交执行准入评审。
