# 下一步操作指令（待 G9A-R2 项目发起人确认后使用）

```text
我确认《G9A-R2 生产装配与演示面收敛独立周门禁报告》，接受
G9A-ASM-P1-001 CONDITIONAL PASS，并同意将该缺陷确认为 CLOSED_IN_GATE9B。

明确该接受只覆盖商业默认 Maven reactor、JAR、Web、动态路由、四方言初始化、
SBOM 和生产配置中的 demo/generator/workflow/job 收敛，以及内部软件回归；
不代表支付沙箱、真实设备/外设、完整 Alpha、生产、商业验收或商业 SLA。

按 CONDITIONAL GO 启动 G9A-R3 页面状态、权限与失败恢复独立准备阶段。
以 G9A-R2 最终封板提交作为起点创建独立准备分支。本阶段只允许修复
G9A-UI-P1-001 的准备审计，复用既有 ADM/POS/UX 已接受需求，不新增业务能力。

先冻结 26 个 Vue/Flutter 正式页面清单，逐页核验路由、按钮、权限、数据范围、
加载、空态、错误提示、防重复点击、离线、业务日切换和失败恢复，并形成直接
组件/Widget 测试缺口、修复批次、验收矩阵和启动评审报告。未经我再次确认，
不得进入正式页面整改、G9A-R4、完整 Alpha 或生产发布。

所有外部 BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED 及零执行边界保持不变。
完成后为我整理下一步操作指令。
```
