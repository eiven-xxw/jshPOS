# G10A-SQL-P2-001 可执行基线报告

## 评审结论

**EXECUTABLE_BASELINE_VERIFIED_AWAITING_SPONSOR_CONFIRMATION**

MySQL 8.4.11 干净执行器已经完成 10k/100k 全部 12 条查询、获批 1m 单项趋势、
JDBC 查询数、租户权限攻击和完整 CI。该结论只确认“红基线可执行、可重复、证据完整”，
不代表 SQL 性能已经达标，也不授权修改生产 SQL、Mapper、索引或迁移。

G10A-SQL-P2-001 继续 OPEN，等待项目发起人确认本报告和下一整改准备批次。

## 执行结果

| 项目 | 结果 |
|---|---|
| 数据库 | MySQL 8.4.11，utf8mb4 / utf8mb4_0900_ai_ci |
| Schema | 22 Owner，Flyway V1—V87 |
| 查询层级执行 | 25 次：12×10k、12×100k、1×获批 1m 趋势 |
| 100k 分查询结论 | 3 项需兼容性 CR；9 项需运行时计划复核；0 项可直接 GO |
| JDBC 查询数 | 报表导出 150；支付对账 501；批次临期 501 |
| 权限边界 | 只读账号可查询、写入拒绝、跨租户攻击返回 0 行 |
| 生产代码/SQL/索引/配置变化 | 0 |
| 已发布迁移变化 | 0 |
| 外部执行 | 0 |

## 1m 获批趋势

只执行 RPT-SALES。返回 480,000 行，计划观察到全表扫描和 filesort；该结果只标记
SYNTHETIC_TREND_ONLY，不得换算为生产容量或商业 SLA。

## 历史失败与修复

- Run 32962757649：失败。租户攻击测试只替换第一个租户占位符，PAY-FACTS 的
  UNION 分支仍使用原租户。归类为测试探针缺陷，不是生产 SQL 越权；失败 Run 永久保留。
- Run 32964662824：修复测试探针后首次完成干净可执行基线。
- Run 32965805674：完成紧凑证据输出，但原始步骤日志仍不利于核验。
- Run 32967025590：最终证据 Run，11 个 Job 全绿，按查询输出有界证据并上传原始 Artifact。

没有通过重跑失败 Job、删除用例或降低门禁掩盖失败。

## 仍然开放的风险

1. RPT-SALES、RPT-INVENTORY、RPT-PAY-REC 涉及分页/导出响应兼容性，
   任何运行时调整前必须独立 CR。
2. 其余 9 条查询需要比较候选 SQL/索引、写放大、容量和租户权限影响，不能按
   “出现 filesort”机械增加索引。
3. 150/501/501 查询数表明三条旅程存在确定性线性放大，需在 Owner 端口边界内设计批量读取，
   禁止跨 Owner Mapper。
4. G10A-RES-P2-001 继续 PREPARED，不得提前整改。
5. 外部 PAY/HWD/PRN/PAR、UAT/REL、LIC/JSH 及全部零执行边界不变。

## 门禁结论

- P0/P1：0（本批测试基础设施范围）。
- P2：G10A-SQL-P2-001 继续 OPEN。
- 建议：CONDITIONAL PASS，只建议进入 SQL、分页与 N+1 的独立整改准备；未经再次确认，
  不得修改生产 SQL、Mapper、索引、数据库对象或迁移。
