# 下一步操作指令

待项目发起人确认本次可执行红基线后，可复制以下指令：

~~~text
我确认《G10A-SQL-P2-001 可执行基线报告》，接受 Gate 10A-R2-R2-R1
EXECUTABLE_BASELINE CONDITIONAL PASS。

明确 G10A-SQL-P2-001 继续 OPEN；该接受只确认 MySQL 8.4.11、固定合成数据和
干净执行器下的计划、查询数、权限与证据红基线，不代表性能达标、生产容量或商业 SLA。

按 CONDITIONAL GO 启动 Gate 10A-R2-R2-R2：
SQL、分页与 N+1 精确整改准备。

本准备阶段只允许：

1. 为 RPT-SALES、RPT-INVENTORY、RPT-PAY-REC 分别形成分页/导出兼容性 CR、
   API 影响、旧客户端兼容和回退方案；
2. 为其余 9 条查询冻结候选 SQL/索引、EXPLAIN 对比、写放大、容量、租户权限和
   前向迁移影响；
3. 为 150/501/501 三组线性查询放大设计 Owner 端口内批量读取方案，
   禁止跨 Owner Mapper；
4. 准备失败测试、固定数据、计划/查询数阈值、租户攻击、迁移与回退验收矩阵；
5. 提交《Gate 10A-R2-R2-R2 启动评审报告》等待我确认。

准备阶段不得修改生产 Java、SQL、Mapper/XML、索引、数据库对象或迁移；
不得自动进入正式整改。G10A-RES-P2-001 继续 PREPARED，R3、外部执行、
完整 Alpha 和生产发布继续禁止，全部既有状态和零执行边界保持不变。

完成后为我整理下一步操作指令。
~~~
