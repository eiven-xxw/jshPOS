# 生产路径、装配、迁移、备份、发布与供应链复审

## 1. 生产路径临时标记

扫描 Fake、Mock、Stub、Locked、InMemory、TODO、FIXME、空实现及未实现异常：

- 已分类：21 处；
- 未分类：0；
- 分类包括数据库锁定局部变量、确定性促销锁定状态、POS 失败关闭会话、测试专用内存 SQLite、
  Windows ACL 兼容分支和未提供平台插件时的失败关闭接口；
- 没有把外部支付或设备端口伪造为成功。

## 2. 商业装配

G9A-R2 当前态复审为 `reactor/jar/web/sql/sbom=0`：

- `ruoyi-demo` 不进入商业默认 reactor/JAR/Web/初始化/SBOM；
- generator/workflow/job 不进入商业默认制品；
- 22 Owner 进入商业模块化单体装配；
- 300 正式 API 保持不变。

## 3. 迁移与数据

- Gate 9C 相对基线的 Server/SQLite/迁移变化必须为 0；
- V18 及其他已发布迁移保持不可变；
- V87 已由 G9A-R4 在 MySQL 8.4 完成空库、V86 升级、重复执行和 validate；
- Gate 9C 不增加表、索引、种子或数据修复脚本。

## 4. 备份、发布与运维

- 内部备份恢复、发布清单、摘要、临时软件签名、回退和诊断已由已接受需求覆盖；
- 当前证据仍是 `SYNTHETIC_RESTORE`、临时软件签名和固定 GitHub 执行器性能；
- 真实 KMS、生产 PITR、跨区域灾备和商业 SLA 没有升级证据等级。

## 5. SBOM、依赖与许可证

- Gate 9C 相对基线依赖变化为 0，并重新生成 Server SBOM、Web/Flutter 供应链清单；
- 内部 readiness P0/P1 为 0；
- `T2-LIC-001` 三项商业许可证关闭证据仍未完成，商业发布继续 `NO-GO`；
- 不以“已知风险”或内部 SBOM 代替法务/替换关闭。
