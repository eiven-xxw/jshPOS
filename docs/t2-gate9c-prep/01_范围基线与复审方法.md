# 范围、基线与复审方法

## 1. 基线

- 起点提交：`1e5807691df9f857fc5fc223244e1e97d5363174`；
- 分支：`t2/gate9c-prep-v1-product-completeness`；
- 复用需求：`T2-CMP-001`；
- 历史 Gate 9A 缺陷账、失败 Run、CR 和 Gate 9B 来源报告只读保留。

Gate 9A 审计时 `ACCEPTED` 为 87 项；后续 `T2-RDY-001` 经项目发起人确认后，当前数量为
88 项。Gate 9C 使用新的当前态审计器，不改写冻结为 87 项的历史审计器。

## 2. 允许范围

- 更新 `G9A-E2E-P1-001` 当前关闭账与项目发起人接受记录；
- 汇总 Gate 9B 四项 Finding 的候选提交、CI、制品和接受证据；
- 只读扫描 88 Requirement、22 Owner、300 API、26 页面、三业态及 SAA/SUB/SVC；
- 复跑 Server、Web、Flutter 和治理基线；
- 形成差距、封板计划、启动评审和下一步建议。

## 3. 禁止范围

- 不修改 Server、Vue、Flutter、Kotlin/Android、基础设施、依赖或已发布迁移；
- 不新增业务能力、Requirement ID、Controller、表、页面或任务；
- 不调用 Provider 网络，不执行真实资金、设备/外设、伙伴现场、完整 Alpha 或生产部署；
- 不创建或移动 tag。

## 4. 判定方法

机器审计链为：

`RTM → Owner 装配 → Controller/OpenAPI → Vue/Flutter 页面 → 生产临时标记分类 →
四项 Finding 关闭账 → 外部状态与零执行边界 → 多平台基线回归 → 证据索引`。

发现既有 P0/P1 只能登记独立修复批次；发现新业务必须另行 CR 与 Requirement ID。
