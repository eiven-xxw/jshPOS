# 逐页缺口与失败种子

## 批次结论

- P0：0；
- P1：8 组；
- 整体缺陷：`G9A-UI-P1-001 OPEN`；
- 这些是既有已接受需求的产品完整性缺陷，不新增 Requirement ID。

## 逐页缺口

| 页面 | 当前可复现缺口 | 后续最小关闭证据 |
|---|---|---|
| VUE-16 | 无直接测试；导出审批/生成、对账处置每次产生新操作标识；缺少页内空错态、单航班和完整高风险确认 | 查询、导出、审批、生成、短期下载、对账处置 mount 测试；原键恢复；独立权限和范围拒绝 |
| VUE-17 | 无直接测试；具备稳定键缓存，但开户、权益发布、技术租户创建和生命周期缺少完整页内错误及影响确认 | 职责分离、可信 tenant 分配、配额、历史保留和失败原键 mount 测试 |
| VUE-18 | 无直接测试；附件与状态命令虽保存原键，但目录发布、工单流转、正文清理缺少确认和安全错误证据 | 门店范围、租约、10MiB、短期下载到期、正文清理、对象键越权 mount 测试 |
| VUE-19 | 无直接测试；具备稳定键缓存，但宽限、降级、暂停、恢复和终止没有页内安全错误及影响确认 | 服务端时钟、重复任务、历史保留、终止二次确认和原键 mount 测试 |
| VUE-20 | 无直接测试；签发和状态变更在每次点击生成新键；一次性秘密缺少关闭清空/失败证据 | 签发/状态/轮换原键、秘密单次显示、不持久化、克隆吊销和外部阻断 mount 测试 |
| FLT-01 | 局部 Widget 已证明两腿边界和 UNKNOWN 查询，但缺少会话导航、业务日变化和离线重启组合 | 权限许可/拒绝、旧上下文失败、原检查点恢复、无净额事实 Widget/E2E |
| FLT-02 | 局部 Widget 已证明原单退款和 UNKNOWN 查询，但缺少完整会话、离线、业务日和权限组合 | 部分/最终退款、原快照、旧业务日、离线重启和会话权限 Widget/E2E |
| FLT-05 | 无 Widget；失败重试通过时间生成新键，可能形成第二条现金/钱箱命令 | 页面级单航班、原键、权限、旧班次/业务日、BLOCKED外设 Widget 测试 |

机器可执行定义见 `contracts/t2/gate9b-r3c-prep/failure-seeds-v1.json`。后续只能以失败
测试先红、最小修复后绿的方式关闭；全局错误拦截器、源码断言或服务端单测不得替代页面证据。
