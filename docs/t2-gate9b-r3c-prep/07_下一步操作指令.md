# G9A-R3C 正式页面整改操作指令

```text
我确认《G9A-R3C 启动评审报告》，接受准备阶段 CONDITIONAL PASS。

按 CONDITIONAL GO 启动 G9A-R3C：报表、终端、商业运营与 POS 辅助页正式整改。

以 G9A-R3C 准备阶段最终封存提交
[填写 G9A-R3C Prep 最终封存提交]
作为起点创建独立分支：
t2/gate9b-sprint27e-g9a-r3c-runtime

本批只允许关闭 VUE-16 至 VUE-20、FLT-01、FLT-02、FLT-05 对应的既有
G9A-UI-P1-001 缺口，复用已接受 Requirement 和 ADR-071，不新增业务能力或
Requirement ID。

严格按 R3C-R0 → R1 → R2 → R3 → R4 → R5 → R6 → R7 → R8 → R9
串行。先补可复现失败测试，再做最小页面修复；前一步未独立 VERIFIED，后一步不得铺开。

重点关闭：五个 Vue 页面直接挂载测试；VUE-16 导出/对账权限和原键；VUE-18 附件权限、
短期下载与清理；VUE-20 一次性秘密、终端状态和原键；VUE-17/19 商业生命周期确认与
失败恢复；FLT-05 原幂等键、权限、业务日与外设阻断；FLT-01/02 会话导航、离线、业务日
和 UNKNOWN 原检查点联合证据。

页面只能调用既有正式 API 或应用端口；服务端可信 tenant、门店、终端、员工和业务日
继续最终授权。禁止修改 Controller/API、Owner 算法、依赖、数据库和已发布迁移，禁止
跨 Owner Mapper、前端重算、Provider SDK/HTTP、真实支付或设备命令。

完成后提交《G9A-R3C 报表终端商业运营与POS辅助页整改独立周门禁报告》等待我确认。
G9A-UI-P1-001 继续 OPEN；不得自动进入 G9A-R3D、G9A-R4、完整 Alpha 或生产发布。

所有外部 BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED 和零执行边界保持不变。
```
