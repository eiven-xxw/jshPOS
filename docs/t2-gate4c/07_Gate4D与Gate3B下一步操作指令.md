# Gate 4D 与 Gate 3B 下一步操作指令

> 文档编号：JSH-POS-T2-G4C-007
> 使用条件：项目发起人确认《T2 Gate 4C / Sprint S7 周门禁报告》后复制执行

## 1. 建议决策

1. 接受 Gate 4C `CONDITIONAL PASS`，将 `T2-CST-001` 从 `VERIFIED` 更新为 `ACCEPTED`。
2. Gate 3B-Prep 继续只做真实资料核验；`T2-PAY-002` 保持 `BLOCKED`，未经独立解阻确认不得产生 Provider 网络调用。
3. 内部轨进入 Gate 4D / Sprint S8，只允许逐项准入 `T2-TRF-001` 基础调拨；Gate 5 的促销、会员和报表最多只做设计准备，不得正式实现。
4. 继续禁止真实支付、生产密钥、未脱敏数据、应付、发票、总账、批次成本、复杂配送 WMS 和后续 Gate 编码。

## 2. 可直接复制的下一步指令

```text
我确认《T2 Gate 4C / Sprint S7 周门禁报告》，接受 Gate 4C CONDITIONAL PASS。

同意将以下需求由 VERIFIED 更新为 ACCEPTED：

- T2-CST-001

T2-PAY-002 继续 BLOCKED。Gate 3B-Prep 继续只允许真实资料核验；未经我确认独立《支付沙箱解阻评审报告》，不得进行任何 Provider 网络调用。

按 CONDITIONAL GO 启动 T2 Gate 4D / Sprint S8，仅允许逐项准入：

- T2-TRF-001：基础调拨申请、审批、发出、在途、收货、差异、取消，以及来源/目的库存与成本的幂等可对账闭环

T2-PRM-001、T2-PRM-002、T2-PRM-003 本 Sprint 只允许补充领域模型、状态机、不变量、API/事件、优惠快照与分摊契约、固定合成向量和验收用例，不允许正式实现。会员、报表及后续 Gate 继续保持 DRAFT。

所有调拨数量效果必须通过已接受的库存 Owner 生成不可变流水，禁止直接更新库存余额或修改历史流水。发出与收货必须是两个独立幂等事实，不得用单次跨仓事务伪造原子调拨；在途数量必须可对账，部分收货、短少、溢余、拒收和取消边界必须明确。

所有调拨成本效果必须通过已接受的成本 Owner 生成只追加流水。目的仓收货成本必须继承来源仓发出成本快照并参与目的仓移动加权，禁止按目的仓当前平均成本伪造调拨成本；差异、更正与退回必须使用新事实或冲正表达，禁止回写历史成本或数量流水。

先冻结数据主权、状态机、权限与职责分离、审计、幂等键、内容摘要、并发顺序、业务日、API/事件、Flyway、容量、监控、回退和安全前向修复，再进入实现。数量与成本继续使用 DECIMAL/BigDecimal，禁止浮点数。

继续使用两个虚构租户、多门店、多仓库和三类业态合成数据；覆盖重复/乱序/晚到、同键异内容、并发发出与收货、部分收货、超收拒绝、短少审批、在途积压、取消边界、跨业务日、成本继承、负库存策略、全量对账、跨租户污染、迁移失败与前向修复。CI 不得降低既有覆盖率、安全、SBOM、许可证和证据门禁。

继续禁止支付 Provider 网络、生产密钥、真实资金、未脱敏数据、促销、会员、报表、应付、发票、总账、批次成本、复杂配送 WMS 及后续 Gate 正式编码；不得用 Fake 解除 SANDBOX、REAL_DEVICE 或 PILOT 阻断，不得宣称 Alpha、可试点或可商用。

完成后提交《T2 Gate 4D / Sprint S8 周门禁报告》，等待我确认。完成后，为我整理下一步操作指令。
```
