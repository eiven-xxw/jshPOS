# T2-SAA-001 第一批正式开发操作指令（待项目发起人确认后使用）

我确认《T2 Gate 8A-Prep 启动评审报告》，接受准备阶段 CONDITIONAL PASS，并接受
CR-T2G8A-001 对 T2-SAA-001 的 CONDITIONAL GO 建议。

按 CONDITIONAL GO 启动 T2 Gate 8A / Sprint S24-A：T2-SAA-001 商户开户、套餐权益、租户
开通、停用和受控恢复的独立正式开发。

以 Gate 8A-Prep 最终封存提交
`[填写 Gate 8A-Prep 最终封存提交]`
作为起点，创建分支 `t2/gate8a-sprint24a-saa001-runtime`。

本批仅允许准入 `T2-SAA-001`。先将运行时 ADR 更新为 Accepted，再按以下顺序串行实现：

1. `jshpos-saas` 模块、Owner 端口、状态机、不变量和中文注释；
2. 商户申请、预检、独立审批和服务端 tenant_id 分配编排；
3. 版本化套餐权益、发布、生效、配额原子校验和服务端授权；
4. 初始化 Saga、激活、暂停、停用、受控恢复和逻辑注销；
5. MySQL 前向迁移、MyBatis-Plus/XML 边界、审计、Outbox 与故障恢复；
6. Vue 运营旅程与 Flutter 受限提示契约；
7. 固定向量、租户攻击、并发、迁移、内部 E2E 和完整 CI。

SaaS Owner 禁止直接写 RuoYi 系统表或其他 Owner 私有表；Foundation 继续独占技术租户和
可信上下文。暂停/到期不得删除历史，必须保留退款、查询、对账、审计、备份恢复、法定导出、
迁移和删除请求的受控入口。客户端 tenant_id、菜单隐藏或本地时间不得成为授权依据。

`T2-SUB-001`、`T2-SVC-001` 继续 DRAFT；外部 BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED
状态保持不变。不得进行真实计费、Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、
生产部署或商业声明。

完成后提交《T2-SAA-001 独立周门禁报告》等待确认，不得自动进入 SUB 或 SVC。
