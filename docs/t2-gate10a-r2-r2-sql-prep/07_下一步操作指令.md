# 下一步操作指令

以下指令仅在准备阶段 CI 全绿后使用：

> 我确认《G10A-SQL-P2-001 启动评审报告》，接受 Gate 10A-R2-R2 准备阶段 CONDITIONAL PASS。
>
> 按 CONDITIONAL GO 启动 Gate 10A-R2-R2-R1：MySQL 8.4 可执行计划与查询数红基线。
> 以 R2-R2 Prep 最终治理提交作为起点创建独立分支。本批只允许添加测试范围的 MySQL 8.4.11
> 合成数据夹具、12 条查询执行适配、EXPLAIN JSON/TREE 采集器、JDBC 查询数探针、租户权限攻击、
> 计划摘要与 CI 证据；不得修改生产 Java、Mapper/XML、SQL、索引、数据库对象、依赖、配置或
> 已发布迁移。
>
> 必须在 10k/100k 固定数据分布执行全部 12 条查询，1m 只运行获批趋势项；保存参数、表统计、
> SHOW INDEX、计划、实际行、查询数和失败 Seed。不得把测试夹具转成生产表或以墙钟波动单独判定。
>
> 完成后提交《G10A-SQL-P2-001 可执行基线报告》和分查询 Go/No-Go/CR 建议等待我确认。
> 未经确认不得修改 SQL、Mapper、索引或迁移，不得进入 N+1/分页运行时整改、RES、R3、完整 Alpha
> 或生产发布。所有外部状态和零执行边界保持不变。
