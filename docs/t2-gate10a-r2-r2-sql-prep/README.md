# Gate 10A-R2-R2 SQL 性能整改准备

本目录只固化 `G10A-SQL-P2-001` 的查询身份、MySQL 8.4 合成数据分布、计划/索引/分页/
N+1/超时/租户权限预算、失败 Seed、停止线和正式执行准入。准备阶段未修改运行时、Mapper、
SQL、索引、数据库对象、依赖、配置或已发布迁移。

文档按 `01` 至 `08` 阅读；机器权威位于 `contracts/t2/gate10a-r2-r2-sql-prep/`。
