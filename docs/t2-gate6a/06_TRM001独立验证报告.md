# T2-TRM-001 独立验证报告

## 1. 结论

`T2-TRM-001` 已完成独立设计准入、正式实现、回归修复和干净执行器验证，结论为 `VERIFIED`。本结论仅覆盖虚构终端、软件生成密钥、内部协议、MySQL 8.4 合成数据和静态/单元/集成证据，不解除 `T2-HWD-001`，也不构成 `REAL_DEVICE`、Alpha、试点或商业验收。

`T2-BAK-001` 在本报告封存时仍为 `DRAFT`，仓库不存在 `jshpos-resilience` 正式运行时，符合 `T2-TRM-001 → T2-BAK-001` 顺序门禁。

## 2. 候选基线与证据

- 分支：`t2/gate6a-sprint13-20260817`
- 候选提交：`82591230563de40f4c29420523313b3342127dbb`
- GitHub Actions：[T2 Gate 6A Run 32043851812](https://github.com/eiven-xxw/jshPOS/actions/runs/32043851812)
- 最终结果：10 个作业/证据组通过，10 个 Artifact 可见；最终证据索引通过机器校验。
- 证据等级：`STATIC + UNIT + MYSQL8.4_SYNTHETIC`；`SANDBOX=0`、`REAL_DEVICE=0`、`PILOT=0`、`CLOUD_DR=0`。

## 3. 验证范围

| 门禁 | 结果 | 关键证据 |
|---|---|---|
| governance | PASS | 基线祖先、RTM、顺序、契约、迁移摘要和禁止范围 |
| server | PASS | 模块化单体全量回归；Sync JaCoCo 行≥90%、分支≥85% |
| mysql-migration-capacity | PASS | MySQL 8.4 空库迁移、12 个正式迁移版本、二次迁移 0、Flyway validate、10 万终端合成容量 |
| terminal-resilience-security | PASS | 激活、凭据、克隆、吊销、版本/能力、租户/门店和 Secret 攻击面 |
| synthetic-vectors | PASS | 18 个固定确定性场景，Provider 网络调用 0 |
| admin-web | PASS | 构建、ESLint、类型检查、回归、依赖审计和许可证 |
| pos-linux | PASS | Flutter 分析/测试/覆盖率、SQLite 回归、Kotlin 编译、Android Debug APK、Flutter SBOM/许可证 |
| pos-windows | PASS | Windows 干净执行器 Flutter 分析和测试 |
| security-sbom-license | PASS | 服务端/Flutter SBOM，高危/严重漏洞、Secret、IaC 与许可证门禁 |
| evidence | PASS | 生产作业制品摘要、测试、覆盖率、攻击矩阵和限制声明机器汇总 |

## 4. 失败与修复总账

- Run `32041979900`：首次受 `setup-java` codeload 502 影响；重跑后发现迁移断言仅预期 10 个版本，修正为当前 12 个版本。
- Run `32042972597`：固定向量仍引用旧测试方法，同时未区分 Flyway baseline `0`；补充回归后修复。
- Run `32043125170`：被后续修复提交按并发策略取消，保留为非通过证据。
- Run `32043216906`：首次受 MySQL Action 下载 502/429 影响；重跑后发现旧同步夹具缺少 `org_unit_id`，补充约束一致的回归数据。
- Run `32043851812`：首次 Windows Flutter Action 下载 429 失败，第二次证据下载 502 失败；均仅重跑失败作业。第三次最终通过，没有改代码、降低阈值、跳过测试或启用自动重跑。

## 5. 不变量与安全结论

- 终端身份由服务端分配，可信上下文绑定租户、组织/门店、终端、指纹摘要、版本、协议和能力快照。
- 激活秘密与设备凭据只显示一次，持久化只保存 HMAC 摘要；轮换、吊销、克隆和重放失败关闭并审计。
- 客户端 tenant/store/capability/status 不能成为授权依据；Mapper、XML、任务、缓存、导出和对象键攻击矩阵无成功逃逸。
- `pos_sync_device` 继续作为唯一终端权威主表；能力、命令结果和审计只追加并有数据库保护。
- Provider SDK/HTTP、真实终端命令、生产/沙箱密钥、真实资金与真实 PII 调用或记录均为 0。

## 6. 未验证边界

- 主认证 Android 设备、厂商 SDK、Android Keystore/硬件证明、真实打印/扫码/秤/钱箱/客显仍受 `T2-HWD-001` 阻断。
- 模拟激活不得作为实机认证结论；`T2-TRM-001` 只能由项目发起人在后续确认时从 `VERIFIED` 更新为 `ACCEPTED`。
- 本报告不准入备份运行时；`T2-BAK-001` 必须在本次独立验证封存并再次通过顺序检查后单独准入。
