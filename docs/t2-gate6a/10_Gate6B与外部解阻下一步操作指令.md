# Gate 6B 与外部解阻下一步操作指令

> 文档编号：JSH-POS-T2-G6A-010
> 使用条件：项目发起人确认《T2 Gate 6A / Sprint S13 周门禁报告》后复制执行

## 可直接复制的下一步指令

```text
我确认《T2 Gate 6A / Sprint S13 周门禁报告》，接受 Gate 6A CONDITIONAL PASS。

同意将以下需求由 VERIFIED 更新为 ACCEPTED：

- T2-TRM-001
- T2-BAK-001

明确保留证据边界：T2-TRM-001 不等于 REAL_DEVICE 认证；T2-BAK-001 的 RPO/RTO 仅为 SYNTHETIC_RESTORE 内部目标，不构成生产灾备或商业 SLA。生产 KMS、真实对象存储、跨区域灾备、真实 PITR 和生产切换继续 BLOCKED/ASSUMPTION。

按 CONDITIONAL GO 启动 T2 Gate 6B / Sprint S14，仅允许逐项准入：

- T2-UPG-001：Provider 无关的服务端、Web、Schema、模板/数据包和 APK 发布治理、签名摘要、兼容决策、灰度编排、营业保护、健康检查、回退与安全前向修复

先完成数据主权、状态机、不变量、权限、审计、API/事件、Flyway、容量、兼容、回退、运行手册、故障注入和测试准入，再进入实现。不得把设计夹具直接转成生产表或运行时。

发布物必须绑定 release ID、类型、版本、渠道、目标租户/门店范围、SHA-256、签名与密钥版本、构建 commit、SBOM、协议/Schema/系统兼容窗口。服务端必须校验已登记终端的可信租户、门店、版本和能力；客户端不得自行声明目标范围、认证状态或升级权限。

发布和终端任务必须使用具名状态机、稳定幂等键、只追加事件和审计。同键异内容拒绝；摘要/签名错误、旧包重放、跨租户包、下载中断、迁移失败、健康检查失败、待同步 Outbox、UNKNOWN 支付/退款、营业时段和终端吊销必须失败关闭。MySQL/SQLite Schema 只前进，已发布迁移不得修改，失败使用安全前向修复。

本 Sprint 只允许合成签名包、虚构终端和软件执行结果。T2-HWD-001 未经独立实机升级解阻评审确认前，禁止真实 APK 安装、厂商静默升级 SDK、固件命令、真实终端重启或远程命令；合成结果不得宣称 REAL_DEVICE。

并行维持三条外部解阻轨：

1. Gate 3B-Prep 只核验首接支付 Provider 的授权沙箱、测试终端、正式接口、签名验签、回调、退款、账单、限流、网络和技术联系人；未经独立《支付沙箱解阻评审报告》确认，不得进行 Provider 网络调用。
2. T2-HWD-001 收集主认证 Android 厂商/型号/固件/SDK、两种打印、扫码、电子秤、钱箱、客显、样机、静默安装和技术联系人；资料齐备后只提交解阻评审报告。
3. T2-PAR-001 收集 5 家设计伙伴、至少 3 家书面试点意愿、合规脱敏样本和旧系统对账条件；不得把合成数据替代伙伴证据。

T2-UAT-001、T2-REL-001 继续保持 DRAFT，只允许更新差距、证据目录、角色签署和 Go/No-Go 模板。支付沙箱、真实硬件、设计伙伴任一 P0 阻断未解除时，不得启动完整 Alpha UAT。

CI 继续执行治理、服务端、Web、MySQL、发布/迁移/回退故障、租户/权限/对象存储攻击、性能、Secret、依赖、SBOM、许可证、覆盖率和证据门禁，并保持 Flutter Linux/Windows、SQLite、Kotlin、Android 及 Gate 0—6A 全量回归。不得降低阈值、跳过失败测试、自动重跑掩盖 Flaky、修改已发布迁移或创建绿色占位。

继续禁止生产密钥、真实资金、真实 PII、支付 Provider 网络、未经授权云写入、真实终端命令和完整 Alpha/UAT；不得宣称 Alpha、可试点或可商用。

完成后提交《T2 Gate 6B / Sprint S14 周门禁报告》《T2-HWD-001 实机升级解阻准备报告》和更新后的外部 P0 差距清单，等待我确认。
```
