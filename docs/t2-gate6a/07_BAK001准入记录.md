# T2-BAK-001 顺序准入记录

## 1. 前置门禁

- `T2-TRM-001` 已在提交 `cc939ee2096564ea48f489cffeffbffaa840064f` 更新为 `VERIFIED`。
- 独立封存运行 [GitHub Actions 32044586171](https://github.com/eiven-xxw/jshPOS/actions/runs/32044586171) 的治理、服务端、MySQL 8.4、终端安全、双平台 POS、Android、SBOM、许可证、Secret 和证据作业全部通过。
- 封存时 `T2-BAK-001=DRAFT` 且 `jshpos-resilience` 不存在，证明未倒置 `T2-TRM-001 → T2-BAK-001` 顺序。

## 2. 本次准入

`T2-BAK-001` 可由 `DRAFT` 更新为 `IN_PROGRESS`，仅实现下列内部合成能力：

1. MySQL 权威事实、业务对象、配置/模板、迁移清单和封板证据的版本化恢复集合；
2. AES-256-GCM 独立对象加密、外部密钥版本引用、SHA-256 清单和对象摘要；
3. 按租户范围命名空间只追加的对象存储端口、不可变保留和受控状态迁移；
4. 从空隔离目标开始的清单、密钥、摘要、Schema/应用版本、对象、投影、租户/业务日、Inbox/Outbox/游标和审计校验；
5. 损坏、缺片、错密钥、跨租户替换、PITR 边界、恢复中断和迁移不兼容失败关闭；
6. 自动计时的内部合成 RPO≤900 秒、RTO≤3600 秒证据。

## 3. 继续阻断

- 跨账号/跨区域云备份、生产 KMS/HSM、真实对象存储、binlog 连续归档和生产灾备切换保持 `BLOCKED/ASSUMPTION`。
- 本地文件、容器、软件密钥和合成数据只能形成 `SYNTHETIC_RESTORE` 证据，不得升级为 `CLOUD_DR` 或商业 SLA。
- `T2-UPG-001` 仍为 `DRAFT`，不得创建升级运行时或发送真实终端命令。
- `T2-PAY-002`、`T2-HWD-001`、`T2-PAR-001` 状态不变；Provider 网络调用、真实设备命令和真实数据继续为 0。
