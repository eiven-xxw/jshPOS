# 基线、差异分类与修复结果

## 1. 冻结基线

- Git 基线：`f708271e977f995e83a24fe398a1bd658726fd09`
- Controller：300 项
- 当前 OpenAPI：257 项
- Controller 无当前 OpenAPI：64 项
- 当前 OpenAPI 无精确 Controller：21 项
- 客户端业务 API 根：14 个，缺正式服务端根为 0
- 有效错误码：1388 个

原始 Gate 9A 报告、`contracts/t2/gate9a-prep/defect-register-v1.json` 和历史
`scripts/audit_t2_gate6g_api.py` 均保持不变。十个 Owner 分组、来源文件、计数和决定记录在
`contracts/t2/gate9b/api-difference-classification-v1.json`；各分组计数之和精确覆盖 64/21，
不存在未分类差异。

## 2. 分类与处理

| Owner | Controller 缺契约 | 契约缺 Controller | 分类 | 处理 |
|---|---:|---:|---|---|
| Service | 14 | 0 | 当前契约发现入口 | 既有正式契约迁入 `openapi-service-v1.yaml`，旧文件成为显式替代指针 |
| Subscription | 10 | 0 | 当前契约发现入口 | 迁入 `openapi-subscription-v1.yaml`，路径参数与 Controller `{id}` 对齐 |
| Replenishment | 11 | 10 | 当前路径漂移 | 固定 `/api/v1/replenishment`、`suggestions/generate` 和规则详情 |
| Exception Center | 8 | 1 | 通用 action 漂移 | 用 claim/start/transfer/plan/repair/review/close/reopen 八个具名操作替代 |
| SaaS | 7 | 7 | 路径参数名漂移 | application/entitlement 参数名与 Controller `{id}` 精确一致 |
| Weighted Barcode | 3 | 1 | 操作漂移 | 补 get/preview/retire，移除无 Controller 的当前 `resolve` 声明 |
| Member Benefit | 3 | 1 | 通用 action 漂移 | pause/resume/revoke 使用具名操作与精确权限 |
| Member Price | 4 | 0 | 当前契约缺页 | 补 create/validate/approve/publish |
| Promotion | 3 | 0 | 当前契约缺页 | 补会员权益签名包 publish/detail/content |
| Member Benefit Promotion | 1 | 1 | 路径漂移 | 固定 `/api/v1/promotions/member-benefit/quotes` |

## 3. 修复后结果

- Controller/OpenAPI：`300/300`
- 双向差异：`0/0`
- operationId 缺失/重复：`0/0`
- 本批七份当前契约覆盖操作：80 项，OpenAPI 权限与 Controller 精确一致 80/80
- Controller 权限缺失：0；保留既有两项设备认证前置例外
- DTO 客户端 tenant 覆盖：0
- 历史替代契约元数据错误：0
- 客户端 API 根缺正式服务端：0/14
- Controller、客户端业务代码和已发布迁移变更：0

结论：85 项差异全部由当前契约发现、路径、参数和具名操作收口解决，没有证据要求修改已接受
业务运行时，也没有新增业务能力。
