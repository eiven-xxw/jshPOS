# T2-PERF-002 独立验证报告

## 验证结论

`VERIFIED / CONDITIONAL PASS / AWAITING SPONSOR ACCEPTANCE`。候选提交
`5c6859056d7a04885a0b3639384e7cf09492292b` 的 GitHub Actions Run
`32705842999` 已从头完成 11 个 Job 并全部成功；T2-PERF-002 保持 `VERIFIED`，
项目发起人确认前不得更新为 `ACCEPTED`。

## 验证矩阵与结果

| 维度 | 固定输入 | 冻结阈值/不变量 | 实测结果 |
|---|---:|---|---|
| 冷启动 | 新 JVM、完整 V1—V79 迁移 | ≤90 秒 | 64.650 秒，PASS |
| HTTP 并发 | 1/8/16，各 240 请求 | 错误率 0；P95≤2500ms；P99≤5000ms；并发16≥8RPS | 并发16为 386.864RPS，P95 60.597ms，P99 66.710ms，0错误 |
| 持续运行 | 并发16、120秒 | 进程存活、全部样本成功、资源有界 | 77648 请求，646.967RPS，P95 27.803ms，P99 34.686ms，0错误 |
| 连接压力 | 并发32、128请求 | 有界成功/拒绝且不崩溃 | 704.823RPS，P95 47.304ms，P99 55.982ms，0错误 |
| 运行资源 | 正式 JAR/MySQL/Redis | RSS≤2GiB、MySQL连接≤80、Redis≤512MiB、GC暂停≤2s | RSS 1398.242MiB、连接18、Redis used 1.954MiB、GC 40.953ms |
| 依赖退化 | Redis/MySQL 分别暂停 | 不伪造成功、恢复检查≤20 | 3004/3001ms 内失败关闭，各1次恢复检查，进程存活 |
| Owner 容量 | 10k/100k/1m 合成事实 | 正式迁移、摘要、金额数量守恒 | Owner/MySQL Job PASS；百万级只作为合成趋势 |
| POS 本地 | 扫码1000、结算200、同步10k、包100k | SQLite 原子、事件不丢、包安装≤120秒 | 4324/1525/1479/6299ms，PASS |

正式 HTTP 共记录 `78496` 个原始样本，全部成功。运行器指纹、容器镜像摘要、
合成租户摘要、GC 日志摘要与请求样本摘要均进入正式运行栈 Artifact。

## 不可变证据

- Run：`https://github.com/eiven-xxw/jshPOS/actions/runs/32705842999`；
- 最终证据索引 Artifact `9512489205`，GitHub SHA-256
  `f913690b201a438b32c9bf7a98bfe2ce4383a307b31f442b6d8182d57ddd4883`；
- 正式运行栈 Artifact `9512450577`，GitHub SHA-256
  `9bff209ae194f9c33d672ab5feb2c8171a127ef641a484b9da80bb5a1aea9f75`；
- 治理双平台、Server、Flutter 双平台/Android、Owner/MySQL、Web、POS、正式运行栈、
  Security、Evidence 共 11 个 Job 全绿，生成 11 份 Artifact。

## 失败发现与关闭

1. Run `32701100510` 发现历史 MySQL 容量测试把 Flyway 当前版本固定为 V74；
   CR-T2G8C-013 改为前向兼容断言，未修改迁移或生产代码。
2. Run `32702135141` 发现附件适配器双构造器缺少显式 Spring 注入元数据；
   CR-T2G8C-014 仅增加 `@Autowired` 和回归测试。
3. Run `32702869788` 发现合成租户套餐缺少既有订阅自查权限；
   CR-T2G8C-015 只修复合成权限夹具，生产权限定义不变。
4. Run `32704146499` 发现 Security 在下载 Artifact 后执行源码范围检查导致误判；
   CR-T2G8C-016 前置范围检查，全部安全扫描仍执行。
5. Run `32704925873` 发现 Evidence 在跨生产者全局搜索同名 JUnit 时拒绝二义性；
   CR-T2G8C-017 绑定具名生产者并继续在来源内强制唯一。

五个失败 Run 和 Artifact 均保留。没有重跑失败 Job、自动重试、阈值放宽、测试跳过、
迁移回写、依赖漂移或绿色占位。

## 范围与证据边界

- 新业务能力：0；已发布迁移变化：0；依赖变化：0；
- 生产业务算法变化：0；正式装配元数据修复：1 个文件；
- Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产部署：0；
- 证据上限：`INTERNAL_FULL_STACK_PERFORMANCE_CANDIDATE`，不构成生产容量或商业 SLA。
