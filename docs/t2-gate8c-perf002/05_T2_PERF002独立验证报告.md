# T2-PERF-002 独立验证报告

## 候选结论

`VERIFIED CANDIDATE / AWAITING COMPLETE CI`。本地已验证治理、契约、执行器算法和 10 万记录签名包的真实 Flutter/SQLite 原子安装；正式 Linux MySQL/Redis/JAR/HTTP 和全量回归由同一提交的专属 CI 证明。

## 验证矩阵

| 维度 | 固定输入 | 硬断言 | 证据 |
|---|---:|---|---|
| 冷启动 | 新 JVM、完整 V1—当前迁移 | 应用健康且 ≤90 秒 | formal-runtime Artifact |
| HTTP 并发 | 1/8/16，并发各 240 请求 | 错误率 0、P95/P99 与吞吐阈值 | formal-runtime-performance.json |
| 持续运行 | 并发16、120秒 | 进程存活、全部样本成功、资源有界 | 原始样本摘要与资源序列 |
| 连接压力 | 并发32 | 有界成功/拒绝且无崩溃 | connection-pressure phase |
| 依赖退化 | Redis/MySQL 分别暂停 | 不伪造成功、恢复≤20次检查 | dependency-faults.json |
| Owner容量 | 10k/100k/1m 合成事实 | 迁移、摘要、金额数量守恒 | JUnit 与 gate1-capacity.json |
| POS本地 | 扫码1000、结算200、同步10k、包100k | SQLite原子、事件不丢、固定阈值 | flutter-performance.jsonl |

## 本地结果

10 万记录包安装实测约 8066ms，记录数 100000、批次余额 99999、包状态 ACTIVE；本地执行器只用于功能和趋势诊断，不进入正式 Ubuntu 墙钟基线。

## 未变更项

生产业务算法、MySQL/SQLite 已发布迁移、依赖、业务 API、Owner 数据主权与状态机变化均为 0。正式运行栈发现并修复一个 Spring 构造注入元数据缺陷。Provider 网络、真实资金、真实设备/外设、伙伴现场、完整 Alpha 和生产部署均为 0。

## 首次 CI 发现

Run `32701100510` 的 Owner 容量作业发现两项历史 MySQL 测试将当前 Flyway 版本固定为 V74，完整当前 Schema 已合法前进到 V79。CR-T2G8C-013 仅修复测试的前向兼容断言，失败 Run 保留，修复提交从头复跑全部作业。

Run `32702135141` 在 Owner 容量修复通过后发现附件适配器的 Spring 构造器未显式标记，正式 JAR 无法启动。CR-T2G8C-014 仅修复依赖注入元数据并增加回归，业务行为、迁移、依赖和阈值不变。

Run `32702869788` 已成功启动正式 JAR 并完成 Gate 8B 的 55 次正式 API 观察，但性能预热访问 `/api/v1/subscriptions/current` 时发现合成租户套餐未装配 `subscription:self:read`。CR-T2G8C-015 仅补齐合成权限套餐及自检，不删除该受保护路径、不修改生产权限定义、业务算法、迁移、依赖或阈值；失败 Artifact `9511299627`（GitHub SHA-256 `a2af7e38177bd290df673ddedc2cc9fccf6ece3f2f65ddf9f4dc0cb1caf9f982`）保留。

Run `32704146499` 的治理双平台、Server、Owner/MySQL、Web、Flutter 双平台/Android、POS 性能与正式运行栈均已通过；Security 在下载 Server/Flutter Artifact 后调用范围检查器，把 `downloaded/` 制品误识别为仓库越界变更而失败。CR-T2G8C-016 仅把同一范围门禁前置到 Artifact 下载前，之后仍完整执行依赖差异、SBOM 许可证、漏洞、Secret 和工作流配置扫描；不扩张白名单或降低任何门槛。
