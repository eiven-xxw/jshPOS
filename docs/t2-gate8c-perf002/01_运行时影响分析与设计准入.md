# T2-PERF-002 运行时影响分析与设计准入

## 准入结论

`CONDITIONAL GO / IN_PROGRESS`。前置 T2-MTN-001 已被项目发起人接受；ADR-065、正式栈模型、执行器、阈值和故障向量均在运行前冻结。

## 唯一范围

- 关闭 Gate 6H 基线未覆盖 Gate 7/8 能力的证据缺口；
- 在正式 MySQL/Redis/JAR/HTTP 栈补齐并发、持续运行、尾延迟、资源、连接/队列压力和退化证据；
- 重跑 Flutter POS 扫码、结算、SQLite 事务、同步积压和恢复；
- 仅当固定负载稳定复现 P0/P1 时，允许不改变业务行为的最小整改。

不新增 Owner、状态机、API、表、迁移、页面或外部能力，不新增依赖，不形成商业 SLA。

## 数据主权与正确性硬门禁

性能脚本只能调用正式 REST/API、应用服务或既有测试端口；禁止直接写业务表。每个负载先验证可信 tenant_id、稳定幂等、金额数量守恒、事件不丢失和预期状态，再计算延迟或吞吐。错误、超时、守恒失败、跨租户污染、样本缺失均直接失败，不以吞吐抵消。

## 兼容、回退与安全

本轮默认只新增测试、脚本、契约、CI 和证据。若实际代码整改发生，必须保留失败 seed、独立回归、兼容与回退说明；禁止修改已发布迁移。虚构凭据只从 CI 环境注入，证据不得保存令牌、密码、正文或 PII。

## 完成门槛

两个 P1 全部内部关闭；固定执行器上全部阈值通过且无自动重跑；开放性能 P0/P1 为 0；完整 Server/Web/Flutter/Android/MySQL/SQLite、安全、SBOM、许可证和既有 Gate 回归全绿。完成后只更新为 VERIFIED 并等待发起人确认。
