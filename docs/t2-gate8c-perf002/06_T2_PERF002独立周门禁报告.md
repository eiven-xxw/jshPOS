# T2-PERF-002 独立周门禁报告

## 当前结论

`VERIFIED / CONDITIONAL PASS / AWAITING SPONSOR ACCEPTANCE`。

候选提交 `5c6859056d7a04885a0b3639384e7cf09492292b` 的 GitHub Actions Run
`32705842999` 已从头完成 11 个 Job 并全部成功。两项既定性能 P1 已在内部证据范围
关闭，开放性能 P0/P1 为 `0/0`。T2-PERF-002 维持 `VERIFIED`，等待项目发起人确认；
不得自动更新为 `ACCEPTED` 或启动 T2-RDY-001。

## 门禁结果

| 门禁 | 结果 | 核验摘要 |
|---|---|---|
| 治理 Ubuntu/Windows | PASS | 基线祖先、RTM、ADR、CR、范围和状态边界一致 |
| Server 全量 | PASS | 既有 Owner、权限、迁移和覆盖率回归通过 |
| Flutter Ubuntu/Windows、Android/Kotlin | PASS | 既有回归、覆盖率与 Android 构建通过 |
| Owner/MySQL 容量 | PASS | 当前正式 Schema、10k/100k/1m 合成容量和守恒通过 |
| Web | PASS | 构建、Lint、类型和组件测试通过 |
| POS 性能 | PASS | 扫码1000、结算200、积压10k、签名包100k 均通过 |
| 正式运行栈 | PASS | MySQL 8.4、Redis 7.4、正式 JAR/HTTP、120秒持续及依赖故障通过 |
| Security | PASS | 租户/权限、Secret/PII、依赖、漏洞、SBOM、许可证和工作流审计通过 |
| Evidence | PASS | 具名生产者、摘要、证据等级、零外部执行和失败关闭全部通过 |

## 关键量化结果

- 冷启动：`64650 ms`；正式 HTTP 原始样本 `78496`，错误 `0`；
- 并发16：`386.864 RPS`，P95 `60.597 ms`，P99 `66.710 ms`；
- 120秒持续阶段：`77648` 次请求、`646.967 RPS`、P95 `27.803 ms`、
  P99 `34.686 ms`、错误 `0`；
- 最大资源：应用 RSS `1398.242 MiB`、MySQL连接 `18`、Redis used memory
  `1.954 MiB`、GC暂停 `40.953 ms`；
- Redis/MySQL 暂停：`3004/3001 ms` 内失败关闭，各一次恢复检查后进程存活；
- POS：扫码1000次 `4324 ms`、结算200笔 `1525 ms`、同步10k条 `1479 ms`、
  10万签名包原子安装 `6299 ms`。

上述结果只在固定 GitHub 托管执行器与内部合成数据下成立，不允许跨执行器直接比较
墙钟，也不形成生产容量或商业 SLA。

## 不可变证据

- Run：`https://github.com/eiven-xxw/jshPOS/actions/runs/32705842999`；
- 最终证据 Artifact `9512489205`，GitHub SHA-256
  `f913690b201a438b32c9bf7a98bfe2ce4383a307b31f442b6d8182d57ddd4883`；
- 正式运行栈 Artifact `9512450577`，GitHub SHA-256
  `9bff209ae194f9c33d672ab5feb2c8171a127ef641a484b9da80bb5a1aea9f75`；
- 制品共 11 份，最终 Evidence 索引绑定各生产者并校验文件摘要和证据分类。

## 失败修复审计

五次候选失败 Run `32701100510`、`32702135141`、`32702869788`、
`32704146499`、`32704925873` 均完整保留，并由 CR-T2G8C-013 至
CR-T2G8C-017 分别记录前向迁移测试、Spring 注入、合成权限、Security 作业顺序和
Evidence 生产者边界问题。每次都通过新提交从头运行完整流水线；自动重跑、跳过失败
测试、删除失败证据、降低阈值、修改已发布迁移和新增依赖均为 `0`。

## 状态与边界

- T2-PERF-002：`VERIFIED`，等待项目发起人接受；
- T2-RDY-001：`DRAFT`，本阶段未启动；
- T2-PAY-002/HWD-001/PRN-001/PAR-001：继续 `BLOCKED`；
- T2-UAT-001/REL-001：继续 `DRAFT`；T2-LIC-001/JSH-001：继续 `DEFERRED`；
- Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、生产部署和商业声明：0；
- 证据最高为 `INTERNAL_FULL_STACK_PERFORMANCE_CANDIDATE`，不代表真实设备、生产容量、
  完整 Alpha、商业验收或商业 SLA。
