# G9A-R3C 十二维验收矩阵

权威机器矩阵位于 `contracts/t2/gate9b-r3c-runtime/surface-acceptance-v1.json`。

十二维为：路由/导航、按钮、最小权限、可信数据范围、加载、空态、安全错误、单航班、离线、
业务日、原操作恢复和直接测试。五个在线 Vue 页的离线维度为 `NOT_APPLICABLE`，但网络错误必须
失败关闭并保留原操作身份；不生成交易事实的商业运营页，其业务日维度为 `NOT_APPLICABLE`。

八页均已达到批次 `VERIFIED_CANDIDATE`。该矩阵不替代 R3D 全部 26 页联合验收。
