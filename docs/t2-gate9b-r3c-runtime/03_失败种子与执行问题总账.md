# G9A-R3C 失败种子与执行问题总账

准备阶段八组 P1 seed 均已在本批关闭：Vue 直接挂载证据、Flutter Widget 证据、页面状态、
原幂等键恢复、高风险确认、授权范围、可信数据访问，以及离线/业务日边界。

本地 Web 全量回归为 44 个测试文件、106 项测试通过。本机未安装 Flutter SDK，因此没有将
静态检查伪造成 Widget 绿色结论；Flutter Ubuntu/Windows、Android/Kotlin 必须由 GitHub 干净
执行器完成。未使用自动重跑或跳过测试掩盖 Flaky。

首次本地完整 Web 门禁真实发现 60 项 Prettier 格式错误并失败关闭；使用项目格式器机械修复后，
从 production build、ESLint、typecheck 到全量测试完整复跑。回归还发现 VUE-16 的 checkbox
旧属性弃用告警，已改用 `value` 并复跑直接挂载测试。两项均未通过跳过或自动重跑掩盖。

首个远程候选 GitHub Actions Run `32803979146` 的 Flutter Ubuntu Job `97670356321` 在
`dart format --output=none --set-exit-if-changed lib test` 发现 4 个本批文件格式不一致并失败关闭。
原失败 Run 完整保留，未使用自动重跑；随后使用与流水线一致的官方 Dart 3.13.0 仅机械格式化
这 4 个文件，并对 `lib/test` 共 131 个文件复核为 0 个待变更，要求由新候选从头执行完整门禁。

第二个远程候选 Run `32804503972` 的 Flutter Ubuntu Job `97672430082` 已通过 131 文件零差异
格式检查与 `flutter analyze --fatal-infos`，随后在 `flutter test --machine --coverage` 失败。
旧工作流只把机器输出重定向到文件，而失败后没有进入上传步骤，因此页面仅保留退出码。该 Run
仍完整保留；工作流仅增加 `pipefail + tee` 同步打印并保存原机器日志，不改变测试命令、阈值、
用例或失败语义，由新候选取得可诊断证据。

机器总账：`contracts/t2/gate9b-r3c-runtime/failure-seed-closure-v1.json`。
