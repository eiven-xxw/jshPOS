# G9A-R3C 失败种子与执行问题总账

准备阶段八组 P1 seed 均已在本批关闭：Vue 直接挂载证据、Flutter Widget 证据、页面状态、
原幂等键恢复、高风险确认、授权范围、可信数据访问，以及离线/业务日边界。

本地 Web 全量回归为 44 个测试文件、106 项测试通过。本机未安装 Flutter SDK，因此没有将
静态检查伪造成 Widget 绿色结论；Flutter Ubuntu/Windows、Android/Kotlin 必须由 GitHub 干净
执行器完成。未使用自动重跑或跳过测试掩盖 Flaky。

首次本地完整 Web 门禁真实发现 60 项 Prettier 格式错误并失败关闭；使用项目格式器机械修复后，
从 production build、ESLint、typecheck 到全量测试完整复跑。回归还发现 VUE-16 的 checkbox
旧属性弃用告警，已改用 `value` 并复跑直接挂载测试。两项均未通过跳过或自动重跑掩盖。

机器总账：`contracts/t2/gate9b-r3c-runtime/failure-seed-closure-v1.json`。
