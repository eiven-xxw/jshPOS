# Gate 5C 与 Gate 3B 下一步操作指令

> 文档编号：JSH-POS-T2-G5B-008  
> 使用条件：项目发起人确认《T2 Gate 5B / Sprint S10 周门禁报告》后复制执行

## 1. 建议决策

1. 接受 Gate 5B `CONDITIONAL PASS`，将 `T2-POS-006`、`T2-ORD-003`、`T2-REF-002` 从 `VERIFIED` 更新为 `ACCEPTED`。
2. 按 `T2-MEM-001 → T2-MEM-002` 启动 Gate 5C / Sprint S11；先建立会员身份、数据最小化、同意与隐私请求边界，再建立等级和不可变积分权益账本，不允许两项一次铺开。
3. `T2-RPT-001/002` 仅允许补充契约、权威事实到查询投影、重建、脱敏导出和合成验收用例，继续 `DRAFT`；支付报表外部账单部分继续被 `T2-PAY-002` 阻断。
4. Gate 3B-Prep 继续只核验首接 Provider 真实资料。未经独立解阻确认，不得新增 SDK/HTTP、真实回调、账单下载或任何 Provider 网络调用。
5. 会员阶段不包含优惠券、储值、充值、预算抢占、营销自动化、外部会员平台或真实短信服务。

## 2. 可直接复制的下一步指令

```text
我确认《T2 Gate 5B / Sprint S10 周门禁报告》，接受 Gate 5B CONDITIONAL PASS。

同意将以下需求由 VERIFIED 更新为 ACCEPTED：

- T2-POS-006
- T2-ORD-003
- T2-REF-002

T2-PAY-002 继续 BLOCKED。Gate 3B-Prep 继续只允许真实资料核验；未经我确认独立《支付沙箱解阻评审报告》，不得进行任何 Provider 网络调用。

按 CONDITIONAL GO 启动 T2 Gate 5C / Sprint S11，仅允许逐项准入：

- T2-MEM-001：会员最小档案、统一身份、同意与撤回、脱敏展示、隐私请求和可追溯审计
- T2-MEM-002：会员等级、不可变积分权益流水、可重建余额投影，以及成交、退货和到期的幂等冲正

必须按 T2-MEM-001 → T2-MEM-002 的依赖顺序完成设计准入和实现，不得一次铺开。只有逐项完成数据主权、状态机、不变量、隐私/权限、审计、API/事件、Flyway/SQLite、容量、兼容、回退和测试准入后，才可由 DRAFT 更新为 READY/IN_PROGRESS。

Member Owner 必须拥有会员身份、同意记录、隐私请求、等级事实和积分流水；Order、Promotion、Return 和 Reporting Owner 只能通过明确端口或版本化事件使用必要的会员快照，禁止跨模块 Mapper 写会员表。

会员身份数据必须最小化收集、用途限定、分类分级、字段脱敏、密文或令牌化存储、保留期治理和全程审计。tenant_id 只来自可信上下文；会员查找、合并/拆分、导入导出、隐私导出/删除请求和后台任务必须执行租户、组织/门店数据范围和权限检查。

POS 离线端禁止持久化不必要的手机号、证件号或其他明文 PII；只允许最小化会员令牌、等级/权益快照版本和业务所需摘要，过期、撤回、换机和终端丢失必须有失效与清理策略。

积分和等级效果只能由 Member Owner 生成只追加事实；余额和等级投影必须可从流水重建。成交获取、使用、到期、退货冲正和人工调整必须有稳定幂等键、原事实引用、原因、操作/审批人和业务日；禁止直接更新余额或回写历史流水。

成交与退货事件必须重复/乱序安全，同键异内容拒绝；晚到、重放、部分失败和投影损坏必须可修复。退货冲正不得超过原交易已发放/已使用的可冲正上限，不得重新计算历史会员策略。

继续使用两个虚构租户、多组织、多门店、多终端和三类业态合成数据，禁止真实 PII。测试覆盖跨租户身份替换、枚举查询、越权查看/导出/删除、脱敏失效、密文与日志泄漏、重复注册、合并冲突、同意撤回、积分重复/乱序/晚到、退货冲正、到期边界、并发消费、全量重建和前向修复。

CI 继续执行治理、服务端、Flutter Linux/Windows、MySQL、SQLite、会员事件重放、租户/PII/权限攻击、Secret、依赖、SBOM、许可证、覆盖率和证据门禁；不得降低阈值、跳过失败测试、自动重跑掩盖 Flaky、修改已发布迁移或创建绿色占位。

T2-RPT-001、T2-RPT-002 本 Sprint 只允许补充契约、权威事实到可重建查询投影、权限/脱敏导出、合成向量和验收用例，继续保持 DRAFT，不允许正式报表运行时实现。

本 Sprint 不包含优惠券、储值、充值、预算抢占、营销自动化、外部会员平台或真实短信。继续禁止支付 Provider 网络、生产密钥、真实资金、未脱敏数据、报表运行时、应付、发票、总账、批次成本、复杂配送 WMS 和后续 Gate 正式编码；不得用 Fake 解除 SANDBOX、REAL_DEVICE 或 PILOT 阻断，不得宣称 Alpha、可试点或可商用。

完成后提交《T2 Gate 5C / Sprint S11 周门禁报告》，等待我确认，并为我整理下一步操作指令。
```
