# G9A-R3A 第一批正式页面整改操作指令

```text
我确认《G9A-R3 启动评审报告》，接受准备阶段 CONDITIONAL PASS，并接受 ADR-071
对页面状态、服务端最终授权、原幂等键恢复和交互测试边界的建议。

按 CONDITIONAL GO 启动 G9A-R3A：主路径、测试底座与组合支付可达性正式整改。

以 G9A-R3 准备阶段最终封存提交
[填写最终封存提交]
作为起点创建独立分支：
t2/gate9b-sprint27c-g9a-r3a-runtime

本批只允许关闭下列页面对应的既有 G9A-UI-P1-001 缺口：

- VUE-01 商品中心
- VUE-02 货架价签面板
- VUE-03 平台基础/组织门店
- VUE-04 高级运营导航
- FLT-03 POS 收银结算
- FLT-04 POS 可信会话壳
- FLT-06 Provider 无关组合支付

复用 T2-UX-001、T2-ADM-001/002、T2-POS-007/008 和 T2-PAY-004，不新增
Requirement ID 或业务能力。先将 ADR-071 更新为 Accepted，再严格执行：

1. 先补路由、权限、加载/空态/错误、重复点击、离线、业务日和失败恢复的失败回归；
2. Vue 如需增加挂载测试依赖，先完成版本锁定、许可证、SBOM、安全与替代分析；
3. 商品、价签、组织门店的每个服务端写按钮绑定与 Controller 对齐的显式最小权限；
4. 写操作显示忙碌、稳定错误码、关联标识和可恢复动作；重复点击只执行一个命令，失败重试复用原幂等键；
5. 将 PosTenderPage 通过可信会话和冻结订单/班次/业务日上下文接入正式导航；禁止页面自行声明 tenant_id、金额或支付成功；
6. T2-PAY-002 继续 BLOCKED，电子支付份额必须失败关闭，不得创建 Provider SDK/HTTP 或网络调用；
7. Vue/Flutter 不得复制领域状态机、直接访问 Mapper/SQLite/MethodChannel 或修改已发布迁移；
8. 7 个页面逐项通过十二维验收，并执行治理、Server、Web、Flutter Linux/Windows、Android/Kotlin、
   MySQL/SQLite、安全、SBOM、许可证、覆盖率和既有 Gate 全量回归。

完成后提交《G9A-R3A 主路径页面整改独立周门禁报告》等待我确认。不得自动进入
G9A-R3B、G9A-R4、完整 Alpha 或生产发布。所有外部 BLOCKED、UAT/REL DRAFT、
LIC/JSH DEFERRED 和零执行边界保持不变。
完成后为我整理下一步操作指令。
```
