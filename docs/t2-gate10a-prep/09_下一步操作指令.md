# 下一步操作指令

当前需要项目发起人分别确认两件事：

1. 是否按 `01_Gate9C_Annotated_Tag精确提案.md` 创建并推送 annotated tag；
2. 是否按 `08_Gate10A_R1第一批正式整改操作指令.md` 启动 R1。

两个确认相互独立。只确认 tag 不授权 R1；只确认 R1 也不授权创建 tag。未经确认，R2—R4、外部执行、完整 Alpha 和生产继续禁止。
