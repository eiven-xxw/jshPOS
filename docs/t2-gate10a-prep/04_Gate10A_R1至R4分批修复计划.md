# Gate 10A R1—R4 串行修复计划

1. **R1 CI、依赖与供应链（已关闭）**：三项 Finding 已由项目发起人确认为 `CLOSED_IN_GATE10A_R1`。
2. **R2 Server、数据库与可维护性（仅准备）**：已授权复杂度、Owner 依赖、异常和事务边界红基线、关键 SQL 计划、索引/N+1 与资源斜率设计；正式运行时整改仍等待确认。
3. **R3 Vue、Flutter、SQLite 与端侧稳定性**：保持 26 页验收不变，拆分大型 Dart 职责，建立 SQLite 页/WAL/Outbox 生命周期与崩溃恢复门禁。
4. **R4 可观测性、长稳、恢复和内部发布复验**：建立 22 Owner 指标/Trace/告警矩阵，先 24 小时后 72 小时，复验恢复、升级回退、制品摘要、SBOM 和 Go/No-Go。

每批必须独立 `VERIFIED` 并由项目发起人确认；前批未接受，后批保持 `DRAFT`。不存在“一次升级所有依赖”或“一次拆分所有类”的授权。
