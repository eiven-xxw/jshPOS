# T2-EXC-001 下一步操作指令

```text
我确认《T2-CLS-001 独立周门禁报告》，接受 T2-CLS-001
CONDITIONAL PASS。

同意将 T2-CLS-001 由 VERIFIED 更新为 ACCEPTED。明确该接受只覆盖虚构租户、
虚构门店和合成权威事实下的门店业务日日结、差异、独立审批、只追加签署、
晚到事实与更正版本的内部软件执行；百万级结果只代表合成趋势，不代表支付沙箱、
真实设备/外设、完整 Alpha、生产、商业验收或商业 SLA。

按 CONDITIONAL GO 启动 T2 Gate 7D / Sprint S22-A 第二批：
T2-EXC-001 同步、数据包、支付退款、库存、成本和报表异常的统一认领、处置、
修复编排、关闭与完整审计。

以 T2-CLS-001 最终封存提交
[填写 T2-CLS-001 最终封存提交]
作为工作分支起点，创建独立分支：
t2/gate7d-sprint22a-exc001-exception-center

本批仅允许逐项准入 T2-EXC-001。先冻结异常数据主权、来源类型、稳定身份、
去重规则、严重级别、状态机、认领租约、职责分离、权限/门店范围、审计、幂等、
Owner 修复端口、API/事件、MySQL 前向迁移、Vue 异常工作台、容量、兼容、回退和
量化验收，再进入正式实现。

Operations Owner 只独占异常案件、来源引用、认领/转派、处置计划、修复命令引用、
状态历史、审计和 Outbox。Sync、Catalog/DataPackage、Payment/Refund、Inventory、
Costing、Reporting 和 DailyClose 继续独占各自事实；异常中心只能消费版本化事件、
显式只读端口和受控修复命令端口，禁止跨 Owner Mapper、任意 SQL、直接更新余额、
流水、订单、资金状态、投影或日结事实。

每个异常必须绑定可信 tenant_id、组织/门店范围、source_owner、source_type、
source_fact_id、source_event_id、source_sequence、source_sha256、dedup_key、severity、
首次/最近观察时间和关联标识。客户端 tenant、门店、来源摘要、严重级别、Owner结果
或关闭状态不得成为权威事实；列表、缓存、任务、导出和对象存储必须执行租户与门店范围。

状态至少覆盖 OPEN、CLAIMED、IN_PROGRESS、WAITING_OWNER、RESOLVED、CLOSED、
REOPENED 和 FAILED。认领使用有期限租约和乐观锁；重复同来源同摘要返回原案件，
同来源异摘要追加新观察或显式冲突，不得静默覆盖。关闭必须基于 Owner 的可验证
修复结果和独立复核；来源事实再次异常时只能 REOPEN 或新建关联案件，不得改写历史。

修复必须由对应 Owner 的具名应用命令执行并使用稳定幂等键；异常中心只保存命令引用、
请求摘要、观察状态和结果摘要。UNKNOWN 支付/退款只能查询、回调或对账观察收敛，
禁止重新生成扣款、退款、订单、库存、成本或同步业务命令。部分 Owner 失败、超时、
ACK 丢失和进程重启必须复用原命令并最终收敛或失败关闭。

商业 V1 工作台至少覆盖同步积压/死信/游标、数据包损坏/版本、支付退款 UNKNOWN 与
内部对账差异、库存负数/流水缺口、成本顺序/重建、报表投影/导出以及日结差异。
外部 Provider、真实设备和打印未解阻时只显示 BLOCKED/UNAVAILABLE，不得创建外部
成功观察、真实设备修复命令或绿色占位。

Vue 页面支持可信门店筛选、队列、严重级别、来源血缘、认领、转派、处置计划、
Owner 修复状态、独立复核、关闭/重开和审计时间线；前端不得重算资金、库存、成本、
报表或日结结果。批量操作必须有权限、行数上限、二次确认、稳定幂等键和逐项结果。

使用两个虚构租户、多组织、多门店、多终端和三业态合成数据；覆盖重复/乱序/晚到、
同键异内容、跨租户/门店、并发认领、租约过期、转派竞态、Owner 超时、ACK 丢失、
修复失败、重启恢复、UNKNOWN、重新出现、摘要篡改、敏感泄漏、百万级开放案件趋势、
迁移失败和安全前向修复。合成趋势不得写成生产 SLA。

CI 继续执行治理、Server、Web、MySQL、Flutter Linux/Windows、SQLite、Android/Kotlin、
异常故障、租户权限、Secret/PII、依赖、SBOM、许可证、覆盖率和既有 Gate 全量回归；
不得降低阈值、跳过测试、自动重跑掩盖 Flaky、修改已发布迁移或创建测试后门。

T2-MEM-003 及 Gate 7D 后续需求继续 DRAFT；T2-SAA-001、T2-SUB-001、T2-SVC-001
仍须独立 CR 且继续 DRAFT。T2-PAY-002、T2-HWD-001、T2-PRN-001、T2-PAR-001
继续 BLOCKED；T2-JSH-001、T2-LIC-001 继续 DEFERRED。不得进行 Provider 网络、
真实资金、真实设备/外设、伙伴现场、完整 Alpha、生产部署或商业可用声明。

完成后提交《T2-EXC-001 独立周门禁报告》等待我确认，不得自动准入 T2-MEM-003、
Gate 7D 后续需求或 Gate 7E。完成后为我整理下一步操作指令。
```
