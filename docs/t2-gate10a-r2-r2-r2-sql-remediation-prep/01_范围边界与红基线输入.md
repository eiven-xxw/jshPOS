# 范围边界与红基线输入

## 授权与状态

- 起点：`8c65991919757cb52786cdf037b7a44f7f095c53`
- Finding：`G10A-SQL-P2-001 = OPEN`
- 相邻 Finding：`G10A-RES-P2-001 = PREPARED`
- 阶段：`DESIGN / FAILURE_TEST_SPEC / IMPACT_ANALYSIS / STARTUP_REVIEW ONLY`
- 生产 Java、Mapper/XML、SQL、索引、数据库对象、依赖、配置和迁移改动：`0`

## R2-R2-R1 可执行输入

MySQL 8.4.11、固定 10k/100k 数据分布下完成 12 条正式 Mapper 查询；批准的 1m 只运行
`RPT-SALES` 趋势。100k 结论为 3 项兼容性 CR、9 项运行时计划复核、0 项直接 GO。

| 类别 | 红基线 |
|---|---:|
| 报表兼容性 CR | 3 |
| 其他候选计划 | 9 |
| 50门店×3报表 | 150 queries |
| 500支付引用 | 501 queries |
| 500临期候选 | 501 queries |

来源 Run 为 `32968444145`，提交为 `8c65991919757cb52786cdf037b7a44f7f095c53`。该证据只证明
整改必要性，不证明性能达标、生产容量或商业 SLA。

## 不变量

- 可信 `tenant_id`、门店数据范围和服务端最终授权不变。
- 报表不重算金额、库存、成本、促销或会员权益，不覆盖 Owner 权威事实。
- 支付 `UNKNOWN` 只观察原命令，不得重新扣款或退款。
- FEFO、批次过期、积分点批次锁顺序、不可变库存/成本事实不变。
- 所有索引或数据库对象候选在获批前均为纸面设计；真正实施只能使用独立 CR 和前向迁移。
