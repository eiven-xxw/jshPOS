# 下一步操作指令（待本次准备报告确认后使用）

```text
我确认《Gate 10A-R2-R2-R2 启动评审报告》，接受准备阶段 CONDITIONAL PASS，
并分别接受 CR-T2G10A-013、CR-T2G10A-014、CR-T2G10A-015 的兼容性方向，
但其运行时仍须按串行步骤独立 VERIFIED。

按 CONDITIONAL GO 启动 Gate 10A-R2-R2-R2-R0/R1：报表失败红基线、
Reporting Owner 批量读取端口与 RPT-SALES 精确整改。

以准备阶段最终封存提交 [填写精确提交] 作为起点创建独立分支。

本批只允许先把 R2R2R2-F01/F04/F07/F08/F09/F12 建成可复现失败测试，
再实现 ReportingBatchReadPort、RPT-SALES 版本化 keyset 分页和受控流式导出；
旧契约保持冻结兼容窗口。不得提前整改 RPT-INVENTORY、RPT-PAY-REC 或其余9条查询。

若需要新增/调整索引，先停止并提交独立索引 CR 与唯一前向迁移方案，未经确认不得实施。
不得改变报表金额口径、Owner 数据主权、可信 tenant_id、权限、审计或300 API既有兼容结论。

完成后提交 R0/R1 独立周门禁报告等待我确认；G10A-SQL-P2-001继续OPEN，
不得自动进入后续查询、G10A-RES、R3、完整Alpha或生产发布。

所有外部 BLOCKED、UAT/REL DRAFT、LIC/JSH DEFERRED 和零执行边界保持不变。
```
