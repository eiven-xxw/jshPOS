# 本地验证记录

- 验证时间：2026-08-27（Asia/Shanghai）
- Java：Microsoft OpenJDK 21.0.10
- 命令：`server/mvnw -f server/pom.xml clean verify -DskipTests=false`
- 结果：50 个 Maven reactor 模块全部 `SUCCESS`，总耗时 2 分 46 秒。
- Reporting：78 项模块测试通过，包含有界分页、签名游标、流式导出恢复、Mapper 策略和租户守卫。
- Release：MySQL 8.4 探针已完成编译；本机没有 Docker，因此不生成或冒充 MySQL 可执行计划证据。
- 迁移：相对准备封存提交的 Flyway/SQLite 迁移变化为 0。

本记录只是本地补充证据。最终索引 Go/No-Go 只能使用 GitHub 固定 `mysql:8.4.11` 执行器的
10k/100k 结果；Web、Flutter 双平台、Android、安全、SBOM 和最终证据索引也以同一 GitHub
提交的完整流水线为准。
