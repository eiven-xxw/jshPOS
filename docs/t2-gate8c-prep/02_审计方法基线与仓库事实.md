# 审计方法、基线与仓库事实

## 基线

- 仓库基线：`68d94211b93156d0d87139e4ab5bef421802ad95`；
- Gate 8B 封存 Run：`32670901692`，10/10 Job 成功；
- 证据 Artifact：`9501459414`，309 文件；
- 证据索引 SHA-256：`3eb5cbfc7e9bc6b7ee7dc52e96598a23da5984f63753f9608b90d9908187396`；
- `T2-E2E-005`：`ACCEPTED`，上限仍为 `INTERNAL_COMMERCIAL_OPERATIONS_CANDIDATE`。

## 审计方法

1. RTM、AGENTS、ADR、CR、索引和证据引用交叉校验；
2. 47 个 `jshpos-*` 正式 Controller 的权限注解与 Mapper 直连静态扫描；
3. Owner 跨模块导入、OpenAPI 方法路径冲突、Flyway 文件分类、Mapper SQL 边界和核心文件规模扫描；
4. 生产配置、管理端点、附件资源、Secret/PII 与默认凭据模式复核；
5. Gate 6H 性能基线与 Gate 7/8 新增能力的时间和覆盖差距比较；
6. 许可证、自有包、签名、KMS、对象存储、灾备、外部 P0、UAT 和发布状态复核。

## 仓库事实快照

- `T2-*` 接受项：83 项（含本次确认的 `T2-E2E-005`）；
- 正式 `jshpos-*` Controller：47 个，未发现缺少权限注解或 Controller 直连 Mapper；
- Server 正式 Java 文件：945 个；Mapper/Repository：97 个；资源 XML：79 个；
- Flutter 最大文件：`checkout_local_service.dart` 3343 行；主结算页面 1257 行；
- 开放发现：P0 3 项、P1 11 项，其中内部代码 P0 1 项、商业/外部 P0 2 项；
- 本阶段运行时、迁移和依赖变更：0。

完整原子发现见 `contracts/t2/gate8c-prep/findings-register.json`。发现中不记录凭据值、商户号、证书或真实 PII。
