# Gate 8C-Prep 完成后的下一步操作指令（待项目发起人确认后使用）

我确认《T2 Gate 8C-Prep 启动评审报告》，接受准备阶段 CONDITIONAL PASS。

按 CONDITIONAL GO 启动 T2 Gate 8C / Sprint S26-A：T2-SEC-002 生产安全配置与资源边界加固。

以 Gate 8C-Prep 最终封存提交作为工作分支起点，创建独立分支：
`t2/gate8c-sprint26a-sec002-runtime`。

本批只允许逐项准入 `T2-SEC-002`。先创建并接受独立运行时 ADR，再按以下顺序完成设计准入、实现和独立 `VERIFIED`：

1. 清除生产配置中的静态默认凭据/令牌，统一改为受控 Secret 引用；可选集成默认关闭，生产缺少必需 Secret 必须启动失败；
2. 将 Actuator 生产暴露收敛为明确白名单，健康详情按授权显示，保留独立认证、审计和回归；
3. 服务附件必须先校验请求大小并改为受限流式处理，覆盖 10 MiB 上限、并发内存、临时文件回收、摘要、租户命名空间和失败清理；
4. 增加默认凭据、Secret/PII、权限、租户、管理端点、资源耗尽和生产配置启动测试；
5. 运行治理、Server、Web、Flutter Linux/Windows、Android/Kotlin、MySQL、SQLite、安全、依赖、SBOM、许可证、覆盖率及既有 Gate 全量回归。

不得降低阈值、跳过失败测试、自动重跑掩盖 Flaky、修改已发布迁移或新增业务能力。发现新的业务需求时必须另行 CR 和 Requirement ID。

`T2-MTN-001/T2-PERF-002/T2-RDY-001` 继续 `DRAFT`；`T2-PAY-002/HWD-001/PRN-001/PAR-001` 继续 `BLOCKED`；`T2-UAT-001/REL-001` 继续 `DRAFT`；`T2-LIC-001/JSH-001` 继续 `DEFERRED`。不得进行 Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、生产部署或商业声明。

完成后提交《T2-SEC-002 独立周门禁报告》等待确认，不得自动进入可维护性、性能或发布整改。
