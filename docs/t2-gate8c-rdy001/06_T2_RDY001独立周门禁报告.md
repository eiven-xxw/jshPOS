# T2-RDY-001 独立周门禁报告

## 当前状态

`VERIFIED CANDIDATE / AWAITING COMPLETE CI`。T2-RDY-001 已完成独立 ADR、设计准入、
实现、本地验证和完整 CI 编排；项目发起人确认前不得更新为 `ACCEPTED`。

## 门禁矩阵

| 门禁 | 当前结果 | 完整 CI 责任 |
|---|---|---|
| Governance Ubuntu/Windows | 本地 PASS | 双平台 UTF-8、范围、RTM、契约与依赖差异 |
| Server | 本地 PASS | 54 模块全量、覆盖率、JAR 与 CycloneDX |
| Web | 本地 PASS | 审计、构建、Lint、类型、组件与许可证 |
| Flutter/SQLite/Android | CI 待执行 | Linux/Windows、覆盖率、Kotlin 与 Debug APK |
| MySQL/Operations | CI 待执行 | V1—当前前向迁移、恢复、回退与部署配置 |
| Security | CI 待执行 | 漏洞、Secret/PII、许可证、工作流与配置扫描 |
| Release readiness | 本地冒烟 PASS | 同 Run 正式制品、临时签名验真和 14 个故障向量 |
| Evidence | CI 待执行 | 九类具名生产者、摘要、证据等级和零外部执行 |

首轮完整 CI Run `32712716043` 的 `mysql-operations` 因发布测试仍固定 V56、而当前
依赖闭包已暴露 71 项合法迁移而失败。失败 Run 与 Job `97387423722` 保留；修复不重跑
失败 Job，而以全 Owner V1—V86 空库迁移目录从头复跑完整工作流。

第二轮 Run `32714332457` 在正式集成回调执行前因空库夹具缺少 `sys_role_menu` 失败；
失败 Run 与 Job `97392319526` 同样保留。最小修复仅补框架前置表，不修改回调或迁移，
后续提交继续从头复跑。

第三轮 Run `32714734191` 已通过全量迁移、回调和表元数据主体检查，但旧白名单未登记
Gate 8A 七张平台全局/租户派生表而失败；Job `97393795481` 保留。修复同时强制父聚合
外键与 `authority_scope`，不会把普通租户业务表加入豁免。

第四轮 Run `32715341831` 已通过全量迁移与租户元数据防护，随后 Compose 静态预检因 CI
未注入合成应用账号 `MYSQL_PASSWORD` 而按设计失败关闭；Job `97395293820` 保留。修复只在
隔离 Job 中补齐 synthetic 应用账号，生产式缺 Secret 失败关闭规则保持不变。

## 整改结果

1. 建立 10 项发布物及唯一身份、摘要、来源和非生产标识；
2. 建立确定性压缩包、发布 BOM、provenance、部署预检和机器可读决策；
3. 建立合成临时 Ed25519 签名，私钥只存在于临时目录并在上传前销毁；
4. 整改自有设备适配包许可/变更日志占位，但不关闭第三方许可；
5. 建立 14 个失败关闭向量和不可变证据索引；
6. 保留商业许可证、外部 P0、真实发布链路与 UAT/REL 阻断。

## 状态与证据边界

- T2-RDY-001：`VERIFIED` 候选；
- T2-PAY-002/HWD-001/PRN-001/PAR-001：`BLOCKED`；
- T2-UAT-001/REL-001：`DRAFT`；T2-LIC-001/JSH-001：`DEFERRED`；
- Provider 网络、真实资金、设备/外设、伙伴现场、完整 Alpha、生产部署和商业 tag：0；
- 最高结论：`INTERNAL_RELEASE_READINESS_CANDIDATE`，不是生产或商业可用。
