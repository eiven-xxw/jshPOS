# 下一步操作指令

建议项目发起人复制确认：

> 我确认《Gate 10A-R2-R2-R2-R0/R1 独立周门禁报告》，接受 RPT-SALES 功能、兼容、查询数、
> 数据完整性与租户边界的内部软件执行结果，同时接受当前 `CONDITIONAL_NO_GO_PENDING_INDEX_CR`。
>
> 我确认 `CR-T2G10A-018` 的 `CONDITIONAL GO`，仅授权 Gate 10A-R2-R2-R2-R1-INDEX：通过唯一
> 前向迁移 `V202608260088` 新增 `idx_rpt_sales_keyset(tenant_id, projection_version,
> business_date, store_id, terminal_id, cashier_id, currency)`。
>
> 先增加失败回归，再实施迁移；不得修改 V1—V87、SQL/Mapper、API、事件或业务语义，不得删除
> 既有索引。完成空库/V87升级/重复执行/Flyway validate/MySQL 8.4.11，并在 10k/100k 复验
> 无全扫、无 filesort、查询数预算、0 重复/缺失/越权和金额守恒。完整 CI 从新提交执行。
>
> `G10A-SQL-P2-001` 继续 OPEN，`G10A-RES-P2-001` 继续 PREPARED；未经我再次确认不得进入
> RPT-INVENTORY、RPT-PAY-REC、RES、R3、完整 Alpha 或生产发布。
