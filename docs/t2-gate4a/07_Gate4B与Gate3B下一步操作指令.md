# Gate 4B 与 Gate 3B 下一步操作指令

> 文档编号：JSH-POS-T2-G4A-NEXT-007
> 日期：2026-08-17
> 当前状态：`PREPARED / NOT ADMITTED`

## 1. 推荐路线

Gate 4A 接受后继续双轨，但两轨不得混用证据：

1. Gate 3B 外部轨保持 `T2-PAY-002=BLOCKED`。只有真实资料齐备并通过独立解阻评审，才可建立首接 Provider 沙箱适配器和网络调用。
2. Gate 4B 内部轨建议只准入 `T2-INV-003` 盘点与 `T2-PUR-001` 采购收货/退货。两者都只能通过已接受的不可变库存账本生成调整、收货或退货流水，禁止直接覆盖余额。
3. `T2-CST-001` 成本和 `T2-TRF-001` 调拨本 Sprint 继续设计、契约与测试准备。成本必须在采购收货事实稳定后实现；调拨必须在门店仓、在途和双方幂等模型冻结后实现。

## 2. 支付沙箱解阻所需输入

请在单独安全渠道准备首接 Provider 的：具体产品与接入模式、授权非生产商户、测试终端、官方接口目录及版本、签名/验签和证书规则、查询/关闭/退款/退款查询、回调重试、防重放、账单样例、错误码、限流、IP 白名单、技术联系人和工单 SLA。仓库和聊天中只记录引用、版本、保管人和脱敏样例，不粘贴密钥或证书私钥。

## 3. 建议复制的下一步指令

```text
我确认《T2 Gate 4A / Sprint S5 周门禁报告》，接受 Gate 4A CONDITIONAL PASS。

同意将以下需求由 VERIFIED 更新为 ACCEPTED：
- T2-INV-001
- T2-INV-002
- T2-INV-004

T2-PAY-002 继续 BLOCKED。Gate 3B-Prep 继续只允许资料核验；未经我确认独立《支付沙箱解阻评审报告》，不得进行任何 Provider 网络调用。

按 CONDITIONAL GO 启动 T2 Gate 4B / Sprint S6，仅允许逐项准入：
- T2-INV-003：盘点快照、复核、差异审批和幂等调整流水
- T2-PUR-001：供应商、采购订单、收货/退货和幂等库存流水

T2-CST-001、T2-TRF-001 本 Sprint 只允许补充领域模型、状态机、不变量、API/事件、Flyway 设计、固定合成向量和验收用例，不允许正式实现。

实施前必须冻结盘点快照时点、账实差异、复核与四眼审批、调整来源幂等；同时冻结供应商/采购单/收货单数据主权、部分收货、超收策略、退货、单位换算、税额边界和库存事务。所有库存效果必须调用已接受的库存 Owner，不得直接更新 inv_stock_balance 或修改历史流水。

继续使用两个虚构租户、多门店、多仓库和三类业态合成数据；覆盖并发盘点、销售穿越盘点时点、重复审批、重复收货、部分/超量收货、退货上限、跨租户污染、迁移失败和前向修复。CI 不得降低现有覆盖率、安全、SBOM、许可证和证据门禁。

继续禁止支付 Provider 网络、生产密钥、真实资金、未脱敏数据、成本、调拨、促销及后续 Gate 正式编码；不得用 Fake 解除 SANDBOX、REAL_DEVICE 或 PILOT 阻断，不得宣称 Alpha、可试点或可商用。

完成后提交《T2 Gate 4B / Sprint S6 周门禁报告》，等待我确认。
```
