# Gate 4B / Sprint S6 范围、非目标与逐项准入

## 1. 正式准入

| Requirement | 正式能力 | 准入结果 |
|---|---|---|
| `T2-INV-003` | 动态/盲盘快照、不可变计数、复盘、复核、审批和盘盈盘亏流水 | READY → IN_PROGRESS |
| `T2-PUR-001` | 供应商、采购订单、审批、分批收货、拒收、容差超收和原收货退货 | READY → IN_PROGRESS |

两项均已完成数据主权、状态、不变量、权限、审计、API、事件、Flyway、容量、回退和测试准入。

## 2. 仅设计准备

- `T2-CST-001`：只冻结成本输入事件、精度、幂等和重建验收，不创建 `inv_cost_*` 运行时。
- `T2-TRF-001`：只冻结申请/审批/发出/在途/收货/差异状态与测试向量，不创建调拨运行时。

## 3. 非目标

支付 Provider 网络、真实资金、生产密钥、无原单采购退货、询价、采购申请、应付结算、发票、移动平均成本、调拨、促销、PDA 离线作业、批次效期和商业试点均不在本 Sprint。`T2-PAY-002` 继续 `BLOCKED`。
